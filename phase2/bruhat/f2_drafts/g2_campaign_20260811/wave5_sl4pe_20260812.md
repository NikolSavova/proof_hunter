# wave5_sl4pe — Hypothesis SL4'-E: the computed-eta pricing machinery, proved at m >= 561

*Wave-5 prover deliverable (closing wave), F2 campaign, 2026-08-12. Single
assignment: hypothesis **SL4'-E** — the computed-eta model-term pricing that
the SL4' assembly (`wave4_sl4p_20260812.md` §3/§4 "main" row) consumes; the
orphan Part A mechanism, referenced but never independently proved. Target
threshold: the sliver-shifted CL obligation `m >= 561` (STATUS_wave4 §2).
Substrate consumed at two-referee MINOR_REPAIRS via `wp4_draft_composite.md`:
Theorem A2(ii)/(iii) only (the `A >= c_A(W) m` band floors and
`s2 >= 1122800/7921`). Read for this deliverable: `STATUS_wave4.md`,
`wave4_sl4p_20260812.md`, `referee_numerics_wave4_sl4p.md` (+ its eta script
and archived output), the composite's §5.3 SL4' display, and the prover/
referee eta scripts `sl4p_nc2_eta.py` / `ref_nw4p_c_eta_edges.py`. NOT read:
`g2_draft_t1_20260803.md`, any other `wave5_*` hypothesis draft (blind
protocol), `wave4_sl4p_repaired_*`. No existing file modified. `gamma = 1/8`
not re-litigated. New scripts (all SAVED and RUN 2026-08-12, outputs archived
beside them) in `g2_scripts/campaign_20260811/wave5_sl4pe/`:
`e1_pricing_certificate.py` (exact-rational certificate),
`e2_truth_margins.py`, `e3_limit_sign.py`.*

**Bottom-line status: PARTIAL, with the assigned machinery PROVED and a
prominent interface DELTA.** The pricing machinery — the exact `qhat`
algebra and the conversion of banded cumulant bounds into
`|eta| <= [R42*(W)/2 + 0.3 R31*(W)^2 + lam^2/2] u` — is **PROVED** below
with explicit named constants, uniformly for all `m >= 561`, by an
exact-rational interval certificate (Lemmas E.1–E.2, Theorem E; script [1]
computes every constant in `Fraction` arithmetic; the only transcendental
input is `e^x <= 1/(1-x)`). **BUT the statement as recorded in STATUS_wave4
§2 item 2 needs correction, and the delta is load-bearing:**

> **DELTA FLAG (against STATUS_wave4 §2 item 2 and `wave4_sl4p_20260812.md`
> §3 (SL4'-E)).** The recorded proof plan — "exact `qhat` algebra + a
> `kappa_4 >= 0` sign lemma", consuming SL1'-w(i) — **cannot prove the
> pricing.** Proposition E.3 exhibits a parameter point satisfying ALL of:
> the [A2] floors, SL1'-w(i) (both clauses), and `kappa_4 >= 0`, at an
> in-band `(m, lam) = (561, 4.5/561)`, where the pricing FAILS by 43%
> (`|eta|/(price*u) = 1.4299`; script [2] block [D]). The pricing genuinely
> requires a JOINT upper bound on the cancellation combination
> `J := (kappa_3 lam/s2)^2 - (kappa_4 lam^2/s2)/2` — hypothesis **(E3)**
> below, proposed ledger name **SL4'-E-J** — which is strictly more than
> SL1'-w(i) + sign lemma deliver. Truth-side confirmation that no weaker
> interface can work: at the W1 right edge (`w = 5`, `m = 561`) the TRUE
> `r31^2 = 0.7857` already exceeds the entire lower-side budget
> `J* = 0.7`, so any proof discarding `kappa_4`'s positive size (which is
> exactly what "sign lemma only" does) dies at W1; the true `r42 = 0.6506`
> is what rescues it (`J = 0.4603 <= 0.6829 = J0(W1)`). Consequences:
> (i) Theorem SL4's hypothesis surface should replace "SL4'-E + sign lemma"
> by "(E3)" (the sign lemma is DOWNGRADED to supporting structure — it is
> neither sufficient nor, given (E3), necessary for the pricing);
> (ii) the SL1' deliverable's target list must ADD (E3). With (E1)–(E3) in
> hand, Theorem E delivers SL4'-E verbatim — same price constants, so **no
> number in the SL4' ledger moves** (§7).

Proved en route: Lemma E.4 — the limit-level `kappa_4` positivity (the
`lam -> 0` sign boundary sits at `w* = 3.3672 < 4`, with `G_4(4) =
0.23234 > 0` and `G_4` strictly increasing on `[4, oo)` by a term-wise
inequality) — the quantitative in-band sign lemma reduces to an `O(lam)`
discretization-control step, recorded for the SL1' prover. Hypothesis (E3)
itself: CONJECTURED, with strong `m >= 561` evidence (worst measured margin
32.6%, at the predicted W1 right edge; scripts [2]/[3]).

## 0. Notation and consumed inventory

Notation = composite §0 / `wave4_sl4p_20260812.md` §0 verbatim: `s2 =
Var(S_lam)`, `kappa_3, kappa_4` the third/fourth cumulants of `S_lam`,
`w = m lam`, bands W1–W7 = `w in (4,5]/(5,6]/(6,8]/(8,10]/(10,20]/(20,40]/
(40, 0.89 m]`, `A = lam^2 s2`, `u = 1/A`, mirror WLOG `lam > 0`, CL scope
`m >= 561` (post-sliver; STATUS_wave4 §2), `|lam| in (4/m, 0.89]`. Banded
scales (SL1'-w(i), consumed as hypothesis, unchanged): `R31* = 1.0/1.2/1.5/
1.7/2.0/2.1/2.2`, `R42* = 0.8/1.4/2.6/3.5/5.2/6.0/6.6`. Normalized true
ratios: `r31 := |kappa_3| lam/s2`, `r42 := kappa_4 lam^2/s2` (signed).

Consumed as PROVED via the composite (two-referee): **[A2](ii)** `A >=
c_A(W) m`, `c_A = 0.28/0.35/0.42/0.52/0.60/0.70/0.80`; **[A2](iii)** `s2 >=
S0 := 1122800/7921 = 141.7497...` on the whole band. Nothing else is
consumed as proved; in particular nothing from SL3', SL4'-X, the sliver, or
any part of Theorem A (no circularity: `eta` is a model-side object).

The model (the orphan Part A closed forms, as consumed byte-identically by
`sl4p_nc2_eta.py` and `ref_nw4p_c_eta_edges.py`): with `eps := 1/sqrt(s2)`,

```
qhat_d = e^{-d^2/(2 s2)}/sqrt(2 pi s2) * [ 1 + a He3(d eps) + b4 He4(d eps) + c6 He6(d eps) ] ,
a = kappa_3/(6 s2^{3/2}) ,  b4 = kappa_4/(24 s2^2) ,  c6 = kappa_3^2/(72 s2^3) = a^2/2 ,
eta := s2 (qhat_0^2 - qhat_{-1} qhat_{+1}) / (qhat_{-1} qhat_{+1}) - 1 .
```

(The identity `c6 = a^2/2` is exact and is used throughout. The bridge from
`qhat` to the inversion integrals of the Edgeworth model `psi` is the
orphan's closed-form-vs-quadrature check, rel. err. `6.6e-31`, reconfirmed
byte-identical by both wave-3 referees and again by the wave-4 sl4p referee;
this file prices `eta` of the closed forms, which is exactly what Lemma
SL4'.1's model term consumes.)

**The hypothesis interface (all named; (E1)+(E2) = SL1'-w(i) verbatim):**

```
(E-A2)  s2 >= S0 = 1122800/7921   and   A = lam^2 s2 >= A0(W) := c_A(W) * 561    [PROVED input, m >= 561]
(E1)    |kappa_3| <= R31*(W) s2 / lam                                            [SL1'-w(i), CONJECTURED]
(E2)    kappa_4 <= R42*(W) s2 / lam^2                                            [SL1'-w(i), CONJECTURED]
(E3)    J := r31^2 - r42/2 <= J0(W) := J*(W) - REM*(W) ,  J*(W) := R42*(W)/2 + 0.3 R31*(W)^2
                                                                                 [NEW (SL4'-E-J), CONJECTURED]
```

No lower bound on `kappa_4` is assumed: (E3) alone (with `r31^2 >= 0`)
forces `r42 >= -2 J0(W) >= -2 J*(W)` internally (Lemma E.2 Step 0), so (E2)
can stay in SL1'-w(i)'s one-sided form and the sign lemma is not part of
the interface.

## 1. The statement proved (and its relation to the recorded one)

**Theorem E (eta pricing at the shifted threshold; PROVED).** *Fix
`m >= 561` and `lam in (4/m, 0.89]`, let `W = W(m lam)` be the band, and
let `(s2, kappa_3, kappa_4)` be ANY reals satisfying (E-A2), (E1), (E2),
(E3). Then `qhat_{-1} qhat_{+1} > 0` (so `eta` is well-defined) and*

```
|eta| <= [ R42*(W)/2 + 0.3 R31*(W)^2 + lam^2/2 ] u ,     u = 1/A ,
```

*i.e. exactly the SL4'-E pricing consumed by Theorem SL4' — same constants,
same `u`-units. In particular the conclusion holds for the true cumulants of
`S_lam` at every interior `k`, for every `m >= 561`, as soon as SL1'-w(i)
and (E3) are proved for them ((E-A2) already holds by [A2]).*

Relation to the recorded SL4'-E: the inequality is IDENTICAL (STATUS_wave4
§2 item 2, `wave4_sl4p_20260812.md` §3). What changes is the input list:
`{exact algebra + sign lemma + SL1'-w(i)}` (recorded, INSUFFICIENT — §5) is
replaced by `{(E1), (E2), (E3)}` with (E3) new. The composite §5.3's SL4'
display writes the third price term as `1/(2 s2)`; note `(lam^2/2) u =
1/(2 s2)` exactly — same object, the wave-4 bracket-times-`u` form is the
one proved here.

## 2. Lemma E.1: the exact algebra

**Lemma E.1 (exact identity).** *With `eps2 := 1/s2 = eps^2` and the closed
forms of §0, define*

```
N   := 1 + 3 b4 - 15 c6 ,
D   := (1 + b4 h4 + c6 h6)^2 - a^2 h3^2 ,          h3 = eps^3 - 3 eps ,
Sig := 2 + b4 (3 + h4) + c6 (h6 - 15) ,            h4 = 3 - 6 eps2 + eps2^2 ,
C_b := (6 - eps2) Sig ,                            h6 = eps2^3 - 15 eps2^2 + 45 eps2 - 15 ,
C_a := (3 - eps2)^2 - (45 - 15 eps2 + eps2^2) Sig / 2 .
```

*Then, exactly:*

```
(i)   eta = s2 ( e^{eps2} N^2 / D - 1 ) - 1 ;
(ii)  N^2 - D = eps2 [ C_b b4 + C_a a^2 ] ;
(iii) eta / u = lam^2/2 + rho1 A + (e^{eps2}/D) [ (C_b/24) r42 + (C_a/36) r31^2 ] ,
      rho1 := s2 (e^{eps2} - 1) - 1 - eps2/2 = sum_{n >= 3} eps2^{n-1}/n!  >= 0 .
```

*Proof.* (i) At `d = 0`: `He3(0) = 0`, `He4(0) = 3`, `He6(0) = -15`, so
`qhat_0 = N / sqrt(2 pi s2)`. At `d = ±1`: `z = ±eps`, and `He4, He6` are
even, `He3` odd, so `qhat_{±1} = e^{-eps2/2} (1 ± a h3 + b4 h4 + c6 h6) /
sqrt(2 pi s2)`; the product is `e^{-eps2} D / (2 pi s2)`, and `qhat_0^2 /
(qhat_- qhat_+) = e^{eps2} N^2/D`; (i) follows from `eta = s2 (qhat_0^2/
(qhat_-qhat_+) - 1) - 1`. (ii) `N^2 - D = [N^2 - (1+x)^2] + a^2 h3^2` with
`x := b4 h4 + c6 h6`; factor `N^2 - (1+x)^2 = (N - 1 - x)(N + 1 + x)`;
`N + 1 + x = Sig` and `N - 1 - x = b4 (3 - h4) - c6 (15 + h6)`; now
`3 - h4 = eps2 (6 - eps2)`, `15 + h6 = eps2 (45 - 15 eps2 + eps2^2)`,
`h3^2 = eps2 (3 - eps2)^2`, and substituting `c6 = a^2/2` collects to (ii).
(Guard: script [1] block [0] verifies (ii) as an exact `Fraction` identity
at 6 pseudo-random rational tuples — `identity holds at all exact rational
tuples: True`.) (iii) Split `eta = [s2(e^{eps2}-1) - 1] + s2 e^{eps2}
(N^2 - D)/D`, use (ii) with `s2 eps2 = 1`, multiply by `A`, and use
`A eps2/2 = lam^2/2`, `A b4 = r42/24`, `A a^2 = r31^2/36`. The series form
of `rho1` is termwise. ∎

## 3. Lemma E.2: the uniform remainder envelope at m >= 561

**Lemma E.2 (certified envelope).** *Under (E-A2), (E1), (E2), (E3), with
`E0 := 1/S0 = 7921/1122800` and the band caps `Lam(W) := wmax(W)/561` for
W1–W6b (`wmax = 5/6/8/10/20/40`), `Lam(W7) := 89/100`, all of the following
hold, with every constant an explicit rational computed in script [1]
(floats shown; the archived fractions are authoritative):*

*Step 0 (two-sided `r42` cap).* `r42 <= R42*` by (E2); and (E3) + `r31^2 >=
0` give `r42 >= 2(r31^2 - J0) >= -2 J0 >= -2 J*`, so `|r42| <= R42†(W) :=
max(R42*, 2 J*)`.

*Step 1 (parameter caps).* `eps2 <= E0`; `|b4| = |r42|/(24 A) <= bb :=
R42†/(24 A0)`; `a^2 = r31^2/(36 A) <= aa := R31*^2/(36 A0)`; `c6 = a^2/2`.

*Step 2 (interval bounds; all elementary).* `|h4| <= 3`, `|h6| <= 15`,
`|3 + h4| <= 6`, `|h6 - 15| <= 30`, `h3^2 <= 9 E0`, whence
`|Sig - 2| <= sb := 6 bb + 30 (aa/2)`, `|D - 1| <= db := 2 xb + xb^2 +
9 E0 aa` with `xb := 3 bb + 15 (aa/2)`, and `1 <= e^{eps2} <= 1/(1 - E0)`
(so `|e^{eps2}/D - 1| <= ph := (E0/(1-E0) + db)/(1 - db)`). Positivity:
`D >= (1 - xb)^2 - 9 E0 aa > 0`, `qhat_0 propto N >= 1 - 3bb - 15(aa/2) >
0`, `qhat_± propto 1 ± a h3 + x >= 1 - xb - (aa + 9E0)/2 > 0` (via `2|a h3|
<= a^2 + h3^2`) — all checked exactly per band (script [1]: `positivity
(D,qhat): True`).*

*Step 3 (coefficient deviations).* `|C_b/24 - 1/2| <= e_b` and
`|C_a/36 + 1| <= e_a`, where `e_b, e_a` come from the exact endpoint
products (`C_b in [(6-E0)(2-sb), 6(2+sb)]`, `C_a in [9 - 6E0 - 45(1+sb/2),
9 - (45-15E0)(1-sb/2)]`).

*Step 4 (main-term caps).* Write `M := (C_b/24) r42 + (C_a/36) r31^2` and
`M0 := r42/2 - r31^2`. Then `|M - M0| <= e_b R42† + e_a R31*^2` and
`|M0| <= M0cap := max(R42*/2, J*)` (upper from (E2) + `r31^2 >= 0`; lower
`-M0 = J <= J0 <= J*` from (E3)).

*Step 5 (the envelope).* With `REM2(W) := (1 + ph)(e_b R42† + e_a R31*^2) +
ph * M0cap` and `d1(W) := Lam^2 E0 / (6 (1 - E0/4))` (which bounds
`rho1 A`, since `rho1 <= (eps2^2/6)/(1 - eps2/4)` by the ratio test and
`rho1 A <= eps2 lam^2/(6(1-eps2/4))`):*

```
| eta/u - ( lam^2/2 + r42/2 - r31^2 ) |  <=  REM*(W) := REM2(W) + d1(W) .
```

*Certified values (script [1], verbatim block [2]):*

```
  REM*(W): 0.017058 / 0.029319 / 0.05938 / 0.080548 / 0.13207 / 0.14494 / 0.15603
  J0(W)  : 0.682942 / 1.10268 / 1.91562 / 2.53645 / 3.66793 / 4.17806 / 4.59597
```

*and the certified upper-side slack check `REM*(W) <= 0.3 R31*(W)^2` prints
True on every band.*

*Proof.* Steps 0–4 are the displayed inequalities (each is a one-line
monotone interval bound; the products in Steps 2–3 multiply intervals of
positive numbers). Step 5: by Lemma E.1(iii), `eta/u - (lam^2/2 + M0) =
rho1 A + (e^{eps2}/D) M - M0`, and `|(e^{eps2}/D) M - M0| <= |e^{eps2}/D -
1| |M0| + (1 + |e^{eps2}/D - 1|) |M - M0| <= ph M0cap + (1+ph)(e_b R42† +
e_a R31*^2)`. All constants are evaluated once per band in exact rational
arithmetic at the worst case (`A = A0(W)`, `eps2 = E0`, `lam = Lam(W)`),
each of which is the monotone extreme of its range at `m >= 561`: `A >=
c_A(W) m >= A0(W)` and `lam <= wmax(W)/m <= Lam(W)` for every `m >= 561`
(W7: `lam <= 0.89` by CL scope). Hence a SINGLE constant set covers all
`m >= 561` — no separate monotonicity argument is needed. ∎

Truth-side sanity (script [2], `REMact` column): at the true cumulants the
actual deviation is `<= 9.97e-5` on W1 and `<= 9.59e-4` at the W7 deep
corner — the certified envelope is 100x–1000x looser than truth and still
30x–100x smaller than the budgets it must fit under; nothing is tight here.

## 4. Theorem E: proof

*Proof of Theorem E.* Well-definedness is Lemma E.2 Step 2. By Lemma
E.1(iii) and Lemma E.2,

```
lam^2/2 + (r42/2 - r31^2) - REM2  <=  eta/u - rho1 A  ,   and
eta/u  <=  lam^2/2 + d1 + (r42/2 - r31^2) + REM2 .
```

*Upper side:* `r42/2 - r31^2 <= R42*/2` by (E2), so `eta/u <= lam^2/2 +
R42*/2 + REM2 + d1 = lam^2/2 + R42*/2 + REM* <= lam^2/2 + R42*/2 +
0.3 R31*^2` by the certified check `REM* <= 0.3 R31*^2` (script [1], every
band True).

*Lower side:* `r42/2 - r31^2 = -J >= -J0 = -(J* - REM*)` by (E3), and
`rho1 A >= 0`, so `eta/u >= lam^2/2 - J* + REM* - REM2 = lam^2/2 - J* + d1
>= -J* >= -(J* + lam^2/2)`.

Both sides give `|eta| <= (J* + lam^2/2) u = [R42*/2 + 0.3 R31*^2 +
lam^2/2] u`. ∎

Remark (where the slack sits): the upper side spends `REM*` out of the
`0.3 R31*^2` term and nothing else (worst case W1: `0.0171` of `0.3`); the
lower side has structural slack `lam^2 + d1` and spends nothing — ALL of
the lower-side content is hypothesis (E3). The `0.3` coefficient is
consumed twice, in different roles: as upper-side remainder headroom and as
part of the lower-side budget `J*`. This is exactly the "partial
cancellation" mechanism the wave-4 draft described qualitatively.

## 5. The delta, made rigorous: non-derivability and the sign lemma's true role

**Proposition E.3 (the recorded interface cannot prove the pricing).**
*There exist reals `(s2, kappa_3, kappa_4)` and an in-band pair `(m, lam)`
with `m >= 561` satisfying (E-A2), (E1), (E2), AND `kappa_4 >= 0` (the sign
lemma's conclusion), for which the pricing inequality FAILS. Hence no
argument consuming only {exact `qhat` algebra, `kappa_4 >= 0`, SL1'-w(i),
[A2]} — the proof plan recorded in STATUS_wave4 §2 item 2 — can prove
SL4'-E.*

*Proof.* Take `m = 561`, `w = 4.5` (W1), `lam = 4.5/561`, `s2 = A0(W1)/
lam^2` (so `A = A0 = 157.08` exactly, in `[c_A m, m]`; `s2 = 2.4413e6 >=
S0`), `kappa_3 = R31*(W1) s2/lam` ((E1) with equality), `kappa_4 = 0`
((E2) and the sign conclusion hold). Script [2] block [D] (dps 30):

```
  eta/u = -1.000964   price = 0.700032   |eta|/(price*u) = 1.4299  ** > 1 ...
```

∎

Two scope notes, both load-bearing for the SL1' prover. (a) *The truth is
adversarial in the same direction:* at the TRUE cumulants of the W1 right
edge (`m = 561`, `w = 5.0`; script [2] block [B]) `r31 = 0.8864`, so
`r31^2 = 0.7857 > 0.7 = J*(W1)`: even perfect knowledge of `kappa_3` plus
`kappa_4 >= 0` overshoots the whole lower-side budget — the pricing
survives there only because `r42 = 0.6506` cancels (`J = 0.4603`). So (E3)
is not an artifact of my proof route; retaining `kappa_4`'s positive SIZE
jointly with `kappa_3^2` is forced by the truth at W1 (and, by the same
measurements, at W2: block [F]). (b) *The sign lemma is downgraded, not
discharged:* given (E3), Theorem E needs no lower bound on `kappa_4` at all
(Lemma E.2 Step 0 manufactures one); without (E3), the sign lemma is
insufficient by the Proposition. Its remaining value is as supporting
structure en route to (E3) (below) and for SL1'-w's own internal needs.

**Lemma E.4 (limit-level fourth-cumulant positivity; PROVED, flagged
class).** *For `x > 0` let `phi_n(x) := sum_{k>=1} k^{n-1} e^{-kx}` (the
`n`-th cumulant of the geometric factor), `h_4(x) := x^4 phi_4(x)`, and*

```
G_4(w) := 6w - 4 pi^2 + sum_{k>=1} e^{-kw} P(kw)/k^2 ,   P(y) = y^4 + 4y^3 + 12y^2 + 24y + 24 .
```

*Then: (i) `kappa_4 lam^4 = m h_4(lam) - sum_{j=1}^m h_4(j lam)` exactly
(per-factor cumulants `k_4^{(j)} = phi_4(lam) - j^4 phi_4(j lam)`, from
`log g_j(x) = log(1 - e^{-jx}) - log(1 - e^{-x})`); (ii) at fixed `w =
m lam`, `lam -> 0`: `lam^5 kappa_4 -> G_4(w)` (Riemann sums; `h_4(0+) = 6`,
`G_4(w) = int_0^w (6 - h_4)`); (iii) for `x >= 4`, `x phi_5(x) - 4 phi_4(x)
= sum_k k^3 e^{-kx} (kx - 4) >= 0` term-wise, so `h_4' = x^3 (4 phi_4 -
x phi_5) <= 0` and `G_4'(w) = 6 - h_4(w) >= 6 - h_4(4) = 0.5798 > 0` on
`[4, oo)`; (iv) `G_4(4) = 0.23234829889 > 0.23` and `h_4(4) = 5.420211696
< 6` (dps-30 evaluations with the series truncated at `K = 300` and an
explicit tail bound `< 6.08e-516`; script [3] block [1] — the campaign's
flagged certificate class for these two point evaluations; everything else
is exact). Hence `G_4(w) >= 0.2323 > 0 for every w >= 4`: the `lam -> 0`
sign boundary of `kappa_4` sits at `w* = 3.3672 < 4` (bracketed by sign
change, `G_4(3.3) = -0.0117 < 0 < G_4(3.5) = 0.0293`; block [2],
record-only), and the in-band limit clears it by `0.63` in `w`.*

What remains for the FULL in-band sign lemma (not needed by Theorem E;
recorded for SL1'): a discretization-control step `|lam^5 kappa_4 - G_4(w)|
<= C_EM lam` with explicit `C_EM` (trapezoid/Euler–Maclaurin on `h_4` with
an `int |h_4''|` bound), which at `m >= 561` (`lam <= w/561`) converts
`G_4 >= 0.2323` into `kappa_4 > 0` for `w >= 4` outright once
`C_EM lam < 0.2323` — plus a separate large-`lam` treatment on W7 (where
positivity has enormous measured margin, `r42 >= 5.46`; script [2]).

## 6. Hypothesis (E3): status, evidence at m >= 561, and the prover roadmap

**(E3) [SL4'-E-J] is CONJECTURED.** It is a truth statement about the
Mahonian cumulants (SL1'-class, same natural home as SL1'-w), NOT provable
from SL1'-w(i) (Proposition E.3; quantitatively: even ADDING the sign lemma,
(E1)+(E2)+`kappa_4 >= 0` allow `J` up to `R31*^2`, and `R31*^2 > J0` on
every band — `1/1.44/2.25/2.89/4/4.41/4.84` vs the J0 row above).
Evidence at the shifted threshold, all from script [2] (dps 30, closed-form
factor cumulants, implementation guarded byte-consistent against the
archived `out_sl4p_nc2.txt` at two points — block [A]):

1. **27 adversarial probes at `m = 561`** (every band edge from both sides,
   interiors, the exact `lam = 0.89` corner `w = 499.29`): every check
   PASSes — `J <= J0(W)`, pricing ratio `<= 1`, `REMact <= REM*(W)`,
   `r31 <= R31*`, `r42 <= R42*`, `kappa_4 > 0`. Verbatim:
   `ALL checks PASS at m = 561: True;  worst pricing ratio = 0.6576`,
   `worst J/J0 by band: W1=0.6740 W2=0.4991 W3=0.3470 W4=0.2853 W5=0.2297
   W6b=0.2203 W7=0.2885`. The binding band is W1 (margin 32.6%), at the
   right edge `w = 5.0`, exactly where the referee's F3 found the pricing
   ratio maximum (`0.6579` at `m = 401`; here `0.6576` at `m = 561` —
   consistent, slightly decreasing in `m`).
2. **`m`-direction:** `J(w = 5.0) = 0.4603 / 0.4601 / 0.4599` at `m = 561 /
   1000 / 5000` vs the `lam -> 0` limit `0.45984` (script [3]): decreasing
   in `m` toward the limit, so the threshold `m = 561` is the measured
   worst case on W1–W6b. On W7's deep-tilt direction `J` INCREASES with `m`
   (`1.3258 / 1.3288 / 1.3318` at `m = 561/1000/5000`, `lam = 0.89`) toward
   the geometric-limit value `~1.332` — vs `J0(W7) = 4.5960`, margin 71%.
3. **`w`-direction:** fine scan on W1 (`w = 4.00(0.05)5.00`) and W7 scan to
   the corner: `max J = 0.4603 at w = 5.0` (W1), `1.3258 at w = 499.29`
   (W7) — the per-band maxima sit at the right edges, matching the limit
   functions (script [3] block [3] table: `max J_lim` at `wmax(W)` on every
   band, margins 32.7%/50.1%/65.3%/71.5%/77.0%/78.0%/78.3%).

**Roadmap for the SL1' prover of (E3)** (from the limit analysis, script
[3], and block [F]):

- On W1–W6b at `m >= 561`, `lam <= 40/561 = 0.0713`: the limit functions
  `r31 -> G_3/G_2`, `r42 -> G_4/G_2` (`G_2(w) = w - pi^2/3 + tail`,
  `G_3(w) = 2w - pi^2 + tail`, displayed in script [3]) reduce (E3) to 1D
  interval certificates on `w in (4, 40]` PLUS one `O(lam)`
  discretization-control lemma (same EM machinery as Lemma E.4's remaining
  step, applied to `h_2, h_3, h_4` jointly). The measured limit-vs-561 gap
  is `<= 5e-4` in `J` (item 2), i.e. two orders below the thinnest margin.
- On W7 the cheap route is a quantitative `kappa_4` lower bound: `J <=
  r31^2 <= R31*^2 = 4.84` misses `J0 = 4.5960` by only 5.3%, so ANY
  certified `r42 >= 2 (R31*^2 - J0(W7)) = 0.488` closes W7 (truth `r42 >=
  5.46` on the band — 11x margin; this is the sign lemma's quantitative
  descendant, and Lemma E.4(iii)–(iv) already gives its limit form).
- **Dead route, do not attempt (block [F], verbatim):** replacing (E3) by a
  tightened `r31` cap `r31 <= sqrt(J0(W))` + sign lemma is impossible on
  W1 and W2 — `r31(right edge) = 0.8864 vs sqrt(J0) = 0.8264 -> DEAD` and
  `1.0739 vs 1.0501 -> DEAD` — and razor-thin on W7 (`2.1240 vs 2.1438`,
  0.9%). The JOINT form is required on W1/W2; on W3–W6b either form works
  (margins 2.6%–6.5% for the tightened-cap form).

## 7. Consumer impact (Theorem SL4' and the campaign ledger)

1. **No ledger number moves.** Theorem E delivers the pricing inequality
   with the SAME constants Theorem SL4' consumes (`main` row, all bands);
   the worst rows W5 `0.9891` / W7 `0.9808` and the sliver quantification
   are untouched.
2. **Hypothesis surface of Theorem SL4', restated:** SL1'-w, SL3'-w,
   SL4'-X, and now **(E3) = SL4'-E-J** in place of "SL4'-E + kappa_4-sign".
   Given (E3) (+ SL1'-w(i), already listed), SL4'-E is a THEOREM (Theorem E
   here) — the pricing drops out of the conjectural surface and the joint
   cancellation bound enters it. Net effect: the conjectural surface
   SHRANK (the pricing implication is discharged) and is now stated in
   cumulant-only terms (every remaining hypothesis is a statement about
   `(s2, kappa_3, kappa_4)` or `|phi|` — none mentions `qhat` or `eta`).
3. **For the composition note (STATUS_wave4 §3 item 6):** cite this file
   for the SL4'-E slot as: pricing machinery PROVED at `m >= 561`
   conditional on (E1)(E2)(E3); (E3) CONJECTURED, worst measured margin
   32.6% at `m = 561`, `w = 5.0`.
4. **For the SL1' deliverable:** add (E3) to its target list (it is the
   ONLY remaining eta-side obligation); budget off the W1-right-edge truth
   `J = 0.4603` vs `J0 = 0.6829`; the W7 slot needs only `r42 >= 0.488`.
   The full sign lemma may be dropped from the critical path (Prop E.3 +
   Lemma E.2 Step 0), though Lemma E.4 hands its limit-level form over
   ready-made.
5. **Threshold bookkeeping:** all constants here are certified uniformly
   for `m >= 561` in one shot (Lemma E.2's single worst-case evaluation);
   nothing needs re-running if the harness threshold moves UP; moving DOWN
   below 561 would require recomputing `A0(W)` and `Lam(W)` only.

## 8. Script table (all SAVED and RUN 2026-08-12; outputs archived beside them)

| # | script (`g2_scripts/campaign_20260811/wave5_sl4pe/`) | validates | key verbatim output |
|---|---|---|---|
| [1] | `e1_pricing_certificate.py` (`out_e1_pricing_certificate.txt`) | Lemma E.1(ii) exact identity; Lemma E.2 constants (exact `Fraction`); upper-side slack; positivity | `identity holds at all exact rational tuples: True`; per band `upper-side REM* <= 0.3 R31*^2: True  positivity(D,qhat): True`; `REM*(W): 0.017058 / 0.029319 / 0.05938 / 0.080548 / 0.13207 / 0.14494 / 0.15603`; `J0(W): 0.682942 / 1.10268 / 1.91562 / 2.53645 / 3.66793 / 4.17806 / 4.59597` (+ exact fractions) |
| [2] | `e2_truth_margins.py` (`out_e2_truth_margins.txt`) | guard vs archived nc2 (block [A]); 27 probes `m = 561` (block [B]); `m = 1000/5000` (block [C]); Prop E.3 point (block [D]); J-max scans (block [E]); (P3') diagnostic (block [F]) | `w=4.9: \|eta\|/u=0.4503 (archived 0.4503) ratio=0.6432 (archived 0.6432)`; `ALL checks PASS at m = 561: True; worst pricing ratio = 0.6576`; `eta/u = -1.000964 price = 0.700032 \|eta\|/(price*u) = 1.4299`; `W1 grid ... max J = 0.4603 at w = 5.0 ... margin 32.6%`; `W1: r31(right edge) = 0.8864 vs sqrt(J0) = 0.8264 -> DEAD` |
| [3] | `e3_limit_sign.py` (`out_e3_limit_sign.txt`) | Lemma E.4 (iii)/(iv); `w*` bracket; limit roadmap table | `G_4(4) = 0.23234829889 (> 0.23: True); series tail (k > 300) < 6.08e-516`; `h_4(4) = 5.420211696 (< 6: True)`; `min over grid = 0.579788`; `w* = 3.367175`; per-band `max J_lim` at right edges, margins `32.7% ... 78.3%` |

Numeric-integrity notes for the referees. Script [1] is EXACT (`Fraction`;
the single transcendental bounded by `e^x <= 1/(1-x)`) — attack the
interval logic, not the arithmetic. Scripts [2]/[3] are mpmath dps-30
point-evaluation class (the house-approved `sl3_nc1` class); [2] carries
6-significant-figure float copies of [1]'s `REM*`/`J0` (rounded in the safe
direction; [1]'s fractions authoritative). Thinnest things to attack: the
`REM* <= 0.3 R31*^2` check at W1 (`0.0171` vs `0.3` — not thin); the (E3)
truth margin at `(m, w) = (561, 5.0)` (32.6% — the real number the campaign
now rests on); Prop E.3's point evaluation (dps 30, closed forms only).

## 9. Status recap (prover's own; two referees owed, house rule)

- **Theorem E (the SL4'-E pricing machinery at `m >= 561`): PROVED**, with
  explicit named constants (`REM*(W)`, `J0(W)`, exact rationals archived),
  conditional on nothing beyond its stated interface (E-A2)+(E1)+(E2)+(E3).
  (E-A2) is a PROVED input; (E1)+(E2) are SL1'-w(i) verbatim; (E3) is NEW.
- **Proposition E.3 (interface delta): PROVED** — the recorded proof plan
  (algebra + sign lemma + SL1'-w(i)) cannot yield SL4'-E; the failure is by
  43% at a hypothesis-consistent point, and the truth at the W1/W2 right
  edges independently forces the joint form.
- **Lemma E.4 (limit-level `kappa_4 > 0`, `w >= 4`): PROVED** modulo two
  flagged dps-30 point evaluations with explicit tail bounds (campaign's
  accepted certificate class); term-wise monotonicity part exact.
- **(E3) [SL4'-E-J]: CONJECTURED** — the entire remaining eta-side
  obligation, now stated in cumulant-only terms with measured worst margin
  32.6% at `(m, w) = (561, 5.0)` and a written reduction roadmap (limit
  certificates + one EM lemma; W7 needs only `r42 >= 0.488`).
- **Hypothesis SL4'-E overall: PARTIAL** — machinery proved, statement
  delivered verbatim to the consumer, conjectural content isolated into
  (E3) and handed to SL1' with the dead routes marked.
- CONJECTURED items consumed: none silently. `gamma = 1/8` untouched. No
  existing file modified.

*End of wave5_sl4pe_20260812.md.*
