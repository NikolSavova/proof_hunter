# (S2) attempt — gpt-5.6-sol, reasoning=high, 2026-08-12 15:18

> ⚠️ SINGLE-MODEL, UNREFEREED (house rule: does not count until an adversarial
> maths referee + numerics referee both pass it). Generated by run_sol.py.

I cannot honestly prove (S2) from the statement supplied: the seven bands \(W_1,\ldots,W_7\) are not defined by their \(w=m\lambda\) endpoints. Those endpoints are load-bearing because the required constants range from \(0.05\) to \(0.80\). In fact, the constant \(0.05\) is false without a bounded-\(w\) restriction.

What can be proved completely is an exact formula for \(R_5\), together with a rigorous pointwise majorant and an exact criterion whose bandwise verification would prove (S2).

Throughout, I use the standard tilted Mahonian law implicit in the campaign.

## SOL.1 — The tilted Mahonian partition function

Let
\[
X_m=\sum_{j=1}^m U_j,
\]
where the \(U_j\) are independent and
\[
\Pr_\lambda(U_j=a)
 =\frac{e^{-\lambda a}}{\sum_{b=0}^{j-1}e^{-\lambda b}},
 \qquad 0\le a\le j-1,\quad \lambda>0.
\]
Put
\[
Z_m(z)=\prod_{j=1}^m\frac{1-e^{-jz}}{1-e^{-z}},
\qquad
L_m(z)=\log Z_m(z),
\]
where the logarithm is the analytic branch on \(\Re z>0\) agreeing with the real logarithm on \(z>0\).

Let
\[
\mu=-L_m'(\lambda),\qquad
s_2=L_m''(\lambda),
\]
and let \(\kappa_3,\kappa_4\) be the third and fourth cumulants of \(X_m\) under \(\Pr_\lambda\). Then
\[
\kappa_3=-L_m'''(\lambda),\qquad
\kappa_4=L_m^{(4)}(\lambda).
\]

The centered characteristic function is
\[
\phi(t)
 =\mathbb E_\lambda e^{it(X_m-\mu)}
 =\exp\!\left(L_m(\lambda-it)-L_m(\lambda)+itL_m'(\lambda)\right).
\]

**Proof.**
The product formula for \(Z_m\) follows from independence and the geometric sums. Also,
\[
\mathbb E_\lambda e^{itX_m}
 =\frac{Z_m(\lambda-it)}{Z_m(\lambda)}.
\]
The cumulant identities follow by differentiating the exponential-family partition function. ∎

---

## SOL.2 — Explicit derivative formulas

For \(p\ge0\) and \(x>0\), define
\[
A_p(x):=\sum_{r=1}^{\infty}r^p e^{-rx}.
\]
In particular,
\[
A_1(x)=\frac{e^{-x}}{(1-e^{-x})^2},
\]
and
\[
A_4(x)
 =\frac{e^{-x}\bigl(1+11e^{-x}+11e^{-2x}+e^{-3x}\bigr)}
        {(1-e^{-x})^5}.
\]

For every integer \(n\ge1\),
\[
L_m^{(n)}(\lambda)
 =(-1)^n\left(
      mA_{n-1}(\lambda)
      -\sum_{j=1}^m j^n A_{n-1}(j\lambda)
   \right).
\]

Consequently,
\[
\boxed{
s_2
 =mA_1(\lambda)
  -\sum_{j=1}^m j^2A_1(j\lambda)
}
\tag{SOL.2.1}
\]
and
\[
\boxed{
L_m^{(5)}(\lambda)
 =\sum_{j=1}^m j^5A_4(j\lambda)-mA_4(\lambda).
}
\tag{SOL.2.2}
\]

**Proof.**
On every half-plane \(\Re z\ge\delta>0\),
\[
\log(1-e^{-az})
 =-\sum_{r=1}^{\infty}\frac{e^{-arz}}r
\]
converges locally uniformly and may be differentiated termwise. Since
\[
L_m(z)
 =-\sum_{j=1}^m\sum_{r=1}^{\infty}\frac{e^{-rjz}}r
   +m\sum_{r=1}^{\infty}\frac{e^{-rz}}r,
\]
the displayed derivative formula follows. The rational expressions for
\(A_1\) and \(A_4\) are the standard differentiated geometric-series
identities. ∎

---

## SOL.3 — Exact formula for the fifth-order remainder

Define
\[
E_4(u):=e^u-\sum_{n=0}^4\frac{u^n}{n!}.
\]

Then
\[
\log\phi(t)
 =-\frac{s_2t^2}{2}
  -\frac{i\kappa_3t^3}{6}
  +\frac{\kappa_4t^4}{24}
  +R_5(t),
\]
where the remainder is exactly
\[
\boxed{
R_5(t)
 =
 \sum_{r=1}^{\infty}\frac1r
 \left[
  m e^{-r\lambda}E_4(irt)
  -\sum_{j=1}^m e^{-rj\lambda}E_4(irjt)
 \right].
}
\tag{SOL.3.1}
\]

The series is absolutely convergent for every real \(t\).

Equivalently,
\[
\boxed{
R_5(t)
 =\frac{(-it)^5}{4!}
   \int_0^1(1-u)^4
       L_m^{(5)}(\lambda-iut)\,du.
}
\tag{SOL.3.2}
\]

**Proof.**
Insert the absolutely convergent series for \(L_m(\lambda-it)\). Subtracting the Taylor terms through degree four replaces each exponential \(e^{iy}\) by \(E_4(iy)\), which gives (SOL.3.1).

Absolute convergence follows from
\[
|E_4(iy)|
 \le 1+\sum_{n=0}^4\frac{|y|^n}{n!}
 \le C(1+|y|^4)
\]
and the exponential factors \(e^{-r\lambda}\) and \(e^{-rj\lambda}\).

Formula (SOL.3.2) is Taylor’s theorem with integral remainder, applied to the analytic function \(L_m\) along the segment from \(\lambda\) to \(\lambda-it\). ∎

---

## SOL.4 — A completely explicit rigorous pointwise bound

Define the explicit quantity
\[
\mathcal B_5(m,\lambda)
 :=\frac1{120}\left(
       mA_4(\lambda)
       +\sum_{j=1}^m j^5A_4(j\lambda)
     \right).
\]

Then, for every \(m\ge1,\lambda>0\), and real \(t\),
\[
\boxed{
|R_5(t)|\le \mathcal B_5(m,\lambda)|t|^5.
}
\tag{SOL.4.1}
\]

Equivalently,
\[
\boxed{
|R_5(t)|
 \le C_{\rm abs}(m,\lambda)
      \frac{s_2|t|^5}{\lambda^3},
}
\tag{SOL.4.2}
\]
where every quantity is explicit and
\[
\boxed{
C_{\rm abs}(m,\lambda)
 :=
 \frac{\lambda^3}{
 120\left(
 mA_1(\lambda)-\sum_{j=1}^m j^2A_1(j\lambda)
 \right)}
 \left(
 mA_4(\lambda)+\sum_{j=1}^m j^5A_4(j\lambda)
 \right).
}
\tag{SOL.4.3}
\]

**Proof.**
For \(z=\lambda-iu t\),
\[
A_4(jz)=\sum_{r\ge1}r^4e^{-rj\lambda}e^{irjut},
\]
hence
\[
|A_4(jz)|\le A_4(j\lambda).
\]
By SOL.2,
\[
|L_m^{(5)}(\lambda-iut)|
 \le mA_4(\lambda)+\sum_{j=1}^m j^5A_4(j\lambda).
\]
Use SOL.3.2 and
\[
\frac1{4!}\int_0^1(1-u)^4\,du
 =\frac1{24}\cdot\frac15
 =\frac1{120}.
\]
Finally substitute the exact variance formula (SOL.2.1). ∎

This is a rigorous replacement, but it is deliberately cancellation-free and is too crude to establish the small constants \(0.05,\ldots,0.25\). Proving those constants requires retaining the cancellation in (SOL.3.1) or (SOL.3.2).

---

## SOL.5 — The exact criterion equivalent to (S2)

For \(t>0\), define
\[
\mathcal Q(m,\lambda,t)
 :=\frac{\lambda^3|R_5(t)|}{s_2t^5},
\tag{SOL.5.1}
\]
with \(R_5\) given exactly by SOL.3.1 and \(s_2\) by SOL.2.1. At \(t=0\), define
\[
\mathcal Q(m,\lambda,0)
 :=\frac{\lambda^3|L_m^{(5)}(\lambda)|}{120s_2}.
\tag{SOL.5.2}
\]
This is the continuous extension at \(t=0\).

For any precisely specified band \(W\), (S2) with constant \(C_5^*(W)\) is exactly the assertion
\[
\boxed{
\sup_{\substack{
m\ge561,\;4/m<\lambda\le0.89\\
m\lambda\in W,\;0\le t\le\lambda/2
}}
\mathcal Q(m,\lambda,t)
\le C_5^*(W).
}
\tag{SOL.5.3}
\]

**Proof.**
For \(t>0\), this is simply the desired inequality divided by the positive quantity \(s_2t^5/\lambda^3\). Taylor’s theorem gives
\[
R_5(t)=\frac{(-it)^5}{120}L_m^{(5)}(\lambda)+O(t^6),
\]
proving the continuous extension at \(t=0\). ∎

Thus the missing band endpoints are not cosmetic: they determine the domain of the supremum in (SOL.5.3).

---

## SOL.6 — Why the constant \(0.05\) cannot be interpreted uniformly

Let
\[
m_N=N^2,\qquad \lambda_N=\frac1N,\qquad
t_N=x\lambda_N
\]
for fixed \(x\in(0,\tfrac12]\). Then \(m_N\lambda_N=N\to\infty\), and
\[
\lim_{N\to\infty}
\mathcal Q(m_N,\lambda_N,t_N)
 =
Q_\infty(x),
\]
where
\[
\boxed{
Q_\infty(x)
 =\frac{\left|
 -\log(1-ix)
 -\displaystyle\sum_{n=1}^4\frac{(ix)^n}{n}
 \right|}{x^5}.
}
\tag{SOL.6.1}
\]

At \(x=\tfrac12\),
\[
Q_\infty\!\left(\frac12\right)
 =
32\sqrt{
 \left(
 -\frac12\log\frac54+\frac18-\frac1{64}
 \right)^2
 +
 \left(
 \arctan\frac12-\frac12+\frac1{24}
 \right)^2
 }.
\]
Numerically,
\[
\boxed{
Q_\infty\!\left(\frac12\right)
 =0.18401349\ldots>0.05.
}
\tag{SOL.6.2}
\]

Therefore a \(C_5=0.05\) statement is false if its band is allowed to contain arbitrarily large \(w=m\lambda\).

**Proof.**
The dominant contribution to \(L_{m_N}\) is
\[
-m_N\log(1-e^{-z});
\]
the sum of numerator terms
\[
\sum_{j=1}^{m_N}\log(1-e^{-jz})
\]
is \(O(N)\) along \(z=(1-iu)/N\), whereas the denominator contribution is \(O(N^2)\). The same comparison after the relevant differentiations shows
\[
\frac{\lambda_N^2s_2}{m_N}\longrightarrow1.
\]

Moreover, after centering and putting \(t=x\lambda_N\), the logarithmic characteristic exponent per summand converges to
\[
-\log(1-ix)-ix.
\]
Removing its terms of degrees two through four leaves
\[
-\log(1-ix)-\sum_{n=1}^4\frac{(ix)^n}{n}.
\]
Since
\[
\frac{s_2t^5}{\lambda^3}
 =m_Nx^5\left(\frac{\lambda_N^2s_2}{m_N}\right),
\]
the asserted limit follows.

For \(x=\frac12\),
\[
\Re R_\infty
 =-\frac12\log\frac54+\frac18-\frac1{64},
\]
and
\[
\Im R_\infty
 =\arctan\frac12-\frac12+\frac1{24}.
\]
Substitution gives (SOL.6.2). ∎

This does not refute the intended campaign statement if, as seems likely, \(W_1\) is a bounded interval near \(4\). It proves, however, that the omitted band definition is mathematically indispensable.

# VERIFICATION RECIPE

A referee can script the following using exact rational arithmetic where possible and directed interval arithmetic for transcendental evaluations.

### 1. Basic scalar functions

For \(x>0\), set \(q=e^{-x}\) and evaluate
\[
A_1(x)=\frac{q}{(1-q)^2},
\]
\[
A_4(x)=\frac{q(1+11q+11q^2+q^3)}{(1-q)^5}.
\]

Check these against direct sums:
\[
A_1(x)=\sum_{r\ge1}r e^{-rx},
\qquad
A_4(x)=\sum_{r\ge1}r^4e^{-rx}.
\]

Expected relative agreement at 80-digit precision: below \(10^{-70}\) after truncating the sums with a certified geometric tail.

### 2. Variance

Compute
\[
s_2=mA_1(\lambda)-\sum_{j=1}^m j^2A_1(j\lambda).
\]

Expected result:
\[
s_2>0.
\]

Independently compute the variances of the truncated geometric variables and check that their sum agrees with the displayed formula.

### 3. Exact remainder, direct-log form

Compute
\[
L_m(z)
 =\sum_{j=1}^m
   \left[\log(1-e^{-jz})-\log(1-e^{-z})\right]
\]
using the analytic continuation from \(\Re z>0\), preferably with `log1p(-exp(-j*z))`.

Then compute
\[
\log\phi(t)
 =L_m(\lambda-it)-L_m(\lambda)+itL_m'(\lambda),
\]
and
\[
R_5^{\rm direct}(t)
 =\log\phi(t)
  +\frac{s_2t^2}{2}
  +\frac{i\kappa_3t^3}{6}
  -\frac{\kappa_4t^4}{24}.
\]

### 4. Exact remainder, series form

Set
\[
E_4(u)=e^u-\left(1+u+\frac{u^2}{2}
                    +\frac{u^3}{6}+\frac{u^4}{24}\right)
\]
and compute
\[
R_5^{\rm series}(t)
 =
 \sum_{r\ge1}\frac1r
 \left[
 me^{-r\lambda}E_4(irt)
 -\sum_{j=1}^m e^{-rj\lambda}E_4(irjt)
 \right].
\]

Bound the tail after \(r=R\) using
\[
|E_4(iy)|\le 1+\sum_{n=0}^4\frac{|y|^n}{n!}
\]
and the exact geometric-tail formulas for
\[
\sum_{r>R}r^k e^{-ar},\qquad 0\le k\le3.
\]

Expected check:
\[
R_5^{\rm direct}(t)=R_5^{\rm series}(t)
\]
within the certified interval error.

### 5. Integral-majorant check

Compute
\[
B_5
 =\frac{t^5}{120}
   \left[mA_4(\lambda)+\sum_{j=1}^m j^5A_4(j\lambda)\right].
\]

Expected check:
\[
|R_5^{\rm direct}(t)|\le B_5.
\]

### 6. Quantity actually required by (S2)

Compute
\[
\mathcal Q(m,\lambda,t)
 =\frac{\lambda^3|R_5^{\rm direct}(t)|}{s_2t^5}.
\]

Near \(t=0\), replace this unstable quotient by
\[
\mathcal Q(m,\lambda,0)
 =\frac{\lambda^3}{120s_2}
  \left|
   \sum_{j=1}^m j^5A_4(j\lambda)-mA_4(\lambda)
  \right|.
\]

Once the exact bands are supplied, the required outputs are:
\[
\sup_{W_1}\mathcal Q\le0.05,\quad
\sup_{W_2}\mathcal Q\le0.06,\quad
\sup_{W_3}\mathcal Q\le0.08,
\]
\[
\sup_{W_4}\mathcal Q\le0.10,\quad
\sup_{W_5}\mathcal Q\le0.15,\quad
\sup_{W_{6b}}\mathcal Q\le0.25,\quad
\sup_{W_7}\mathcal Q\le0.80.
\]
These are obligations, not verified outputs here.

### 7. Deep-tilt limiting check

Evaluate
\[
a=-\frac12\log\frac54+\frac18-\frac1{64}
  =-0.002196775657104877\ldots,
\]
\[
b=\arctan\frac12-\frac12+\frac1{24}
  =0.00531427566747278\ldots.
\]
Then
\[
32\sqrt{a^2+b^2}
 =0.18401349\ldots.
\]

For
\[
(m,\lambda,t)=(N^2,N^{-1},(2N)^{-1}),
\]
the computed \(\mathcal Q\) should converge to \(0.18401349\ldots\).

# WHAT REMAINS

1. **(S2) is not proved here.** The exact \(w=m\lambda\) endpoints and endpoint conventions of \(W_1,\ldots,W_7\) are absent from the supplied statement, so the seven required suprema are not defined.
2. Even after supplying those endpoints, one still needs a rigorous continuum certification of (SOL.5.3), retaining the cancellations in SOL.3. The cancellation-free bound SOL.4 is generally too weak for \(0.05,\ldots,0.25\).
3. No numerical “measured truth” value from the ledger has been promoted to a proof.
4. The strongest fully proved result above is the exact remainder identity SOL.3 and its explicit pointwise majorant SOL.4. Thus (S2), and hence CL through this route, remains open.