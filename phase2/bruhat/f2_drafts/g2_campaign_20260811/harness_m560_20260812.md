# Harness extension to m = 560 — checkpointed resume of the dead m=540 run (2026-08-12, wave 4)

**Script (SAVED + RUN):**
`/Users/sihaohuang/Desktop/Coding/proof_hunter/phase2/bruhat/f2_drafts/g2_scripts/campaign_20260811/harness_m560/run_m560.py`
**Results file (checkpointed, one line per m, flushed immediately):**
`.../g2_scripts/campaign_20260811/harness_m560/results_m560.txt`

STATUS: RUN IN PROGRESS at note-writing time — the checkpointed run was
launched in the background (started 2026-08-12); at the last check it had
certified through `m = 490`, all PASS (verbatim last row of results_m560.txt:
` 490 119805  59902  59902   3.0439e-07 0.9977974495  1.07925   PASS`).
Rows 4..481 are honored from `wave2_repairs/results_m540.txt` (all PASS);
rows 482+ are appended one-per-m with immediate flush, so NOTHING is lost if
the process dies: relaunching `python3 run_m560.py` resumes exactly after
the last written row. §2 is to be filled with the verbatim tail (including
the `# OVERALL` line and checkpoint varfit values) once the run reaches 560.

## 1. What this run is

STATUS_wave3.md rec 1: the wave-2 repairs run
`wave2_repairs/run_m540.py` DIED at `m = 481` (all rows PASS through 481, no
`# OVERALL` line). This run is a byte-faithful method copy (same running-sum
convolution for the exact integer Mahonian coefficients, same six exact
certificates C1–C6, exact Fractions in every verdict, floats display-only)
with three declared changes:

1. **MMAX = 560** — closes G4's part-(c) band `[482, 536]` (crude `C_A`
   crossover `m* = 535/537`) AND finitely closes the SL-sliver's harness
   option (composite §5.3: W1 far sliver sized to ~450, ~560 under the
   orphan's cruder floor).
2. **Checkpointing** — each completed `m` appends one PASS/FAIL row to
   `results_m560.txt` immediately (flush); on restart, PASS rows already in
   `wave2_repairs/results_m540.txt` (m = 4..481) or in its own partial file
   are detected and skipped in the verdict path. The exact polynomial is
   still rebuilt through skipped `m` (the recurrence needs it), and the C6
   strict-increase chain is kept EXACT across the resume boundary: `prev
   varfit` is recomputed as an exact Fraction from the rebuilt polynomial's
   central ratio (valid by the skipped row's own C3 PASS) — never parsed
   from printed floats.
3. **Exact-symmetry 2x scan** — the argmin scan runs over `1 <= k <= N/2`
   only, justified by the exact C1 check `a == a[::-1]` performed first each
   `m` (`r(k) = r(N-k)` exactly; smallest-k tie-break preserved); full scan
   fallback on C1 failure (never triggered).

Certificates per m (identical statements to run_m540.py / run_m200.py):
C1 symmetry+positivity; C2 argmin = floor(N/2) (m >= 5); C3 min ratio ==
central ratio exactly; C4 exact odd-N central tie; C5 varfit = sigma^2(r-1)
>= 187/216 with equality iff m = 6; C6 varfit strictly increasing (m >= 6).
Named constants: `sigma^2 = m(m-1)(2m+5)/72`, `C_SHARP = 187/216`,
`C_LIMIT_FIT = 27/25`.

Resume logic sanity-tested before the real run (fresh 4..40 then resumed
41..60 against the same file: `new rows: 20; prior rows honored: 37;
failures: 0; OVERALL: PASS`, C6 chained exactly across the boundary).

## 2. Verbatim run output (tail of results_m560.txt)

[TO BE FILLED ON COMPLETION]

## 3. Consequences

[TO BE FILLED ON COMPLETION]
