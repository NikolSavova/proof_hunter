# STATUS_wave6 — G2 closure campaign, wave-6/6b synthesis (2026-08-12)

*Synthesis editor pass, wave 6 final (read-everything role; blind protocol
lifted for this file only). Sources: everything new under
`g2_campaign_20260811/` since STATUS_wave5 — the constants-scout plan
`wave6_s1_plan_20260812.md` (+ its archived
`wave6_scout/out_scout_s1_targets.txt`), the four cross-model drafts
`sol_s1..s4_20260812.md` (OpenAI gpt-5.6-sol, reasoning=high, generated by
`wave6_sol/run_sol.py`), the SIX wave-6b referee reports on them
(`referee_maths_sol_s1/s2/s4.md`, `referee_numerics_sol_s1/s3/s4.md` + their
script directories), and the reassembled composition
`CL_composition_v2_20260812.md` (+ its
`wave6b_composition/out_compose_chain_v2.txt`) — with `STATUS_wave5.md` as
the governing prior ledger. `g2_draft_t1_20260803.md` remains unread by every
wave-6/6b agent and by this editor. No existing file modified; this file is
new (no-erasing rule). House rules applied: an item counts ONLY when BOTH its
referees hold SURVIVES or MINOR_REPAIRS; a cross-model draft gets NO extra
credit for being cross-model. Facts from this editor's own disk checks
(2026-08-12, ~17:1x): (i) exactly SIX sol referee reports exist — the
commissioned s2-NUMERICS and s3-MATHS lanes produced NO artifact (this does
not change any verdict: s2 and s4 already carry a failing report each, and
s3's one report is itself blocking — but the debts are recorded in §2a);
(ii) NO file matching `*flip*` exists — the flip was again NOT executed
(correctly; preconditions unmet); (iii) `referee_composition.md` and
`referee_hygiene.md` still do NOT exist — the wave-5 process debts on the
composition (now v2) and the hygiene overlay remain open; (iv)
`wave6b_composition/out_compose_chain_v2.txt` ends `ALL CHECKS PASS: True`
with `C*(m >= 561) = 19.5659 <= 20: True` and the fallback
`19.5803 <= 20: True`. One new editor script (SAVED and RUN, output archived
beside it, quoted in §4): `g2_scripts/campaign_20260811/wave6_status/`
`status6_afortiori_check.py` (`out_status6_afortiori_check.txt`) — pairwise
comparison of the refereed (S1) ceilings against BOTH target generations; no
other new mathematics is asserted here.*

**Executive summary.** Wave 6 re-architected the (S1) targets to spend chain
slack on provability (worst-band margin 2.94% -> 27.21%, chain constant
18.2281 -> 19.5659 <= 20); wave 6b then attacked all four open statements
with a second model and refereed the results at the standard bar. Outcome:
**(S1) is DISCHARGED** — Theorem SOL.9 (`sol_s1_20260812.md`) proves the
re-architected banded cumulant scales for all `m >= 561`, `0 < |lam| <=
0.89`, maths MINOR_REPAIRS + numerics MINOR_REPAIRS, with the numerics
referee's rigorous outward-rounded interval computation
(`wave6b_ref_s1/ref3_band_certificate_iv.py` + `ref2_mn_bounds.py`) adopted
as the certificate of record (the prover shipped its central certificate
unexecuted — the referee lane executed it). The editor's §4 check shows the
same refereed ceilings ALSO clear the wave-5 (paper Table-4) targets on all
14 entries (worst margin 2.86%), so BOTH generations of (S1) are discharged
by the same artifact. The other three attacks FAILED the bar: **(S2) FATAL**
(nothing proved — "not for error — for absence"; plus a false premise about
the band definitions that must not be cited), **(S3) MAJOR_ISSUES** (sound
architecture, every testable constant true, but the central 18.9M-box
certificate was never executed, and a factor-1.992 Euler–Maclaurin constant
flaw needs fixing), **(S4) MAJOR_ISSUES twice** (credible non-circular
`m >= 700` core; the `[561, 699]` coverage is CIRCULAR as written and a
load-bearing remainder lemma is proved by assertion). The composition was
reassembled as **Theorem CL-C v2** (`CL_composition_v2_20260812.md`):
CL(79, 20, 0.89) at `m >= 561` **PROVED MODULO exactly (S2'), (S3'), (S4)**
— the conditional surface shrank 4 -> 3 — chain-verified end-to-end at
`19.5659 <= 20` (fallback `19.5803 <= 20`). **CL itself remains OPEN;
Theorem A remains PROVED CONDITIONAL, now on three named statements.** No
flip. No grade inflation: the bottom line is §3.

## 1. Wave-6/6b verdict table (cross-model provenance explicit)

| Target | Prover artifact (provenance) | Maths referee | Numerics referee | Counts under house rules? |
|---|---|---|---|---|
| **(S1) re-architecture (design)** | `wave6_s1_plan_20260812.md` (this campaign's scout; DESIGN, not proof) | — (design note; its closure arithmetic re-verified independently by BOTH s1 referees and by `compose_chain_v2.py`) | — | design adopted by CL-C v2; the moved-piece re-certification is folded into the v2 unit-referee debt (§2a) |
| **(S1) at the NEW targets** | `sol_s1_20260812.md` (OpenAI gpt-5.6-sol, single-model as drafted) | **MINOR_REPAIRS** (`referee_maths_sol_s1.md`: every lemma SOL.1–SOL.9 re-derived by hand, ALL CORRECT; blocking R1 = certificate unexecuted; R2–R5 one-liners) | **MINOR_REPAIRS** (`referee_numerics_sol_s1.md`: rigorous outward-rounded interval re-certification of every band cell, zero bisections — ADOPTED as certificate of record, discharging R1; ~170-pt truth sweep + 104-probe envelope attack, 0 violations; F1 provenance, F2 statement hygiene) | **YES — (S1) DISCHARGED, two-referee.** First cross-model theorem of the campaign. Repairs R2–R5/F2 recorded, none constant-moving |
| **(S2)** | `sol_s2_20260812.md` (gpt-5.6-sol) | **FATAL** (`referee_maths_sol_s2.md`: proves ZERO of the seven band bounds — the draft itself says (S2) remains open; its only bound is 20–23x short on W1; its "bands undefined" premise is FALSE at corpus level (F2) and its obligations table is stale vs the scout's `C5*(W7) = 0.50` (F3); salvage inventory F6 recorded — incl. the SOL.6 hard floor `0.184013` for any unbounded band and the F4 lead that SOL.4 + a sup argument plausibly closes W7) | — (lane produced no artifact; moot — one FATAL suffices to fail the bar) | **NO — (S2) remains OPEN**, zero usable proof content toward the seven suprema |
| **(S3)** | `sol_s3_20260812.md` (gpt-5.6-sol) | — (lane produced no artifact; still owed if the draft is revived) | **MAJOR_ISSUES** (`referee_numerics_sol_s3.md`: every testable claim TRUE — band sups clear truth by 7.9–46%, W7 legs verified incl. exact `lam -> 0+` closed forms, exact floors valid, no Prop E.3 smuggling, 28x scan-gap headroom — but the central 18,874,368-box certificate and SOL.16/17 evaluations were NEVER EXECUTED (F1); EM remainder constant understated by factor 1.992 (F2, numerically absorbed); threshold-generation paragraph owed (F3)) | **NO — (S3) remains OPEN**; closest to closure (run the corrected certificate, apply F2/F3, then BOTH referee passes) |
| **(S4)** | `sol_s4_20260812.md` (gpt-5.6-sol) | **MAJOR_ISSUES** (`referee_maths_sol_s4.md`: `m >= 700` C^2-local-CLT chain sound and non-circular, delivered constant 0.545; but F1: `[561, 699]` coverage CIRCULAR — (S4) is consumed from 561; F2: (SOL.14)/(SOL.15) proved by assertion, displayed envelope unobtainable by the stated route (abs-series 8.29 > 7.008 at the deep corner); F3 mis-citation of R.1 beyond W1; F4 one false displayed bound `E_mid[0]`; repair sketch closes 561–699 at 0.7241) | **MAJOR_ISSUES** (`referee_numerics_sol_s4.md`, independently concordant: N1 same scope gap, own sizing closes `[561, 699]` at 0.689; N2 (SOL.5) support covers W1 only — W2–W7 chain derivable (worst margin 10.9%) but unwritten; N3 `E_mid[0]` display; 37/38 checks pass; end-to-end truth `s2(r-1) in [0.9979, 0.9990]` at all corners incl. the gap range) | **NO — (S4) remains OPEN**; best-understood residue (both referees quantified in-kind repairs; plausibly one focused revision from citable) |
| **Composition v2** | `CL_composition_v2_20260812.md` (+ `compose_chain_v2.py`, `ALL CHECKS PASS: True`) | **ZERO referees** (new file; supersedes v1 as the operative composition — v1's never-discharged unit-referee debt TRANSFERS to v2) | — | NOT citable until its unit referee lands (its own §6 says so) |
| **Flip** | **NOT EXECUTED — correctly.** Disk check (ii): no flip artifact. Preconditions unmet: (S2')/(S3')/(S4) unproved; block-C re-run at landed spec + assembly §8 human ratification still owed | — | — | — |

House-rule reading: **newly citable at two-referee MINOR_REPAIRS-or-better
after wave 6b:** `sol_s1_20260812.md` = Theorem SOL.9 (cite ONLY together
with both referee reports AND the certificate-of-record provenance:
`wave6b_ref_s1/ref3_band_certificate_iv.py` + `ref2_mn_bounds.py` — the
executed certificate is the REFEREE's artifact, not the prover's).
**NOT citable:** `sol_s2/s3/s4_20260812.md` (failed the bar; s2's F2 claim
must never be cited as fact), `CL_composition_v2_20260812.md` and
`wave6_s1_plan_20260812.md` (zero referees as units),
`wave4_hygiene_20260812.md` (debt unchanged since wave 5).

## 2. The final G2 ledger after wave 6b

### CL(79, 20, 0.89) — the deep-tilt core lemma. **NOT PROVED. STILL OPEN — now PROVED MODULO exactly THREE named statements (was four), with every implication node two-referee.**

- **Scope (unconditional, unchanged):** `CL(79, 20, 0.89) for m >= 561`;
  `m in [401, 560]` consumer-discharged by exact computation (557/557 PASS —
  now a FIFTH independent parse, `compose_chain_v2.py` block [A]).
- **The assembled statement:** Theorem CL-C v2 (`CL_composition_v2_20260812.md`
  §2) = CL at `m >= 561` from (S2') + (S3') + (S4), at the wave-6
  re-architected constants, with composed constant `C*(m >= 561) = 19.5659
  <= 20` (worst row `0.978293`, the W6b ledger row at the certification
  point `m = 561`; designed 2% row reserve) and `C*(m >= 1581) = 15.1678 <=
  136` (8.97x). The (S2)-FALLBACK chain (`C5*(W7) = 0.80` kept, W7 (S1)
  constants read as `2.42/7.28`) also closes: `19.5803 <= 20` — Theorem
  SOL.9's geometric W7 bound covers both readings. Fact R.G is RETIRED; the
  W1 `[561, 699]` rung is the M3 per-cell construction (worst `0.485067` at
  561); Lemma R.2 tail `0.976016` at 700. The composition itself still owes
  its unit referee (§2a item 1).
- **What is now PROVED input (new since wave 5):** **(S1) — Theorem SOL.9**
  (`sol_s1_20260812.md`, two-referee MINOR_REPAIRS): `|kappa_3| <= R31*(W)
  s2/|lam|`, `kappa_4 <= R42*(W) s2/lam^2` for ALL `m >= 561`, `0 < |lam| <=
  0.89` (two-sided superset of the consumed scope), `R31* =
  1.19/1.44/1.82/2.04/2.38/2.56/2.71`, `R42* =
  0.87/1.62/3.11/4.27/6.38/7.33/8.17`. Proof: exact cumulant representation
  -> termwise geometric-envelope trick (`D_3 <= a(lam) D_2`, `D_4 <= b(lam)
  D_2`, no sign assumption on `h_4` differences) -> trapezoid enclosure with
  explicit `M_n` -> finite rational band certificate on `4 <= w <= 40` ->
  global geometric bound `a(0.89) < 2.1304 < 2.71`, `b(0.89) < 6.4114 <
  8.17` on W7 (NO monotonicity-in-m needed) -> reflection. Certified
  per-band ceilings sit 1.49–4.46% inside the claimed table, which sits
  strictly below the targets.
- **The conditional surface, exactly (verbatim in CL-C v2 §4) — all
  CONJECTURED, none with a proof artifact:**
  1. **(S2') [SL1'-w(ii), W7-adjusted]** the R5 core-remainder bound
     `|R5(t)| <= C5*(W) s2 t^5/lam^3` on `[0, lam/2]`, `C5* =
     0.05/0.06/0.08/0.10/0.15/0.25/0.50` (W7's 0.50 is the scout's
     adjustment, adopted on the s2 referee's direct measurements
     `Q = 0.1947/0.2115`, `C_abs`-sup sample `0.2624`, all `>= 1.9x` room,
     plus the SOL.6 hard floor `0.184013 < 0.50`; the 0.80-fallback is
     fully priced). Wave-6b attempt FATAL; salvage inventory recorded
     (exact identities SOL.3.1/3.2, criterion SOL.5.3, SOL.4 as W7-closer
     lead; W1–W6b need genuine cancellation — the crude bound is 20–23x
     short there).
  2. **(S3') [(E3) at the recalibrated thresholds]** `J = r31^2 - r42/2 <=
     J0(W) = 0.834155/1.38585/2.44993/3.25094/4.67285/5.38338/6.01476`
     (exact fractions archived; thresholds GREW bandwise from wave 5 —
     safe direction, verified exactly — so any proof against the OLD row
     discharges (S3') a fortiori). Measured margins 44.8–82.9% (worst W1).
     Wave-6b attempt MAJOR_ISSUES: certifiable-looking plan, certificate
     never run, F2 constant flaw (x1.992) and F3 threshold paragraph owed.
  3. **(S4) [bootstrap seed]** `|s2(r(k) - 1) - 1| <= 0.89`, `m >= 561`,
     unchanged. Consumed via referee-M2's chord/monotone iteration —
     re-verified at the new worst rows: contraction margin `>= 4.55%`,
     basins `x_seed >= 0.92007 > 0.89` (incl. the fallback W7 row).
     Wave-6b attempt MAJOR_ISSUES x2: `m >= 700` core sound (constant
     0.545); `[561, 699]` gap circular as written but repairable-in-kind
     (referee sizings 0.7241 / 0.689148 at 561, both `< 0.89`); the
     (SOL.14) remainder lemma needs an honest proof (envelope
     `6.72 + 5.136 u/(1-u)` still yields `0.0018 < 0.0021`).
- **Truth side (corroborative, grown):** the s1 referees' ~170-point
  truth-under-ceiling sweep + 90-probe (S1) truth attack (0 violations);
  s3 referee's band scans (J inside every claimed sup, 7.9–46%); s4
  referees' end-to-end `s2(r-1) in [0.997920, 0.999001]` at all corners
  INCLUDING `[561, 699]`; plus everything wave 5 had (CL exactly TRUE at
  401/402, 17.1x).

### Prop 3.5(ii) — refined small-tilt law [T.9]. **CLOSED (modulo listed repairs) — unchanged by wave 6.**

### Prop 3.5(i) — crude uniform law [T.8]. **PARTIAL — reduced to CL (reduction two-referee, unchanged); CL's residual scope `m >= 561`, now three-hypothesis.**

R2 remains conditional on exactly CL; part I's finite citation stands at
`m <= 560`. The flip-time re-run of `assembly_checks.py` block C at the
landed spec (band-2 margin `2.83e-4`) is still owed.

### G2 overall

Theorem A (Mahonian limit `sigma^2(r_m(k) - 1) -> 1`, F2(a)): **PROVED
CONDITIONAL on exactly (S2'), (S3'), (S4)** — three statements, down from
four. Finite companions: unconditional theorems for `5 <= m <= 560`
(unchanged). G4: part-(c) band `[401, 536]` computation-closed (unchanged);
constant chase remains. G3: untouched, research.

### §2a. Debt inventory (process; none moves a verdict)

1. **Unit referee on `CL_composition_v2_20260812.md`** (v1's transferred
   debt; must also ratify: the moved-piece re-certifications — SL4'-R rows
   at `m = 561`, Theorem E `REM*`/`J0` replacement — Fact R.G's retirement,
   the m-monotonicity structural argument for the new rows, and the (S2')
   0.50-adoption reasoning). **Single-verifier pass on
   `wave4_hygiene_20260812.md`** (unchanged since wave 5).
2. **Missing referee lanes if drafts are revived:** s3-MATHS (owed before
   (S3) can ever close via sol_s3), s2-NUMERICS (moot unless a new (S2)
   artifact appears — one FATAL already fails the bar).
3. **Recorded-not-applied text repairs** (next hygiene batch): all of
   STATUS_wave5 §2a; PLUS sol_s1 R2–R5/F2 (one-liners: interval citation
   for (26)/(27), the `b' > 0` one-differentiation proof, `C^2[0, w]`
   clause, sign-free SOL.7 chain, restore the `lam in (4/m, 0.89]` range
   clause); PLUS (if revived) sol_s3 F2/F3 and sol_s4 R1–R4/N1–N3.
4. **Flip preconditions** unchanged: (S2')/(S3')/(S4) proofs + referees,
   composition unit referee, block-C re-run at landed spec, assembly §8
   human ratification.
5. **Stalled fresh harness re-run** (record-only, `m = 441`) unchanged.

## 3. THE BOTTOM LINE: is Theorem A = F2(a) fully proved?

**NO — and the residue is not "minor repairs".** Theorem A = F2(a) is
**PROVED CONDITIONAL on exactly three named open statements (S2'), (S3'),
(S4)**, none of which has a proof artifact that passed the two-referee bar.
That is real progress — the surface shrank from four statements to three,
and the discharged one, (S1), carried the campaign's thinnest margins
(3.7%/3.9% at wave 5) — but the conditionality that remains is REAL
MATHEMATICS: (S2') has had NO successful attack of any kind (the wave-6b
attempt proved literally none of its seven bounds, and its W1–W6b constants
provably need cancellation the crude route cannot give); (S3')'s
near-complete plan still lacks its executed certificate AND its maths
referee; (S4) is missing 139 integers of scope plus an honest proof of its
key remainder lemma. A reader who removes any one of the three has no
theorem. No grade inflation is possible.

**What IS fully in place (the complete conditional chain, every node
citable except the two flagged process debts):** for `m in [5, 560]`,
Theorem A's finite part holds UNCONDITIONALLY by exact integer computation
(harness + hygiene + sliver, unchanged). For `m >= 561`,
[(S2') + (S3') + (S4) ==> CL(79, 20, 0.89)] via Theorem CL-C v2
(`CL_composition_v2_20260812.md`, consuming: I1–I7 as in v1 — composite
A2/A3/C.1/[W.6], Theorem SL3', sliver SLV.1–.3, Theorem SL4'-R/R.1/R.2/R.3,
Theorem E/Prop E.3/Lemma E.4, Theorem X.1/X.2/X.3, exact harness — PLUS I8 =
Theorem SOL.9 discharging (S1), two-referee, certificate of record
`wave6b_ref_s1/ref2+ref3`; chain re-verified end-to-end,
`C* = 19.5659 <= 20`, fallback `19.5803 <= 20`; composition's own unit
referee still owed); and [CL ==> Theorem A] via the wave-2/3 assembly
(unchanged, refereed).

**The exact residue, nothing else:**

1. **(S3')** — closest: execute sol_s3's certificate with the F2-corrected
   EM constant, apply F2/F3 in text, then BOTH referee passes (numerics
   judges the resolution budget generous, 28x; maths lane never ran).
2. **(S4)** — revise sol_s4 per R1–R4/N1–N3: add the `[561, 699]` segment
   (both referees' sizings close it at ~0.69–0.73 vs 0.89), prove (SOL.14)
   with an honest envelope, write the W2–W7 crossover chain, fix the
   display; then two referees.
3. **(S2')** — the only statement with no credible artifact: new prover
   package (salvage: exact identities + criterion; W7 plausibly closes via
   the C_abs sup argument at 0.50; W1–W6b need genuine
   cancellation-retaining analysis); then two referees.
4. **Process:** unit-referee the composition v2 (+ hygiene overlay pass),
   apply the recorded text repairs, then the flip (block-C re-run at the
   landed spec + assembly §8 human ratification).

Items 1–3 are the remaining mathematics; item 4 is mechanical/process.

**Standing caveats (inherited, all still flagged):** the 2% composed row
reserve is DESIGNED slack spending — thinner than v1's 9%, do not sharpen
(the wave-5 do-not-sharpen list stands: NX-5's `2.76e-4`, X.a's spent
floor, the [B2] >= 4.55% bootstrap margins, plus s4's knife-edge displays
`E_loc[1]` +2.87e-8 / `E_cross[0]` +1.05e-7); Theorem SL3' and E.5.3/E.6
remain monotone-cell class; the (S1) certificate of record is rigorous
outward-rounded interval arithmetic (stronger than the flagged classes);
the s2 draft's "bands undefined" claim is FALSE and must never be cited;
sol_s3's SOL.18 floors are pinned to the WAVE-5 J0 generation (W1/W2
slacks 0.0029/0.0027 — any REM* regrowth ~0.003 breaks them; the grown
wave-6 row is strictly safer).

## 4. The paper's F2 section: does Theorem 6.5 need a further edit?

**Theorem 6.5 itself (F2 conditional on CL): NO edit needed** — its
statement "assume Conjecture CL, then `sigma^2(r_m - 1) = 1 - (27/25)/m +
O(m^-2)` ..." consumes CL as a unit, and CL is still open; nothing wave 6b
proved or failed touches that implication.

**The surrounding reduction apparatus: YES, an edit is warranted — but it
is BLOCKED on the composition-v2 unit referee, with one exception.** The
current paper text (change_log 3f–3h) presents Conjecture 6.7 = (S1)–(S4)
at the WAVE-5 constants (Table 4: `R_3 = 1.0/…/2.2`, `R_4 = 0.8/…/6.6`,
`C_5(W7) = 0.80`, old `J_0` row), Prop 6.8 with composed constant 18.23,
and the evidence remark's 3.7%/3.9% headline. After wave 6b: (S1) is a
THEOREM, the operative surface is three statements at the NEW constants,
and the operative composed constant is 19.5659. The paper must NOT cite
`CL_composition_v2` or the scout plan until the unit referee lands (house
rule) — so the full renumbering/retabling edit waits.

**The exception (available NOW, minimal-churn):** `sol_s1_20260812.md` +
its two referee reports ARE citable today, and the editor check
`status6_afortiori_check.py` (output archived beside it, verbatim) shows
the refereed certificate discharges the paper's EXISTING Table-4
constants a fortiori:

```
  certified ceilings clear ALL 14 OLD (wave-5/Table-4) targets: True
  certified ceilings clear ALL 14 NEW (wave-6) targets: True
  worst old-target margin: 2.86% (W7 r42)
```

(the certified per-band ceilings `0.900/1.090/1.370/1.550/1.850/1.970` +
W7 `2.1304` and `0.680/1.250/2.400/3.260/4.980/5.650` + W7 `6.4114` sit
strictly below BOTH generations of targets). So the paper may, without
touching Table 4 or Prop 6.8's 18.23, upgrade Conjecture 6.7's (S1) row to
a theorem (cited through `sol_s1_20260812.md` + both referee reports +
certificate-of-record provenance) and edit the evidence remark accordingly
— this editor-level a-fortiori comparison should be ratified in the v2
unit-referee pass before or alongside the paper edit.

**The one-sentence claim the F2 section may make after the edit:**

> "Theorem A holds conditional on the single explicitly-stated core lemma
> CL(79, 20, 0.89), whose scope has been reduced by exact integer
> computation to `m >= 561` and whose proof is assembled, with every input
> verified by two independent adversarial referees, as an implication from
> exactly three explicitly-stated open statements about the tilted
> Mahonian law — a fifth-order remainder bound, a joint cancellation
> bound, and an a-priori ratio seed — the fourth such statement (the
> banded third/fourth-cumulant scales) having been proved outright by a
> cross-model prover and certified by rigorous interval arithmetic; the
> three survivors are supported by extensive exact and adversarial
> computation but none is yet proved."

Prohibitions (all still in force): the paper must NOT say F2(a) is proved;
must NOT call CL "nearly proved" (the surface shrank this wave, but wave 5
proved it can also GROW under scrutiny); must not cite composition v1/v2,
the scout plan, or the hygiene overlay until their referee passes land;
must cite SL4' only through the repaired file; must not cite the s2/s3/s4
sol drafts as progress (s3/s4 may be described, if at all, only as
attempts with named referee-verified gaps).

## 5. Remaining human steps

1. **Ratify (now):** read Theorem CL-C v2 (`CL_composition_v2_20260812.md`
   §2/§4) and the (S2') 0.50-adoption reasoning (§4 there); confirm the
   three-statement surface is acceptable as the paper's stated
   conditionality. Commission the v2 unit referee (folding in the
   moved-piece deltas, R.G retirement, and this file's §4 a-fortiori
   check) and the hygiene-overlay pass.
2. **Paper edit (now, minimal):** upgrade Conjecture 6.7's (S1) row to a
   theorem via the citable sol_s1 package (per §4's exception); hold the
   full retabling (new constants, 19.5659, three-statement surface,
   updated one-sentence claim) until the v2 referee lands.
3. **Commission the remaining mathematics, in this order:** (S3')
   certificate run + text fixes + two referees (closest); (S4) revision
   per R1–R4/N1–N3 + two referees; (S2') new prover + two referees (the
   only fresh mathematics — budget off the salvage inventory and the
   0.184013 floor).
4. **Then the flip:** block-C re-run at the landed spec (band-2 margin
   `2.83e-4`) + assembly §8 human ratification — and Theorem A is
   unconditional.
5. **Decide on the stalled fresh harness re-run** (record-only; relaunch
   or retire). **G4 next** (constant chase per wp2-a2 §10 item 3);
   **G3 after** (untouched, research).

*End of STATUS_wave6.md.*
