# G2 campaign repairs — 2026-08-11 (repair-application session, STATUS.md §5.3)

> Status doc for the repairs demanded by the wave-1 referee reports
> (`referee_maths_wp1_c.md` / `referee_numerics_wp1_c.md` on `wp1_draft_c.md`;
> `referee_maths_wp2_b.md` / `referee_numerics_wp2_b.md` on `wp2_draft_b.md`)
> and by the T2 numerics referee (`referee_t2_numerics.md` on
> `g2_draft_t2_20260803.md`), as consolidated in `STATUS.md` §2a / §2b / §3.
> **Nothing here modifies any existing file** (no-erasing rule): all fixes are
> NEW dated scripts + fixed COPIES under
> `g2_scripts/campaign_20260811/repairs/` + the errata lists below, to be
> folded into the draft texts only at paper-assembly time. Mirrors
> `g1b_repairs_20260802.md`.
>
> Every numeric claim below is from a script SAVED in
> `g2_scripts/campaign_20260811/repairs/` and RUN 2026-08-11 (CPython 3.12.2,
> stdlib + mpmath; exact `Fraction`/integer arithmetic for every rational
> step, mpmath dps 30–60 elsewhere; dual-precision cross-checks per house
> precedent). Verbatim outputs are archived next to the scripts as
> `out_*.txt`.
>
> **Bottom line: every repair in STATUS §2a (A1–A5), §2b (B1–B8), and §3
> (T1–T9 = referee findings F1–F9) is now APPLIED (as recorded errata) and,
> where it had numeric content, VERIFIED by a saved script. The only repair
> with mathematical content (T2-F1, the broken `B_lam/B_m` chain) is CLOSED
> with a new, fully derived chain giving `|B_lam/B_m - 1| <= 0.362 w^2` on
> `|w| <= 1`, `m >= 30` — the downstream `c_w = 1/2` combination still
> closes (0.362 + 0.09 = 0.452 < 0.5). The fixed wp2-b library changes NO
> certified printed digit (STATUS's prediction verified: the only output
> change anywhere is one float-noise diagnostic, 1.19e-16 -> 1.25e-16).
> wp1-c and wp2-b are therefore citable with their repair lists discharged;
> the T2 draft still awaits its MATHS referee (house-rule debt, STATUS §3) —
> nothing here changes that.**

Repair-script inventory (all under `g2_scripts/campaign_20260811/repairs/`):

| script | covers | verdict |
|---|---|---|
| `rep_wp1c_repairs.py` | §A: wp1-c R1, R2, R4, R5 (+ T2-F3's numbers) | PASS |
| `wp2b_lib_fixed.py` + `wp2b_nc{2,3,4}_*_fixed.py` (+ `wp2b_n0_resid_table.py` copy) | §B1: the two series coefficients, full re-runs | PASS (see §D) |
| `rep_wp2b_extras.py` | §B3, B4, B5, B7, B8 | PASS |
| `rep_t2f1_blam_chain.py` | §T1 (= referee F1, the mathematical repair) | PASS |
| `rep_t2_numerics.py` | §T2, T5, T6, T7, T8 (= F2, F5–F8) | PASS |
| `out_t2_nc10_rerun.txt`, `out_t2_nc5_rerun.txt` | §T4, T9 (= F4, F9): re-runs of the untouched originals | reproduced |
| `out_nc{2,3,4}_orig.txt` vs `out_nc{2,3,4}_fixed.txt` | §D: the no-digit-moves prediction | verified |

---

## A. wp1-c repair list (STATUS §2a; both referees MINOR_REPAIRS)

### A1 — ✅ CLOSED (R1 both referees: false margin side-claim)
**Obligation:** wp1-c §9 item 3 claims all 13 named constants rounded down
with "margin >= 5e-5"; FALSE for six of them.
**Done:** all 13 constants recomputed at dps 60 by BOTH the closed form of
`I(M, r)` and independent `mpmath.quad` quadrature (agreement < 1e-50);
every margin listed and positive (all roundings ARE safe-direction):

```
c_1(0) 9.12e-6   c_1(1) 8.99e-5   c_1(2) 6.21e-5   c_1(3) 2.35e-5
c_1(4) 8.39e-5   c_1(5) 5.29e-5   c_1(6) 1.25e-5   c_1(pi) 4.44e-5
c_1'(0) 8.22e-5  c_1'(1) 8.23e-5  c_1'(2) 1.48e-5  c_1'(4) 1.85e-5
c_V 5.13e-5      min = 9.116e-6 at c_1(0)
```

The six sub-5e-5 constants are exactly the referees' list
(`c_1(0), c_1(3), c_1(6), c_1(pi), c_1'(2), c_1'(4)`).
**Erratum text (fold in at assembly):** §9 item 3's "margin >= 5e-5" ->
"margin >= 9e-6 (min 9.1e-6 at `c_1(0)`), eleven orders above the evaluation
error". No constant changes.
**Script:** `rep_wp1c_repairs.py` (R1 block).

### A2 — ✅ CLOSED (R2 both: wrong digit in an aside)
**Obligation:** §5 "Beyond |lam| = 1.7627" prints `2 asinh(sqrt 10) = 3.7358`.
**Done:** recomputed at dps 60: `2 asinh(sqrt 10) = 3.7371022422...`.
**Erratum text:** "3.7358" -> "3.7371". Non-load-bearing (the rho-family
statement is unaffected).
**Script:** `rep_wp1c_repairs.py` (R2 block).

### A3 — ✅ CLOSED (R3 maths: W.5(iii) statement/proof range alignment)
**Obligation:** W.5(iii)'s statement gives the range
`t in [max(pi/m, t_0(lam)), pi]`, but its proof's case 1 establishes the
LARGER range `[pi/m, pi]` for all `|lam| <= pi/m` — which §6's coverage
table row 3 and the "supersedes (T.7b-final) on its whole scope" claim rely
on. Text-only (safe direction; no number involved).
**Erratum text (restated clause, fold in at assembly):**
> **Corollary W.5(iii')** (unified floor, case-wise). For `m >= 30`:
> (case 1) if `|lam| <= pi/m`, then `|phi_lam(t)| <= exp(-c_V m)` for ALL
> `t in [pi/m, pi]`;
> (case 2) if `pi/m <= |lam| <= 1.7627`, the same bound holds for
> `t in [t_0(lam), pi]`;
> with `c_V = q(1.5700, 1.00183) = 0.0372` in both cases.

This is exactly what the existing proof proves (its two cases verbatim); the
old statement is the (weaker) intersection of the two clauses.
**Script:** none needed (no numeric content; the constants are re-certified
under A1).

### A4 — ✅ CLOSED (R4 both: grid-only monotonicity vs the "no grid certificates" claim)
**Obligation:** "`sinh(x)/sin(x)` increasing on `(0, pi/2)`" (sets the worst
case `m = 30` for `r_V <= 1.00183` in W.5(iii) case 1) was certified only by
NC-W3's grid, contradicting §0/§9's "no grid-certified inequality appears
anywhere".
**Done:** the one-line proof is recorded and certified:
`F(x) := cosh x sin x - sinh x cos x` has `F(0) = 0` and
`F'(x) = 2 sinh x sin x > 0` on `(0, pi)`, so `F > 0`, and
`(sinh x / sin x)' = F(x)/sin^2 x > 0` — equivalently `tan x > tanh x` on
`(0, pi/2)`. Certified: the derivative identity at 200 random points (dps
60, max rel dev 1.7e-61), `sinh/sin` strictly increasing on a 1500-point
grid, `tan > tanh` spot checks.
**Erratum text:** add the one-line proof to W.5(iii)'s proof; the "no grid
certificates anywhere" claim then becomes true.
**Script:** `rep_wp1c_repairs.py` (R4 block).

### A5 — ✅ CLOSED (R5 both: two over-broad/imprecise wordings)
**Obligation:** (a) "Supersedes Lemma T.7c everywhere" ignores W.4's extra
hypotheses `K <= m/4`, `m >= 30`; (b) NC-W4(b)'s "reproduced: 292672" for
T2's "~2.5e5" is not a reproduction.
**Done/erratum text:** (a) -> "supersedes T.7c in every downstream use
(fixed `K`, `m >= 30`); for `K > m/4` use the rho-family W.5(i) on its
`t`-ranges". (b) -> "same convention recomputed: 2.9e5 (unit-step scan:
292672); T2 quoted '~2.5e5' — the discrepancy is in T2's own loop and is
immaterial, both old numbers being catastrophic". Certified by an
independent unit-step re-scan of all four (V)-thresholds:
old `292672` (s2 = C_0 easiest point) / `1065849` (uniform worst case);
new `879` (pi/m cut, c_V) / `185` (t_1 cut, c_1(pi)) — all four match
wp1-c's quoted values exactly.
**Script:** `rep_wp1c_repairs.py` (R5 block).

**A-summary: wp1-c's repair list is discharged; no constant, lemma,
hypothesis, or threshold changed (as both referees predicted).**

---

## B. wp2-b repair list (STATUS §2b; maths SURVIVES-WITH-MINOR-REPAIRS, numerics MINOR_REPAIRS)

### B1 — ✅ CLOSED (both referees: the two wrong series coefficients in `wp2b_lib.py`)
**Obligation:** `g4`'s small-u fallback last term must be `-u^5/15840` (not
`/22176`) and `g5`'s `-u^4/3168` (not `/4435.2`) — both were off by the
factor 5/7; re-run NC-W2/3/4; predicted impact <= 2.7e-6, no printed digit
moves.
**Done:** fixed COPY `wp2b_lib_fixed.py` created (originals untouched; the
derivation of the correct coefficients from `g`'s Bernoulli series —
`-3024/47900160 = -1/15840`, `-15120/47900160 = -1/3168` — is recorded in
its header). NC-W2, NC-W3, NC-W4 re-run as fixed copies importing the fixed
library. **Result: see §D — the prediction holds exactly** (byte-identical
outputs except one float-precision diagnostic line).
**Scripts:** `wp2b_lib_fixed.py`, `wp2b_nc2_dictionary_fixed.py`,
`wp2b_nc3_taylor_fixed.py`, `wp2b_nc4_assembly_fixed.py`; outputs
`out_nc{2,3,4}_orig.txt` / `out_nc{2,3,4}_fixed.txt`.

### B2 — ✅ CLOSED as errata (maths F2 / numerics 7: Prop W.6 status + `c_w(4)`)
**Obligation:** relabel Prop W.6 from "PROVED" to grid-certified; state
`c_w(4) = 1` (the grid's 0.9506 is not a safe bound — the maths referee's
finer grid finds 0.9509); note the harmless spurious `+(dir_ratio - 1)`
addend in the script's upper branch.
**Erratum text (fold in at assembly):** Prop W.6's status line ->
"grid-certified (explicit monotone formulas maximized on a w-grid at
m in {180, 500, 2000, 1e4}; worst at 180; per-piece monotonicity provable —
referee_maths_wp2_b §3 F2); `c_w(1) = 0.407`, `c_w(2) = 0.466` (so T.9's
`c_w = 1/2` stands for K <= 2, same certificate class), **`c_w(4) = 1`**
(the grid value 0.9506/0.9509 is not carried as a constant)". Add: "the
script's upper branch 1 carries a spurious `+(dir_ratio - 1)` addend; it
only weakens the bound (soundness unaffected)."
**Script:** none new needed (the relabeling is text; the underlying numbers
were re-verified by both referees and reproduce under the fixed library, §D;
the optional per-piece-monotonicity upgrade page is NOT done here — recorded
under "What remains").

### B3 — ✅ CLOSED (numerics 1: PW_grid K=4 exceeded beyond the grid)
**Obligation:** the certified 4.9126 (m <= 2000) is exceeded beyond the
grid; carry the K=4 grid row as "m <= 2000" or extend and restate ~4.93
(C_R^PT grid K=4: 5.2985 -> ~5.31).
**Done:** recomputed the pointwise bucket at K = 4 beyond the grid with the
FIXED library: max 4.9213 (m = 2000, w = 2.725), 4.9226 (5000), 4.9231
(10000), **4.9233 (20000, w = 2.725** — the referee's point, reproduced to
all four digits**)**, 4.9234 (50000) — still rising slowly.
**Erratum text:** Theorem W.7's K=4 grid-flavor row -> "4.9126 (certified
m <= 2000); ~4.93 on the extended tested range (m <= 5e4), still rising —
carry 4.93 as the working figure"; `C_R^PT grid` K=4: 5.2985 ->
**5.32** (= 4.93 + 0.01402 + 0.3719). Downstream §8 comparisons unaffected
at their stated precision.
**Script:** `rep_wp2b_extras.py` (B3 block).

### B4 — ✅ CLOSED (numerics 2 / maths F3: W.1(ii) exhaustive integer-m sweep)
**Obligation:** the all-integer-m max of the four-band deficit bound is
0.379644 at (32, 4.0) (mod-4 bumps), not 0.3789 at (30, 4.0); run the
exhaustive sweep.
**Done:** EXHAUSTIVE sweep, every integer m in [30, 400] x 200-point
w-grid on (0, 4], exact integer band sums `Q(n)` + dps-40 kernel values:
max = **0.379644 at (m, w) = (32, 4.0)** — the referee's value to all six
digits — `<= 0.40`, so **`c_4 = 0.60` stands**.
**Erratum text:** W.1(ii)'s certificate line -> "exhaustive integer sweep
m = 30..400 (max 0.379644 at (32, 4.0), mod-4 bumps) + the sampled tail
m = 500..3000 + continuum limit 0.367; still grid-certificate class in w
(Sturm-able on demand)".
**Script:** `rep_wp2b_extras.py` (B4 block).

### B5 — ✅ CLOSED (maths F4: the two one-line tail arguments, now inlined)
**Obligation:** inline the closing arguments for W.1(i) (all m >= 30) and
NC-W2(f)'s certificate ranges (the maths referee's V2 supplied them).
**Done, certified in exact integer/Fraction arithmetic:**
1. *W.1(i) fully proved for all m >= 30:* the reduction of
   `coef(m) = S_4/(240 m^2 lambda) <= 33/1000` to
   `330 m^2 (m-1)(2m+5) - 100(m+1)(2m+1)(3m^2+3m-1) = 10(6m^4 - 51m^3 -
   265m^2 + 10) >= 0` is verified as an exact polynomial identity;
   positivity for m >= 30 from `6m^2 - 51m - 265 = 3605 > 0` at m = 30 and
   increasing (vertex 51/12 < 30); exhaustive integer check 30..5000.
2. *NC-W2(f) closed for all m >= 30:* exact expansions
   `30(120 m^5 - 545(S_4+m)) = q4(m) = 330m^5 - 8175m^4 - 5450m^3 - 15805m`
   and `42(273 m^7 - 1500(S_6+m)) = q6(m) = 2466m^7 - 31500m^6 - 31500m^5 +
   10500m^3 - 64500m` verified as identities (exact ints, 4 spot m);
   termwise domination for m >= 30 (`330m >= 9900 >= 8175 + 1725`, etc. —
   the inequality chains are printed in the script); exhaustive Fraction
   check 30..3000.
**Erratum text:** add both arguments to W.1(i)'s proof and NC-W2(f)'s
caption; W.1(i)'s status is then PROVED-all-m with no sampled-range caveat.
**Script:** `rep_wp2b_extras.py` (B5 block).

### B6 — ✅ CLOSED as erratum (maths F5: envelope caveat sentence)
**Obligation:** add the sentence that the FINAL T.9 envelope can be fixed
only after wp2-a's `Delta_ker` lands (it may carry its own `w^2/m`
dependence).
**Erratum text (add to Prop W.6 and Theorem W.7):** "The audit above covers
parts (I) + (II) of the envelope only; the final T.9 statement's `c_w` can
be fixed only after wp2-a's `Delta_ker` bound lands — if that bound carries
its own `w^2/m`-order dependence, the merged `c_w` may exceed the audited
values even at K = 1."
**Script:** none (text only; structurally clear from W.7's decomposition).

### B7 — ✅ CLOSED (numerics 4: reword the c_w(4) cancellation diagnosis)
**Obligation:** §5's Finding / §9 item 2 claim the true envelope's
smallness needs a cancellation "coupling the buckets"; the numerics referee
measured that the cancellation is INTERNAL to W.6's two parts.
**Done:** the signed sum `[(B_lam - B_m) + 36a^2/P0^2]/(B_m w^2)` measured
with the fixed library over m in {60, 120, 300, 1000, 2000}, w in (0, 4]:
range **[+0.0050, +0.0114]** (referee, on m = 60..2000 with its own grid:
+0.0050..+0.0109 — consistent; the tiny upper-end extension comes from the
added m values/grid points). Parts (I) (~ -0.08 at w = 4) and (II) (~ +0.09)
cancel almost completely with no kernel involvement.
**Erratum text:** §5 Finding / §9 item 2 -> "the cancellation is internal
to W.6's two parts (measured signed sum +0.005..+0.011 B_m w^2); signed
two-sided kappa_3 (and kappa_5) boxes along the tilt suffice — no
bucket-coupling lemma is needed for `c_w(4) <= 1/2`" (the boxes themselves
remain future work, §9 item 4's wish list).
**Script:** `rep_wp2b_extras.py` (B7 block).

### B8 — ✅ CLOSED (maths F6 / numerics 5: trivia)
All certified in `rep_wp2b_extras.py` (B8 block) except where noted:
1. "(13 monomials of weight 8 and 10" -> **15** (counted from the committed
   `wp2b_n0_resid_table.py`: 28 total = 5 weight-4 + 8 weight-6 + 15
   weight-8/10).
2. §0's `PW_closed(4) = 187.5` -> **187.414**, `21.07` -> 21.063 (as the
   fixed-library re-run prints; safe-direction roundings, now exact quotes).
3. Drop numpy from the environment banner (no wp2-b script imports it) —
   text only.
4. `v := F(0) - 1 > 0` in Theorem W.7: now PROVED on W.7's stated scope
   `m >= 180`: the sufficient condition
   `|L''(0)|_box + h^2 sup|L''''|/12 < 1` (which gives
   `log F(0) >= h^2 (1 - |L''(0)|) - h^4 sup|L''''|/12 > 0`) evaluates via
   the nc3 boxes to 0.0080 (K=1) .. 0.1053 (K=4) at m = 180, decreasing in
   m — certified at every table (K, m) with m >= 180. (At K = 4, m = 30 the
   crude-box condition fails, 1.036 — outside W.7's statement; on the
   harness range the numerics referee measured v > 0 directly, min
   1.29e-5.)
5. Lin monotonicity: one line — `d/dx [e^{c/x}/x] = -e^{c/x}(c/x^3 + 1/x^2)
   < 0` and `s2min = c_K lambda(m)` is increasing in m, so `Lin(K, m)` is
   decreasing (and `m^2 Lin` too: 0.3719/0.2795/0.1346/0.0674 at
   m = 180/240/500/1000, K = 4).
6. Hermite sup certificates re-run on the FULL stated range `|y| <= 1/2`
   (1001-point grid, n = 1..8): all hold, max ratio 1.0000 (attained at
   He_1) — the grid now matches the statement (numerics repair 5a).
7. "`e^{1.5/s2min} <= 1.00001`" -> "**<= 1.000016** at m >= 180 (1.0000095
   at K = 1, 1.0000153 at K = 4)".

**B-summary: wp2-b's repair list is discharged; no table entry changed at
its printed precision except the two the referees demanded (K=4 grid-flavor
PW 4.9126 -> ~4.93 with scope label, hence C_R^PT grid K=4 5.2985 -> 5.32;
and the W.1(ii) max footnote 0.3789 -> 0.379644 at (32, 4.0)) — both in the
direction the referees measured, with `c_4 = 0.60` and every conclusion
standing.**

---

## C. T2 numerics-referee repair list (STATUS §3, findings F1–F9)

*(These repairs are prerequisites for T2's §5 "fully proved" list to stand;
they do NOT discharge the outstanding T2 MATHS referee pass — see "What
remains".)*

### T1 = F1 — ✅ CLOSED (moderate; **the only repair with mathematical content**)
**Obligation:** T.9 Step 2's chain for `B_lam = B_m(1 + theta * 0.35 w^2)`
uses the FALSE display `(1-delta)^{-2} <= 1 + 2.1 delta for delta <= 0.35`
(false for every delta >= ~0.025 — reproduced: at delta = 0.033 the two
sides are 1.06942 vs 1.06930). Repair options offered: restate for
`|w| <= 1` with a corrected inequality, or cite wp2-b Prop W.6.
**Done — replacement lemma, restated for the scope Theorem A's region-3
handoff actually needs (`w_0 <= 1`), every constant named and certified in
exact rationals:**

> **Lemma T.9-Step2' (valid `B_lam/B_m` chain).** For `m >= 30` and
> `|w| <= 1`:
> ```
> |B_lam/B_m - 1| <= 0.362 w^2 .
> ```
> *Proof.* `B_lam/B_m = ratio * R` with
> `ratio := kappa_4(lam)/kappa_4(0)`, `R := (lambda/s2)^2 = (1-delta)^{-2}`,
> `delta := 1 - s2/lambda in [0, 1)`.
> (1) `|ratio - 1| <= c_A w^2`, `c_A := 600/2200 = 3/11 <= 0.27273`
> [(T.4') recentred: `|kappa_4(lam) - kappa_4(0)| <= w^2 m^5/2200` on
> `|w| <= pi`, `m >= 30`; `|kappa_4(0)| = S*_4/120 >= m^5/600` from the
> certified `S*_4 >= m^5/5`, m >= 8].
> (2) `delta <= c_D w^2`, `c_D := 0.0330` [wp2-b Lemma W.1(i), now PROVED
> for ALL m >= 30 by repair B5 — this replaces (T.4)'s upper clause, whose
> m-dependent factor was the source of the old chain's trouble].
> (3) `(1-d)^{-2} <= 1 + 2d + 3.5 d^2` on `0 <= d <= 0.033`: exactly,
> `(1-d)^{-2} - 1 - 2d = d^2 phi(d)` with
> `phi(d) = 3/(1-d) + d/(1-d)^2` increasing (positive power-series
> coefficients) and `phi(0.033) = 2934000/935089 = 3.1377 <= 3.5` (exact
> rational).
> (4) Hence on `|w| <= 1`: `1 <= R <= 1 + c_R w^2`,
> `c_R := 2 c_D + 3.5 c_D^2 = 0.0698115 <= 0.0699`.
> (5) Upper: `B_lam/B_m <= (1 + c_A w^2)(1 + c_R w^2) <= 1 + (c_A + c_R +
> c_A c_R) w^2 = 1 + 0.361578... w^2 <= 1 + 0.362 w^2` (using `w^2 <= 1`);
> lower: `B_lam/B_m >= 1 - c_A w^2 >= 1 - 0.273 w^2`. QED

**Downstream check:** T2's Step-2 combination "0.35 + 0.09 < 0.5" becomes
`0.362 + 0.09 = 0.452 < 0.5` — **the `c_w = 1/2` sub-claim of T.9 Step 2
still closes on `|w| <= 1`** (certified in exact rationals). For
`1 < |w| <= 4` the envelope is superseded by wp2-b Prop W.6 (the campaign's
authoritative, both-refereed envelope: `c_w(1) = 0.407`, `c_w(2) = 0.466`,
`c_w(4) = 1`, grid-certified status per repair B2) — T.9's Step 2 should
cite it rather than carry its own wide-`w` chain, exactly as STATUS §4
recommends.
**Truth margin (dual precision, float fixed-lib vs mpmath dps 50,
agreement < 2e-5 at every grid point):** measured
`max |B_lam/B_m - 1|/w^2 = 0.1134` (at m = 30, small w; over
m in {30, 60, 120}, w in (0, pi]) — the referee's 0.1134 reproduced
exactly; the new proved 0.362 has a 3.2x truth margin on `|w| <= 1`.
**Erratum text:** replace T.9 Step 2's display and its "0.35 w^2" by the
lemma above (0.362 w^2, scope `|w| <= 1`) + the W.6 citation for wider `w`;
strike `B_lam/B_m = 1 + theta*0.35 w^2` from §5's "load-bearing pieces
fully proved here" list and enter Lemma T.9-Step2' in its place.
**Script:** `rep_t2f1_blam_chain.py` (blocks (a)–(d)).

### T2 = F2 — ✅ CLOSED ((T.9''c) constant rounded unsafely)
**Done:** the chain evaluates to 2.6113e-4 (first tail term 7.0139e-5,
divided by 0.2686) > printed 2.61e-4. **Erratum:** print **2.62e-4**
(statement true as corrected; NC-T9c's measured ratio 0.322 then has intact
slack). Also re-certified: (T.9''b)'s printed denominator 2.8e6 IS safe
(chain gives 2.855e6).
**Script:** `rep_t2_numerics.py` (F2 block).

### T3 = F3 — ✅ CLOSED (T.8-final's "(V) holds for m >~ 2.5e5" not reproducible)
**Done:** unit-step scans of (V) under both conventions (in
`rep_wp1c_repairs.py` R5, shared with wp1-c's A5): first-holds at
`m = 292672` at the easiest point `s2 = C_0 = 2000` (i.e. ~2.9e5; the T2
referee's independent recomputation gave ~2.96e5, same order, loop-grain
difference) and `m = 1065849` at the uniform worst case (~1.07e6).
**Erratum:** T.8-final's parenthetical -> "(numerically (V) then first
holds at m ~ 2.9e5 at the easiest point s2 = C_0, and m ~ 1.07e6 uniformly
over the hypothesis set; the previous '~2.5e5' was not reproducible)".
Mis-sizes only an admitted restriction; wp1-c's new exponents make the
point moot (879 / 185).
**Script:** `rep_wp1c_repairs.py` (R5 block).

### T4 = F4 — ✅ CLOSED (NC-T10 row's slack misquote)
**Done:** the untouched original `t2_nc10_far.py` re-run (output archived:
`out_t2_nc10_rerun.txt`); its part (c) rows give bound/measured slacks
0.9955/1.262e-3 = **789x** (m=30, w=3), 0.7838/9.729e-5 = 8.06e3x (30, 1),
0.9911/1.981e-6 = 5.0e5x (60, 3), 0.6143/1.447e-8 = **4.25e7x** (60, 1).
**Erratum:** §6 NC-T10 row "slack 24x-4e7x" -> "slack 789x-4.25e7x" (the
"24x" matches nothing in the script's output; conservative-direction
misquote, as the referee found).
**Artifact:** `out_t2_nc10_rerun.txt`.

### T5 = F5 — ✅ CLOSED (T.9'' step-2 parenthetical false at small m)
**Done:** `m <= 0.01 (m+1)^{r+1}/(r+1)` is FALSE at (m, r) = (4, 3)
(1.5625 < 4) and (5, 3) (3.24 < 5); TRUE for all m >= 6 (exact Fractions:
m = 6..1000 at r = 3; r = 3..12 at m = 6..40; increasing in r).
**Erratum:** "for r >= 3, m >= 4" -> "for r >= 3, m >= 6" (the lemma's
scope m >= 30 is unaffected).
**Script:** `rep_t2_numerics.py` (F5 block).

### T6 = F6 — ✅ CLOSED (T.4 crude clause chain reaches only m >= 3)
**Done:** `S_4/(m^2 lambda)` = 17.000 at m = 2 (chain's `<= 12` fails),
11.879 at m = 3 (chain OK). The clause itself is TRUE at m = 2 by direct
evaluation: deficit at w = pi is 0.43007 <= pi^2/20 = 0.49348.
**Erratum:** the crude clause's "(all m >= 2)" -> "(m >= 3 by the displayed
chain; m = 2 by direct check, deficit 0.4301 <= 0.4935)". Nothing
downstream uses m = 2 (T.10 needs m >= 53).
**Script:** `rep_t2_numerics.py` (F6 block).

### T7 = F7 — ✅ CLOSED (T.4 lower-bound display drops a term)
**Done:** the correct intermediate line is
`lambda - s2 >= (lam^2/240)[S_4(1 - w^2/19) - m] = (lam^2/240)[S*_4(1 -
w^2/19) - m w^2/19]`; the dropped `-(lam^2/240) m w^2/19` has relative size
`<= 5w^2/(19 m^4) <= 3.3e-6` at m >= 30, w <= pi (certified: max 3.21e-6) —
absorbed by the 0.02857 -> 0.0285 rounding, so (T.4)'s final constants
stand.
**Erratum:** insert the corrected display + the one-line absorption remark.
**Script:** `rep_t2_numerics.py` (F7 block).

### T8 = F8 — ✅ CLOSED (three typos)
**Done, all certified:**
(i) g'' series is `u/120 - u^3/1512 + u^5/28800 - ...` — the u^5
coefficient is `42/1209600 = 1/28800` exactly (exact Bernoulli arithmetic;
numeric limit 3.4718e-5 at dps 60), not 1/43200 (the consecutive-term-ratio
remark "1512/43200 = 1/28.6" becomes "1512/28800 = 1/19.05", still < 1 on
the used range — the alternating-decreasing argument is unaffected, indeed
the corrected ratio is what the referee's analytic tail bound in
`ref_kernel_smallu2.py` uses).
(ii) Step 0's `E U^2` at lam = 1/2 is `q(1+q)/(1-q)^2 = 6.294` (not 6.31);
2000/6.294 = 317.8, so "for m <= 316, s2 >= 2000 forces lam < 1/2" survives
a fortiori (and "s2 <= 6.31 m" may stand as a weaker safe bound if
preferred; the honest print is 6.294).
(iii) `t2_nc5_cf.py`'s docstring value is 0.015549; the true
`sin^2(1/8) = 0.015544` (the draft TEXT already has it right — the fix is
one character in a docstring; original script untouched per the no-erasing
rule, correction recorded here).
**Script:** `rep_t2_numerics.py` (F8 block).

### T9 = F9 — ✅ RECORDED (observation; no change demanded)
T.6(iii-final)'s displayed derivation is the loosest of the PROVED-stamped
items ("1/24 -> 1/6 to be safe", the unverified "|z| <= 0.19" chain); the
bound carries ~60x measured headroom (original `t2_nc5_cf.py` re-run,
archived `out_t2_nc5_rerun.txt`: (T.6iii-final) max ratio 0.0167 at m = 30,
w = 0.001). Safe as stated; flagged for the T2 maths referee to tighten or
re-derive at their discretion. No erratum text beyond this flag.
**Artifact:** `out_t2_nc5_rerun.txt`.

**C-summary: F1 is repaired with a new proved chain (restated scope
`|w| <= 1` + W.6 citation beyond); F2–F8 are display/typo errata, each
certified; F9 is flagged. T2's §5 "fully proved" list stands with
`B_lam/B_m = 1 + theta 0.35 w^2` REPLACED by Lemma T.9-Step2'
(`0.362 w^2` on `|w| <= 1`). The T2 MATHS referee pass remains outstanding
(house-rule debt) — these repairs feed it, they do not substitute for
it.**

---

## D. The fixed-library re-run: STATUS's no-digit-moves prediction — VERIFIED

STATUS §2b item 1 predicted the two coefficient fixes would move NO printed
digit (impact <= 2.7e-6, an order below every quoted margin). Protocol:
the three affected checks were run twice on 2026-08-11 — the untouched
originals from `g2_scripts/campaign_20260811/wp2_b/` (baseline) and the
fixed copies from `repairs/` (importing `wp2b_lib_fixed`) — and the full
outputs diffed:

| check | baseline vs fixed | result |
|---|---|---|
| NC-W2 (`wp2b_nc2_dictionary.py`) | `out_nc2_orig.txt` vs `out_nc2_fixed.txt` | **byte-identical** (incl. all box ratios 0.9994/0.9851/0.9983/0.5413/0.4761/0.7861/**0.9959**, deficit max 0.3789, floors 0.9698/0.8887/0.6629) |
| NC-W3 (`wp2b_nc3_taylor.py`) | `out_nc3_orig.txt` vs `out_nc3_fixed.txt` | identical except ONE line: the float-precision quadrature diagnostic "max rel dev phat vs Z*P = 1.19e-16" -> "1.25e-16" (machine-noise level, explicitly labeled "float-precision comparison" in the output; the certified Taylor table 0.02034..0.00009 and Pmin values unchanged) |
| NC-W4 (`wp2b_nc4_assembly.py`) | `out_nc4_orig.txt` vs `out_nc4_fixed.txt` | **byte-identical** (incl. PW 1.5491/4.0889/4.9126, PW_closed 10.278/21.063/187.414, c_w 0.4067/0.4658/0.9506, Lin 0.2308/0.2571/0.3719, and all 12 rows of the truth table (6)) |

All three fixed runs self-report PASS. **The prediction holds: no certified
printed digit moved; the only change anywhere is one float-noise diagnostic.**
(NC-W1 was not re-run: it does not exercise the g4/g5 fallbacks and its
generated `wp2b_n0_resid_table.py` would be regenerated in place, which the
no-erasing rule forbids; the maths referee verified it regenerates
byte-identically.)

Repair-B3/B4/B7 numbers quoted in §B were computed WITH the fixed library
(so the new working figures 4.93 / 0.379644 / [+0.0050, +0.0114] are
fixed-library values).

---

## Errata index (fold into the drafts at paper-assembly; recorded above)

Into `wp1_draft_c.md`: A1 (margin claim), A2 (3.7371), A3 (W.5(iii')
case-wise statement), A4 (one-line sinh/sin proof; then the "no grid
certificates" claim is true), A5 (two wordings).
Into `wp2_draft_b.md`: B1 (lib coefficients — use `wp2b_lib_fixed.py`),
B2 (W.6 relabel + `c_w(4) = 1` + spurious-addend note), B3 (K=4 grid row
scope/4.93; C_R^PT 5.32), B4 (0.379644 at (32, 4.0) footnote + exhaustive
label), B5 (two inlined tail arguments; W.1(i) -> PROVED all m >= 30),
B6 (envelope-awaits-Delta_ker sentence), B7 (reworded Finding), B8 (seven
trivia incl. v > 0 proved on m >= 180).
Into `g2_draft_t2_20260803.md`: T1 (Lemma T.9-Step2' replaces the broken
Step-2 display; §5 "fully proved" list amended), T2 (2.62e-4), T3 (2.9e5 /
1.07e6), T4 (789x–4.25e7x), T5 (m >= 6), T6 (m >= 3 + direct m = 2 check),
T7 (restored display line), T8 (1/28800; 6.294; docstring 0.015544),
T9 (flag only).

---

## What remains for G2 after this doc

The repairs above discharge STATUS §4 item 1 ("apply the listed repairs")
in full. Unchanged by this session (STATUS §4 items 2–5, verbatim):

1. **The T2 MATHS referee pass** (house-rule debt; STATUS §3). These
   repairs are input to it, not a substitute. Until it runs, T2's PROVED
   inventory retains single-referee status.
2. **wp2-a: the kernel-transfer + denominator bucket `Delta_ker`** (T2 §8
   item 4's last piece; exactly defined in wp2-b Theorem W.7; true size
   ~1.4/4.1/5.0 C_R units). Then the T.9 merge (W.7 + Delta_ker + wp1-c
   W.4(i)) with explicit C_R(K) and the m_2(1) = 143-class thresholds.
3. **The harness extension to m ~ 200** (covers the 150 < m < m_2(K) bands;
   STATUS §5 item 4 — a separate scripted session per the no-erasing rule).
4. **Prop 3.5(i)'s genuinely open mathematics**: the deep-tilt core model
   lemma (strip-analyticity route), T.8's C = 600 bucket certificate, and
   the region-2 handoff (wp3-a) — untouched here.
5. Optional upgrades flagged but not required: W.6's per-piece monotonicity
   page (B2), the signed kappa_3/kappa_5 boxes that would prove
   `c_w(4) <= 1/2` (B7), Sturm certificates for the remaining grid-class
   items (W.1(ii) in w, PW grid).

*End of repairs_20260811.md. All scripts and outputs:
`g2_scripts/campaign_20260811/repairs/`. No pre-existing file was modified
or overwritten.*
