# G2 closure, draft T1 — tilted-frame window law with uniform-in-lambda constants

*Blind-protocol note: this draft was written from `F2_PROOF_DRAFT.md` (merged draft),
`g1_draft_b.md` (refereed, SURVIVES — its B.0–B.9 may be cited as established), and
`mahonian.py` only; no other `g2_*` file or script directory was read. Route: the
direct transfer prescribed by g1_draft_b §8.4 — repeat the B.0–B.9 skeleton for the
tilted factors (truncated geometrics), with every constant tracked uniformly in the
tilt parameter `lam`. Scripts in `f2_drafts/g2_scripts/t1/` (paths resolved relative
to `__file__`; stdlib+numpy only). Status of each section is marked PROVED / PARTIAL /
FLAGGED at its head; the honest ledger is §T.10.*

**Target (= merged draft Prop 3.5 + the region-2 variance bound filed inside G2):**

(i) *(crude uniform law)* absolute `C_0, C` such that for every interior `k` with
`sigma_lam^2 >= C_0` (`lam = lam(k)` the mean-matching tilt):
`r(k) - 1 = sigma_lam^{-2} (1 + theta C / min(m, sigma_lam^2))`, `|theta| <= 1`.

(ii) *(refined small-tilt law)* for every fixed `K >= 1` and `|lam| <= K/m`
(`w := lam m`, so `|w| <= K`), with constants uniform on `|w| <= K`:
`sigma_lam^2 (r(k) - 1) = 1 - B_m (1 + O(w^2)) + O(m^{-2})`, all constants explicit.

(iii) *(variance lower bound, Theorem A region 2)* explicit `c > 0`, `m_0` with
`sigma_lam(k)^2 >= c k (1 + k/m)` for `sqrt(m)/4 <= k <= N/2`, `m >= m_0` — so
`sigma_lam^2 >= C_0` holds there for large `m`, and the (i)/(ii) ranges OVERLAP for
every `K >= 1` (killing the historical `[1/m, 3.7/m]` coverage hole; see §T.9).

---

## T.0 Notation; tilted cumulants in closed form; uniform expansions  [PROVED]

## T.1 Tilted characteristic function: factorization, cf identity, Gaussian domination  [PROVED]

## T.2 Log-series in the tilted frame: cumulant bounds uniform in lam  [PROVED]

## T.3 Small-tilt model approximation (tilted B.1) and integral toolkit  [PROVED]

## T.4 Odd-term cancellation in the symmetric second difference  [PROVED]

## T.5 Kernel and factorization transfer in the tilted frame (tilted B.5–B.6)  [PROVED]

## T.6 Refined small-tilt window law (tilted B.7–B.9) = Prop 3.5(ii)  [PROVED modulo listed finite computations]

## T.7 Crude uniform law at large tilt = Prop 3.5(i)  [PARTIAL — see §T.10]

## T.8 Variance lower bound sigma_lam^2 >= c k(1+k/m)  [PROVED]

## T.9 Overlap of regimes; assembly for Theorem A regions 2–3  [PROVED given T.6–T.8]

## T.10 What remains — honest ledger  [READ THIS]

## T.11 Numeric-check index

*Sections are filled in below in order; this header block is the map.*

---
