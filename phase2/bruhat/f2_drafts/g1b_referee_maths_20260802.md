# Adversarial referee report — `g1_draft_b.md` (G1 closure) — MATHS PASS
> Date: 2026-08-02. Referee: blind adversarial pass (Claude Fable 5 agent, independent of the
> drafting sessions), instructed to break the proof. Companion computational audit:
> `g1b_referee_numerics_20260802.md`. This closes HANDOFF Lane-1 item 1 ("referee g1_draft_b").

## (i) VERDICT: **SURVIVES WITH MINOR REPAIRS**

I attacked every step B.0–B.9, recomputed the key algebra by hand, re-derived the kernel-integral constants independently, re-ran all shipped scripts, and re-certified the polynomial inequalities with *exact* real-root isolation (the draft used floating `nroots`). No fatal or major error exists. The analytic skeleton is genuinely airtight given the merged draft's Lemmas 1.1–1.5, and the constants table reproduces exactly. Required repairs are listed under (ii); all are hours-of-work, none is new mathematics.

**What I verified positively (highlights):**
- **B.0**: re-certified all five polynomial inequalities with exact isolation: largest real roots in (10.0950, 10.0952), 25.67, 6.87, 1.000, (29.8837, 29.8846) — all below the claimed thresholds 11/30/30/30/30, positive leading coefficients, exact-rational positivity at thresholds. TRUE, now rigorously.
- **B.1**: `c_8` arithmetic checked by hand (`a_4 = ζ(8)/4π⁸ = 1/37800`; `2/(37800·256·9) = 1/43545600` ✓); term-ratio ≤ 1/2 at `t_1` is an exact-endpoint computation ✓; the exponential-Taylor bookkeeping (`U_0² − β²t⁸ ≥ 0`, direction of every inequality) ✓. Independent 60-digit mpmath check: ratio ≤ 0.485 for m up to **150** (I extended beyond the draft's 100) — bound holds.
- **B.2(i)** sign pattern (`−γt⁶ → +g He₆`) rederived by hand ✓.
- **B.3**: I re-summed the scaled table by hand and got `C_1''(110) = 3.826` exactly as claimed; superpoly piece 0.306 at m=110 (matches "0.31"); monotonicity exponents (`α ≤ 4.5`, `c = 0.193`, decreasing from m ≈ 24) ✓; total 4.13 < 4.3 ✓. Ground truth: `σm³|E₂| ≤ 0.115` at m = 20–60 ✓.
- **B.4**: `(15·0.03674 + 52.5·0.0081)/√(2π) = 0.3895`; `+4.3/110 → 0.4286 < 0.45` ✓.
- **B.5**: product-to-sum factorization verified by hand; it is correct including the x±1 ↔ k±1 bookkeeping.
- **B.6**: I re-derived the Gaussian-moment integral from scratch and got exactly the displayed per-n factor `(π/λ^{n+2})(2n−1)!!(2n+2)` and the cross-term bracket; the `Delta_out`/`Delta_tail` prefactors are ~2× *conservative* relative to my sharper derivation (safe direction); `2πλ²Δ_box = KB/m³` with KB(180) = 106.52 confirmed by run.
- **B.7**: regenerated the **full 10-monomial N-table symbolically — every integer coefficient matches exactly, no extra monomials, b-linear cancellation confirmed**; the quartic collapse `16He₃²+12He₂He₄−28He₆ = 240y⁴−1008y²+384` recomputed by hand (y⁶: 16+12−28=0 ✓); the identity `−(log P)'' = 12bHe₂ + N/P²` verified symbolically as rational functions.
- **B.7'**: Taylor-with-integral-remainder constant h⁴/12 ✓; the exact `λh² = 1` and `e^{h²}` Gaussian-ratio steps ✓.
- **B.8/B.9**: the `u = (v+w)/(1+δ)`, `z = (w−δv)/((1+δ)(1+v))` algebra ✓; the conversion constant `λ|w| = 2πλ²e^{y²+h²}|ΔD|/(P₋P₊)` ✓; at table points |z| ≈ 3·10⁻¹⁰ so the 1.02 absorption is safe. `g1b_final.py` reproduces the §6 table: C₂ = 1.073/1.564/3.023/37.60/3934.3/474.6, honestly rounded to 1.1/1.6/3.1/38/3940/475. Cross-row `e^{y₀²}` scaling of each bucket hand-checked and internally consistent.
- **End-to-end truth** (exact Mahonian rows, m = 20–60): measured `m²|E₁|` → 0.21/0.53/0.92/7.0/58.8 for y₀ = 0.1/0.5/1/2/3, all far under the proved C₂; center residual matches the derived `N(0)/P(0)² = 384b²−90g ≈ −0.195/m²` to 3–4 digits — the merged draft's NC-7 calibration is indeed now a theorem-level derivation.
- **No circularity with G2**: nothing tilted is used anywhere in B.0–B.9; inputs are exactly merged-draft Lemmas 1.1, 1.2, 1.3(i)(ii), 1.4, 1.5 (I additionally spot-verified merged 1.3(ii)'s termwise algebra — j=2 surplus 3m²−15 ≥ j=m deficit m²−1 for m ≥ 3 — since B.1(a) leans on its term-ratio display).

## (ii) Issues found

1. **MINOR — false coverage claim for small m in Cor B.4**: "*C_1 = 0.45 (for m >= 110; 4 <= m <= 150 is covered exactly by the harness, NC-1)*". NC-1 verifies argmin/varfit facts, **not** the pointwise bound `|E(k)| ≤ 0.45/(σm²)`; NC-B3 samples only m ∈ {20,30,40,60}. As written, the pointwise constant on 4 ≤ m ≤ 109 is *asserted, not established*. Repair: one finite exact loop over 4 ≤ m ≤ 109 (minutes), or restate Prop 2.1 with the m ≥ 110 threshold.
2. **MINOR — threshold band 150 < m < m₁(y₀)**: window law proved only for m ≥ 180 (y₀ ≤ 1), harness exact only to 150. The draft flags this itself (§8.2) and the ledger's Prop 2.2 permits `m ≥ m_1(y_0)`, so it is not statement drift — but any downstream all-m use (G4/(c)) is invalid until the harness is actually extended (currently planned, not done).
3. **MINOR — float-grade certificates**: B.0 was certified via floating `nroots` (roots near thresholds: 10.095 vs 11; **29.884 vs 30 — margin 0.12**); I re-proved these with exact isolation, so B.0 stands, but the shipped scripts alone are not referee-grade. Same for `polymax`/corner/`p_j` sups (grid + float critical points; margins huge). Draft concedes Sturm upgrade; it should be done for a paper.
4. **MINOR — unstated monotonicity of P_min in m**: C₂'s validity for all m ≥ m₁ divides buckets by P_min(m₁)², implicitly needing P_min(m) ≥ P_min(m₁) (true — deviation |P−1| ~ b·sup|He₄| ~ 1/m — but never argued). Likewise the rectangle "worst corner" scan is endpoints-only while P, N are nonlinear in b; harmless (critical points sit at b ~ O(m), far off the width-10⁻³/m rectangle) but a one-line remark is owed.
5. **MINOR — notational error in Lemma B.6's `Delta_tail` display**: "`v_a` are the raw coefficients of V" combined with `v_a lambda^a T_a` double-counts λ^a read literally. The scripts (and Prop B.3(IV)'s `b λ² T_2` pattern) compute the correct quantity `βT₂+γT₃+(β²/2)T₄`; fix the display.
6. **COSMETIC — Prop B.3 prose "(II) and (IV) are < 1e-9"**: actually ≈ 3·10⁻⁹ at m = 110 by direct computation; immaterial (0.16 slack in the 4.3 budget).
7. **COSMETIC — §6 table, y₀ = 0.1 "measured" column**: reads 0.194 "(center)"; the true window sup at m=60 is 0.206 (referee rerun). Bound 1.1 dwarfs both; relabel.
8. **COSMETIC — script hygiene**: hardcoded `/Users/sihaohuang/...` paths break the measured-truth sections of `g1b_truth.py`/`g1b_final.py` on other machines (contradicting the header's "runs from anywhere"; the constants table itself runs fine); `g1b_truth.py`'s double-float Lemma-A check emits garbage ratios ~10⁵¹ (pure cancellation — the 60-digit `g1b_lemA.py` is the real check and passes); dead line 83 in `g1b_const2.py`; the `min(·,0.5)` cap in `deltE`'s denominator would be unsound for large dbar (dbar ≈ 10⁻⁶ here, no effect).

**Numerics-vs-proof audit:** every load-bearing numerical step is either an exact symbolic computation (N-table) or a finite evaluation of explicitly displayed closed-form bounds (C₁'', KB, SP, dbar, corner sups) — admissible as proof steps in principle; issues 1 and 3 are the only places where the *executed* computation falls short of the *claimed* certification. NC-B1/B3/B5/B9 are honest sanity checks, not load-bearing.

## (iii) Statement match vs ledger

**Substantively matched, with one mild drift.** Corollary B.9 is the ledger's Prop 2.2 **verbatim-in-substance** — `σ²log r(k) = 1 + B_m(y²−1) + E₁(k)`, `|E₁| ≤ C₂(y₀)/m²` for `|y| ≤ y₀`, `m ≥ m₁(y₀)` — exactly the shape the ledger's G1 row and Prop 2.2 prescribe, now with explicit constants (C₂(1) = 3.1, m₁ = 180) and, beyond the ask, the exact m⁻²-order term `N(y)/P(y)²`. Corollary B.4 delivers Prop 2.1's display with C₁ = 0.45 but adds a threshold m ≥ 110 that the ledger's Prop 2.1 ("for every k") does not carry — that is the drift, and it is repairable by the finite check of issue 1. Conclusion: **G1 is closed in substance for m ≥ m₁(y₀)**; the residual obligations are the two finite computations (issues 1–2), correctly and honestly flagged by the draft itself, plus the referee-grade certificate upgrade (issue 3). G2 is untouched, as the draft states.
