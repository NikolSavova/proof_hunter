# Phased reflected-direction closure lane

The result and exact scope are in `CLOSURE_RESULT.md`.

Core artifacts:

- `phased_predicate.py`: enlarged minimal one/two-translate abstract predicate;
- `phased_lemma_verify.py`: literal elementary-shape audit through `t=100`;
- `test_phased_predicate.py`: randomized abstract-to-literal regression;
- `periodic_phase_gadget.py`: exact alternating parity architecture;
- `staircase_family.py`: unbounded four-parameter construction and exact
  `85/294` obstruction identity;
- `phased_verify.py`: independent literal `A_t+A_t` certificate checker.

All JSON files are deterministic outputs of those scripts.  OR-Tools is not
required for this lane.
