# Erdős #1208 handoff — 2026-08-19

## 2026-08-20 direct cube-root restart — optimal orientations reduce to nested endpoint cores

The arithmetic tower record below is now frozen as a separate partial upper
bound.  The direct `1/3` attack has resumed at the preferred adaptive swap
gate.  For any loopless multigraph, choose an orientation minimizing

\[
 \mathcal E_2=\sum_v(d^+(v))^2
\]

and put `U_t={v:d^+(v)>=t}`.  The new exact theorem is

\[
 \sum_{t\ge1}|E(G[U_t])|
 \le \mathcal E_2
 \le |E|+\sum_{t\ge1}|E(G[U_t])|.
\]

Indeed, reversing one optimal edge `u->v` gives `d^+(u)<=d^+(v)+1`;
summing the tail loads gives the identity, with the error equal exactly to
the number of edges whose tail load exceeds the head load by one.  A dyadic
layer argument then shows that any excessive orientation energy produces an
induced core `U_t` which is simultaneously dense and mass-bearing through
`t|E(G[U_t])|`.

For the adaptive swap multigraph, `|E|=O_K/2` and the charge moment is
twice `E_2`.  Hence the cube-root theorem is exponent-equivalent to the
nested endpoint packing estimate

\[
 \sum_{t\ge1}|E(G_{\rm sw}[U_t])|
 \le K N^{o(1)}|E(G_{\rm sw})|.
\]

This is strictly better targeted than maximum degeneracy: stars contribute
nothing to the nested term, while a polynomial failure exposes a dyadic
mutually-rich core with the correct size bias.  Read
`SWAP_OPTIMAL_ORIENTATION_NESTED_CORE_GATE.md` and run
`verify_swap_optimal_orientation_nested_core.py`.

The exact optimal-orientation stress is now also complete through Costas
size 37 and radial side 8.  A path-reversal algorithm is certified against
exhaustive orientations.  The normalized optimum energy `E_2/(K|E|)` stays
below `0.39` on every genuine row but grows from `1.50` to `9.66` on radial
rows; the optimal maximum load is at most `13` on the genuine rows and is
`85` on radial side 8.  The physical-endpoint audit changes the next target:
in the Costas-37 maximizing core, `48.33%` of the edge copies have eight
distinct fixed physical endpoints, and another `34.47%` have only a
cross-role contact.  Thus the asymptotic survivor is matching-heavy, not a
shared-endpoint star.  Read
`SWAP_OPTIMAL_NESTED_CORE_STRESS_AND_MATCHING_SURVIVOR.md` and run
`analyze_swap_optimal_nested_cores.py --larger`.

The matching branch now has a sharper exact reduction.  For its core
multigraph `H_t`, with `M_t` edge copies, put

\[
 W_t=\sum_v {d_{H_t}(v)\choose2}.
\]

Since `|U_t|<=|E|/t`, Cauchy gives
`W_t>=2tM_t^2/|E|-M_t`.  Therefore the linear wedge estimate
`W_t<=K N^{o(1)}M_t` implies the required `tM_t<=K N^{o(1)}|E|`.
Canonical mixed endpoint potentials split these wedges into parallel,
repeated-potential, endpoint-contact, and fully diffuse twelve-endpoint
branches.  On Costas 37 the last is the largest (`839929/2119705=39.62%`),
while repeated potential has zero mass.  A clean same-potential cell pair
does have a rigorous injection into `P_L^2`, where
`P_L={d in D:Ld in D}`, but the dominant core is potential-injective.
Read `SWAP_MATCHING_WEDGE_POTENTIAL_GATE.md` and run
`verify_swap_matching_wedge_potential_gate.py`.

**Immediate next action:** derive the exact endpoint normal form for a
diffuse twelve-endpoint wedge together with one further high-core extension
at each neighbouring cell, and prove its linear packing bound
`W_t^12<=K N^{o(1)}M_t`.  Parallel reuse and endpoint-contact remain separate
remainder branches.  Do not return to a one-cell pointwise bound: the exact
stress now shows that the load-bearing configuration is a diffuse,
matching-like partial orthogonal array with distinct mixed potentials.

That extension has now been completed and collapses to a matching-cell
four-cycle gate.  Write `h_z` for the number of vertices in one active
matching component, `mu_z` for its maximum parallel multiplicity, and `r_z`
for its maximum simple pair-codegree.  Exact length-two-path counting gives

\[
 d_{\rm weighted}(H_z)
 \le\mu_z(1+\sqrt{r_zh_z}).
\]

Hence the matching branch closes from the single support-compensated bound
`mu_z^2 r_z h_z<=K^2N^{o(1)}`.  Every edge copy has the symmetric normal form

\[
 a=J(s-z-Lb-Lc),\qquad
 a,b,c,s-b,s-c,z+Lc,z+Lb\in D,
\]

with both reconstructed shifts adaptive-popular.  Genuine maximum component
sizes/codegrees through Costas 37 are only `(h,r)=(9,4),(10,5),(9,5),(11,7)`
for primes 23,29,31,37.  Costas 37 has 63,119 simple matching four-cycles;
16,786 use sixteen distinct physical endpoints, while the majority now has
an endpoint contact.  Read `SWAP_MATCHING_C4_COMMON_NEIGHBOUR_GATE.md` and
run `verify_swap_matching_c4_common_neighbour_gate.py`.

The common-neighbour factor now has a lossless support/collision dichotomy.
For opposite cells at displacement `d`, one selected pair of incident edge
copies at a common centre defines `r=s_B-s_C` and realizes the three linked
differences

\[
 r,\qquad r+d,\qquad J(r+Ld)\in D-D.
\]

Selecting one `r` per common centre gives
`n^2<=X(n+2Q)`, where `X` is the number of selected `r` labels and `Q` is
the number of equal-`r` centre pairs.  Every such repeated pair is a fully
decorated four-cycle carrying the five displacements
`delta,Ldelta,h,h-delta,J(h-Ldelta)`, with the duplicated roles and all four
popular-shift pairs retained.  Thus the exact residual is now a global
packing theorem for distinct correlated triples versus repeated-`r`
five-direction collisions.

The split is genuinely balanced on the hard stress: on Costas 37, 29,811
of 63,119 cycles have no common `r` on either diagonal; in the sixteen-
endpoint branch the count is 8,004 of 16,786.  Fixed coarse cells are not
unique (maximum opposite/`r` centre load 5 and `(z,d,r)` load 16).  By
contrast, the lifted modular-parabola `m^2` equality model has exactly eight
adaptive popular shifts, matching components of size at most three, and no
matching four-cycles through prime 43.  Read
`SWAP_MATCHING_COMMON_R_SUPPORT_COLLISION_DICHOTOMY.md` and run
`verify_swap_matching_common_r_support_collision.py`.

Endpoint contact has now been separated rigorously from clean codegree.  If
`kappa_z` is the maximum number of cells in the component containing one
physical endpoint and `r_z^circ` is the maximum codegree of a pair with
eight distinct physical endpoints, then

\[
 d_{\rm wt}(H_z)
 \le\mu_z\left(1+\sqrt{(4\kappa_z+r_z^\circ)h_z}\right).
\]

Repeated `r` is invariant under switching the diagonal of a matching
four-cycle.  Since adjacent cells are physically disjoint, every contact
lies on a diagonal; switching to it routes every contact repeated-`r`
cycle to the endpoint term.  The surviving repeated branch therefore has
sixteen physical endpoints.  A fixed clean `(B,C,r)` extension also has an
exact pair-linear eight-corner form with projections

\[
 u,\qquad y,\qquad u+y,\qquad u+Jy,
\]

plus four adaptive-popular constraints.  Read
`SWAP_MATCHING_ENDPOINT_PENCIL_CLEAN_CODEGREE_GATE.md` and run its verifier.

There is now a strictly sharper weighted treatment of the endpoint term.
For a centre cell `C` and physical endpoint `x`, put

\[
 \lambda(C,x)=\sum_{B\sim C:x\in E(B)}m(C,B),\qquad
 P(C,x)=\sum_{B<B':x\in E(B)\cap E(B')}m(C,B)m(C,B').
\]

Then the endpoint-contact wedge mass is at most
`P_end=sum_{C,x}P(C,x)`.  If `M` is the matching edge-copy mass and
`Theta=max P(C,x)/lambda(C,x)`, exact double counting gives

\[
 P_{\rm end}\le8\Theta M.
\]

Thus the direct endpoint theorem is the aggregate linear estimate

\[
 \boxed{P_{\rm end}\le K N^{o(1)}M.}
\]

It removes `h_z`, couples parallel multiplicity to the pencil where it
actually occurs, and discounts a single heavy parallel fibre exactly.  For
two copies sharing one oriented endpoint the complete comparison is

\[
 \delta,L\delta,\rho,\delta+\rho,\delta+J\rho\in D-D,
 \qquad \delta\in D,
\]

with both popular-set differences retained.  Exact `P_end/M` values are
only `0.281,1.431,2.612,1.469,1.481` on Costas primes
`17,23,29,31,37`, versus `K` between `9.5` and `11.1`.  Read
`SWAP_MATCHING_WEIGHTED_ENDPOINT_PENCIL_GATE.md` and run its verifier.

**Current immediate action:** prove the weighted endpoint-pencil estimate
above and the clean common-`r` support/collision packing.  For the endpoint
branch, retain the raw physical difference and the two simultaneous
popular-set differences; for the clean branch, retain the pair-linear
eight-corner system.  Do not return to an unweighted `D-D` representation
bound or multiply independent maxima `mu_z,kappa_z,h_z`.

## 2026-08-20 stronger record — quadratic-11235917 plus CM gives 0.49368323

The strongest certified bounds in the repository are now

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49368323}.
\]

The bounded-inertia/Eisenstein-CM construction uses

\[
 E=\mathbb Q(\sqrt{11235917}),\qquad
 11235917=7\cdot11\cdot337\cdot433.
\]

At the exact optimum `T=217`, PARI certifies
`Cl(E)=C_14 x C_2`, narrow class group `C_14 x C_2 x C_2`, trivial
localized `S`-class group, 219 exact S-unit squareclasses, and ray/sign rank
four.  Thus the safe Kummer rank is `d=215`.  All 11,123 required CM-useful
ideals pass with zero rejections; the relation count is

\[
 r\le216+217+11123=11556,
 \qquad 4r=215^2-1.
\]

The exact real-tower input is
`log RD=318.9527585916460414427...`.  The continuous all-depth threshold is
`0.4936832199308881880151091959...`; at the safe headline exponent the
100/150-digit endpoint margins are `0.0016328170...` and `0.0032470752...`,
and every omitted local slope is inactive.  The exact nearby-count audit has
unique optimum `T=217`.

Read `REAL_QUADRATIC_11235917_CM_EISENSTEIN_RECORD.md` and run
`verify_cm_eisenstein_real_quadratic_11235917.py`.  The independent
`INDEPENDENT_HOSTILE_AUDIT_QUADRATIC11235917_CM.md` and its verifier rebuild
the localized basis, ray kernel, complete usefulness scan, GS budget, root
discriminant, endpoint, and nearby counts and obtain the same result.

This is a rigorous partial upper-bound improvement, not a solution of Erdős
#1208.  It remains far above the conjectural cube-root exponent, and the
finite base-field search is not an asymptotic optimality theorem.

The ordinary and mandatory genus-relaxed searches over every positive
fundamental discriminant `10,000,001<=D<=12,000,000` are complete.  The
ordinary pass found the certified field above.  The genus pass enumerated
607,918 fields and 12,811 relaxed-positive nominations.  Its four
half-million leaders were all exact false positives: localization at the
first 205 ideals kills their `S`-class groups, the ray image has full rank
four, and the true generator rank returns to `d=T-2`.  Their corrected
favorable margins range from `-0.94348...` to `-2.67571...`.  Read
`REAL_QUADRATIC_CM_GENUS_SEARCH_TEN_TO_TWELVE_MILLION.md` and run its full or
`--exact-only` verifier.  This is a finite nomination/search audit with the
scope stated there, not a theorem about all larger discriminants or
nonprefix assignments.

## 2026-08-20 stronger record — quadratic-6999893 plus CM gives 0.49368416

The strongest certified bounds in the repository are now

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49368416}.
\]

The bounded-inertia/Eisenstein-CM construction uses

\[
 E=\mathbb Q(\sqrt{6999893}),\qquad 6999893=31\cdot349\cdot647.
\]

The exact optimum is `T=219`, not the discovery screen's fixed-alpha count.
The certified `C_4` class-group calculation gives 221 explicit S-unit
squareclasses, ray/sign rank four, and safe Kummer rank `d=217`.  All 11,335
required CM-useful ideals pass with zero rejections; the relation count is

\[
 r\le218+219+11335=11772,
 \qquad4r=217^2-1.
\]

The exact real-tower input is `log RD=322.04430443122049...`.  The continuous
all-depth threshold is `0.4936841573736362558301044898...`; at the safe
headline exponent the 100/150-digit endpoint margins are `0.0004338013...`
and `0.0008626918...`, with all omitted slopes inactive.  Exact nearby counts
`216..223` have unique optimum `T=219`.

Read `REAL_QUADRATIC_6999893_CM_EISENSTEIN_RECORD.md` and run
`verify_cm_eisenstein_real_quadratic_6999893.py`.  The independent
`HOSTILE_AUDIT_QUADRATIC6999893_CM.md` and its verifier explicitly reconstruct
the cyclic-four S-unit basis, ray kernel, full usefulness scan, GS budget,
root discriminant, and endpoint and obtain the same result.

The norm-prefix all-square/all-useful count is locked across every
`50<=T<=600`.  An exact nested `C_4` S-unit basis has ray rank four throughout,
and an exhaustive all-depth sweep finds `T=219` uniquely feasible.  The
closest count `T=217` has favorable high-precision margin
`-0.00173352039...`; the winner retains adverse margin `+0.00055311353...`.
Read `QUADRATIC6999893_CM_PREFIX_COUNT_LOCK.md` and run its Python/C++
verifier pair.  Its scope is norm-prefix/all-square; nonprefix and mixed
inertia are handled separately rather than inferred from this scan.

At the exact winning count `T=219`, the complementary structural lock is
also complete.  The cyclic-four class and residual ray coordinates give 16
exhaustive cells; crossing them with all 220 square/fourth-power cap counts
excludes 3,519 noncanonical assignments.  Uncapped inertia is rigorously
dominated by a fourth-power cap.  The closest excluded case is the canonical
prime assignment with one square cap weakened to a fourth-power cap, whose
favorable optimized margin is already `-0.2570248351...`; all rank-gaining
cells are much worse.  Read `QUADRATIC6999893_CM_STRUCTURAL_LOCK.md` and run
its verifier.  Its scope is fixed `T=219`; together with the count lock it
covers the broad prefix-count line and the full structural cross-section at
the winner, not their complete cross-product.

This is a rigorous partial upper-bound improvement, not a solution of Erdos
#1208.  The broader finite base-field search is still active, and no finite
screen is treated as an asymptotic optimality theorem.

The ordinary real-quadratic screen over every positive fundamental
discriminant `6,000,001<=D<=10,000,000` is complete.  Its eight half-million
intervals contain 1,215,889 fields; the broad-to-dense all-useful filter finds
`D=6999893` as the unique overall leader, and none of the later interval
leaders approaches its fixed-target margin.  Read
`REAL_QUADRATIC_CM_SEARCH_SIX_TO_TEN_MILLION.md`.  The optimized scanner now
has a compile-time norm cutoff plus an explicit prefix-completeness assertion.
This is a reproducible finite search audit, not an asymptotic dominance result.

The previously separate interval `5,000,001<=D<=6,000,000` is also complete:
303,942 positive fundamental discriminants were screened.  Its ordinary-rank
leader `D=5277317` loses at the live exponent, but a relaxed genus allowance
appeared to revive it.  Exact localization shows instead
`Cl(E)=Cl^+(E)=C_12`, trivial prefix `S`-class group already at `T=205`, and
full ray rank four, so the true rank remains `d=T-2`.  Every
`205<=T<=250` then fails even in the favorable all-useful model; the best cell
has margin `-0.1047595999...`.  The stronger exact-usefulness audit finds zero
CM rejections at the best `T=221` prefix and threshold
`0.4936846497781865...`, still worse than the live record.  The other three positive screen candidates
are excluded with best favorable margins at most `-0.2499649349...`; their
apparent genus bonuses likewise disappear in the exact ray kernel.  Read
`REAL_QUADRATIC_5277317_CM_PREFIX_NO_GO.md`,
`HOSTILE_NO_GO_QUADRATIC5277317_CM.md` and
`QUADRATIC_5M_CANDIDATES_EXACT_NO_GO.md`, and run their verifiers.  Together
with the adjacent search notes, this finishes the ordinary finite screen from
two through ten million, subject to the stated count/prefix filter.

## 2026-08-20 stronger record — quadratic-4108373 plus CM gives 0.49368647

The strongest certified bounds in the repository are now

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49368647}.
\]

The bounded-inertia/Eisenstein-CM construction uses

\[
 E=\mathbb Q(\sqrt{4108373}),\qquad 4108373=17\cdot67\cdot3607.
\]

At the exact winning prefix `T=217`, the certified class-number-two
calculation has trivial localized class group, 219 explicit S-unit
squareclasses, ray/sign rank four, and safe Kummer rank `d=215`.  All 11,123
required CM-useful ideals pass with zero rejections; the last useful norm is
121,367.  The relation budget is

\[
 r\le216+217+11123=11556,
 \qquad4r=215^2-1.
\]

The real-tower input is `log RD=317.6510581919590...`.  The exact all-depth
threshold is `0.493686459809675808859...`; at the safe headline exponent the
90/150-digit endpoint margins are `0.00165395...` and `0.00328909...`, and
the maximum omitted local slope is below the active frontier.  Exact nearby
counts `213,215,...,225` have unique optimum `T=217`.

Read `REAL_QUADRATIC_4108373_CM_EISENSTEIN_RECORD.md` and run
`verify_cm_eisenstein_real_quadratic_4108373.py`.  The independent
`QUADRATIC4108373_CM_EXACT_RECORD.md` and its verifier reconstruct the field,
kernel, usefulness scan, GS budget, all-depth endpoint, and nearby count
table separately.  A third implementation,
`HOSTILE_AUDIT_QUADRATIC4108373_CM.md` with
`verify_hostile_quadratic4108373_cm.py`, explicitly rebuilds the nonprincipal
class-number-two S-unit basis and again obtains the same kernel, zero-rejection
useful prefix, root discriminant, and threshold.

At the winning count `T=217`, the construction is also locked against every
nonprefix prime-ideal assignment and every mixture of square, fourth-power,
and uncapped tame inertia.  Exact class/ray coordinates give 16 structural
cells; crossing these with all 218 cap counts yields 3,487 noncanonical cases.
All are excluded by all-depth, all-anchor endpoint certificates.  The closest
cell (one square changed to a fourth-power cap) already has favorable margin
`-0.25517...`; every assignment that gains Kummer rank pays much more in root
discriminant.  Read `QUADRATIC4108373_CM_STRUCTURAL_LOCK.md` and run its
verifier.  Its scope is fixed `T=217`; it is not a broad-count theorem.

The complementary norm-prefix count lock checks every `50<=T<=600` under
the favorable all-square/all-useful model.  Exact ray rank is four throughout
and `T=217` is uniquely feasible.  The closest excluded count `T=215` has
high-precision favorable margin `-0.00321518...`; the winner retains adverse
margin `+0.00211516...`.  Read `QUADRATIC4108373_CM_PREFIX_COUNT_LOCK.md` and
run its Python/C++ verifier pair.  Together the two locks close a broad prefix
count range and the full structural cross-section at the winning count, but
not their complete cross-product at all counts.

`REAL_QUADRATIC_CM_SEARCH_TWO_TO_FIVE_MILLION.md` records the exact finite
discriminant coverage and optimistic broad-to-dense nomination screen that
found this field.  It also records the independently certified fallback
`D=3200972`, `T=215`, whose threshold is `0.4936875853118874...`.  The search
is not an optimality theorem beyond its finite interval or outside the stated
prefix/all-square filter.  The theorem above rests only on the exact field
verifiers.  Erdos #1208 remains open and the result remains far from the
conjectural cube-root exponent.

## 2026-08-20 stronger record — quadratic-2278757 plus CM gives 0.49368818

The strongest certified bounds in the repository are now

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49368818}.
\]

The bounded-inertia tower uses

\[
 E=\mathbb Q(\sqrt{2278757}),\qquad 2278757=13\cdot59\cdot2971,
\]

followed by the Eisenstein CM one-coordinate disk packing.  The exact
class-number-two calculation at `T=223` gives 225 independent S-unit
squareclasses, four sign/mod-4 conditions, and safe Kummer rank `d=221`.
The selected primes kill the ordinary class group.  The explicit direct
S-unit basis and an independent PARI row-space computation agree.

There are 11,765 useful Frobenius-square caps and zero CM-usefulness
rejections; the last useful norm is 128,591.  With 223 inertia squares,

\[
 r\le222+223+11765=12210,
 \qquad 4r=221^2-1.
\]

The exact real-tower input is `log RD=328.9023201108733...`.  The safe
continuous threshold is `0.4936881673432459...`; at the advertised exponent
the independently reconstructed hostile verifier has endpoint margins
`0.00216812...` and `0.00431177...`, while the canonical verifier also passes
at 90 and 150 digits.  Read `HOSTILE_AUDIT_QUADRATIC2278757_CM.md` and run
`verify_hostile_quadratic2278757_cm.py`; the separate
`verify_cm_eisenstein_real_quadratic_2278757.py` is a second full certificate.

This improves the preceding theorem but still does not solve #1208.  A
larger finite base search is in flight; optimistic fields are not promoted
until their S-unit kernel, CM usefulness, root discriminant, and all-depth
endpoint have all been certified.

## 2026-08-20 stronger record — quadratic-821453 plus CM gives 0.49369313

The strongest certified bounds in the repository are now

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49369313}.
\]

The winning bounded-inertia tower is over

\[
 E=\mathbb Q(\sqrt{821453}),\qquad 821453=467\cdot1759,
\]

followed by the Eisenstein CM one-coordinate disk packing.  For the first
219 odd prime ideals, the exact BNF/S-unit/ray calculation gives class number
one, 221 S-unit columns, four independent sign/mod-4 conditions, and Kummer
rank `d=217`.  The narrow class number is two, so there is no norm-minus-one
unit; the rank-four ray calculation and a separate integer sign/residue audit
handle this explicitly rather than assuming full unit signatures.

With 219 inertia-square caps and 11,335 useful Frobenius-square caps,

\[
 r\le218+219+11335=11772,
 \qquad 4r=217^2-1.
\]

The full exact CM-usefulness scan has zero rejections.  The ramified prefix
ends at `(1213,1213,split root 395)` and the useful prefix ends at
`(122527,122527,split root 3683)`.  The ideals over the base-ramified primes
467 and 1,759 and the inert norm-nine ideal over 3 are all handled explicitly.
The real-tower input is `log D_L=322.225490258272...`.

At `alpha=0.49369313`, `w_0=40752.95`, the 150-digit endpoint margins are
`0.00116605599...` and `0.00119653743...`; the fixed-anchor crossings are
`0.4936931229546...` and `0.4936931263645...`.  The active right slope is
strictly above every omitted fourth-and-later local depth.

Read `REAL_QUADRATIC_821453_CM_EISENSTEIN_RECORD.md` and run
`verify_cm_eisenstein_real_quadratic_821453.py`.  The independent
`HOSTILE_AUDIT_QUADRATIC821453_CM.md` and
`verify_hostile_quadratic821453_cm.py` reconstruct the exact Kummer kernel,
usefulness scan, endpoint, and all-square non-prefix ray-rank cases; they find
no flaw.  In particular, lowering the four-row ray rank by a non-prefix
ramified set raises the root discriminant enough to make every such case much
worse.

`REAL_QUADRATIC_CM_SEARCH_TO_ONE_MILLION.md` records the finite discovery
screen over all 303,957 positive fundamental discriminants at most one
million.  It is explicitly a floating, optimistic nomination screen rather
than an optimality theorem.  Its secondary genus-bonus mode flags
`D=880440` and nearby fields for exact S-class/ray audits; it makes no exponent
claim for them.

The exact **prime-prefix** audit of the five leading genus candidates is now
complete.  In every case the first 180 ideals already trivialize the localized
class group, the exact ray image has rank four, and hence `d=t-2`, with none
of the optimistically granted genus dimensions surviving.  For `D=880440`,
even an all-useful relaxation over every `180<=t<=280` has global best margin
`-1.6761378...` at the live exponent; the other four leaders are worse.
Read `QUADRATIC_GENUS_PREFIX_LEADERS_EXACT_NO_GO.md` and run
`verify_quadratic880440_genus_no_go.py`.  This result is deliberately
prefix-only.

The nonprefix loophole is also closed for the leading field `D=880440`
through the full dense window `205<=|T|<=250`.  The rank-aware verifier
enumerates all 67 ordinary-class subspaces and all 714 relevant ray-color
subspaces.  Proper class spans and reduced local-ray spans are killed even
after granting favorable generator ranks, declaring every outside ideal
useful, and allowing useful roles to reuse ramified ideals.  The generic
full-span case is reduced to the true norm prefix by an exact all-depth
exchange argument, including separate checks for norms 3, 5, and 7.  Read
`GENUS_BONUS_880440_RANK_AWARE_NO_GO.md` and run
`verify_genus_bonus_880440_rank_aware_no_go.py`.  Its precise remaining scope
is mixed inertia, counts outside `205..250`, or a different presentation;
the original genus-screen candidate family is dead.

The next three ordinary all-useful leaders from the million-field screen are
also exactly eliminated.  For `D=979277,994733,544268`, the ray image has
rank four and `d=T-2` throughout `205<=T<=250`; all three exact CM usefulness
scans actually have zero rejections.  Nevertheless their best optimistic
all-useful endpoint margins at `alpha=0.49369313` are respectively
`-0.41527`, `-0.50272`, and `-0.75946`, with the all-anchor concavity and
depth exclusions certified.  Read `ORDINARY_CM_NEXT_LEADERS_EXACT_NO_GO.md`
and run `verify_ordinary_cm_next_leaders_exact_no_go.py`.

The next two genus-rich leaders are eliminated nonprefix as well.  For
`D=963480` and `D=937365`, an exact PARI audit partitions every ramified set
with `205<=|T|<=250` by all 67 ordinary-class subspaces and all 714 proper
ray subspaces.  The proper-span branches remain negative even after granting
overly favorable Kummer ranks and global reuse of useful ideals.  Full ray
span forces the exact rank `d=T-2`, where all-depth exchange reduces the
problem to the norm prefix; the closest margins are still `-1.99919` and
`-2.06324`.  Read `GENUS_BONUS_963480_937365_RANK_AWARE_NO_GO.md` and run
`verify_genus_bonus_963480_937365_rank_aware_audit.py`.  Its scope is the
all-square presentation and the stated count window.

The prior `D=43133` record is also now locked against every square/fourth/
uncapped inertia mixture and arbitrary prime-ideal reassignment, including
the rank-changing `d=222,223` cases.  Read
`QUADRATIC43133_CM_MIXED_ASSIGNMENT_LOCK.md` and run its verifier.

The new `D=821453` record has now received the same stronger lock.  Across
all five exact ray-rank classes and all 220 square/fourth-cap counts, the
all-square norm-prefix cell is uniquely feasible at `alpha=0.49369313`.
The closest competitor (one fourth-power inertia cap) already has favorable
all-anchor margin `-0.2561...`; all rank-reducing nonprefix cells are much
worse, and uncapped inertia is dominated by a fourth-power cap.  Read
`QUADRATIC821453_CM_MIXED_ASSIGNMENT_LOCK.md` and run its standalone verifier.
An independent reuse of the earlier field-independent engine, documented in
`QUADRATIC821453_CM_MIXED_INERTIA_RANK_ASSIGNMENT_LOCK.md`, reproduces the
full cross-product and the strictly increasing threshold diagnostics
`0.4936931244...` (zero changes), `0.4936943413...` (one change), through
`0.4943215686...` (all 219 changes).

The norm-prefix ramified count has also been locked across the much broader
range `50<=T<=600`.  Exact ray columns have rank four throughout, so every
one of the 551 all-square cells has `d=T-2`; an all-depth exhaustive endpoint
sweep, favorably declaring every eligible ideal useful, finds `T=219`
uniquely feasible already at `alpha=0.4936931245`.  The nearest competitor
`T=217` has certified favorable margin `-0.00126665...`, while the winner
retains adverse-constant margin `+0.0000118246...`.  Read
`QUADRATIC821453_CM_PREFIX_COUNT_LOCK.md` and run
`verify_quadratic821453_cm_prefix_count_lock.py`.  This is a finite prefix
count lock, not a tail theorem or a nonprefix result outside the already
audited winning count.

The new exponent is a genuine partial upper theorem, not a resolution of
#1208.  It remains far above the conjectural cube-root scale.

## 2026-08-20 stronger record — quadratic-43133 plus CM gives 0.49369772

The strongest certified bounds in the repository are now

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49369772}.
\]

The winning construction combines bounded tame inertia over

\[
 E=\mathbb Q(\sqrt{43133})
\]

with the Eisenstein CM one-coordinate disk packing.  PARI's unconditional
`bnfcertify` check gives class number one.  For the first 223 odd prime
ideals, the exact S-unit/ray calculation gives 225 columns, four independent
sign/mod-4 constraints, and generator rank `d=221`.  With 223 inertia-square
caps and 11,765 useful Frobenius-square caps,

\[
 r\le222+223+11765=12210,
 \qquad 4r=221^2-1.
\]

The full mod-3 CM usefulness scan has zero rejections; the last useful norm
is 129,629.  The inert norm-9 ideal above 3 lies in the ramified prefix and
is excluded from useful caps; its CM ramification is already paid through
the relative discriminant.  The real-tower root-discriminant input is
`log D_L=331.612492408513...`.

At `alpha=0.49369772`, `w_0=42282.88`, the 150-digit endpoint margins are
`0.0013481280...` and `0.0013585741...`.  The fixed-anchor roots are
`0.493697712148876...` and `0.493697716021533...`, and the active right
slope is strictly above every omitted fourth-and-later depth.

Read `REAL_QUADRATIC_43133_CM_EISENSTEIN_RECORD.md` and run
`verify_cm_eisenstein_real_quadratic_43133.py`.  The discovery audit
`scan_cm_eisenstein_real_quadratic_bases.cpp` screened all 30,394 positive
fundamental discriminants at most 100,000 under an explicitly optimistic
all-useful model; the theorem itself uses only the exact field verifier.

This improves the previous exponent by about `1.376e-5`, but remains a
partial upper theorem rather than a resolution of #1208.  The expected
endpoint is still `1/3+o(1)`.

A separate exact-prime-ideal screen of the first 200 totally real cubic and
first 200 totally real quartic fields found no improvement.  Two cubic
fields (`Disc=4729,5521`) looked competitive only under an over-generous
narrow-class/rank model; exact sign and dyadic Kummer matrices reduce their
constructible rank to `d=t-3`, after which even an all-useful relaxation is
strongly negative at the live endpoint.  No quartic crossed even the
optimistic screen.  Read `SMALL_DEGREE_CM_BASE_SCREEN_NO_GO.md` and run
`verify_small_degree_cm_base_screen.py`.

## 2026-08-20 exact obstruction — the quadratic-1949 excess relation is unavoidable

The combined quadratic-1949/CM record remains

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49371148}.
\]

The tempting replacement of the base-presentation estimate `r_0<=d+1`
by `r_0<=d` is impossible for the live prefixes.  An exact Kummer and
Hilbert-symbol computation gives

\[
 (d,r_0)=(225,226)\quad\text{for 227 ideals},
 \qquad
 (d,r_0)=(227,228)\quad\text{for 229 ideals}.
\]

The localized global cup products have ranks 226 and 228, respectively,
matching the Shafarevich--Koch upper bound.  Consequently the cup map spans
all of `H^2`: the excess relation cannot be deleted, and no nonzero linear
combination of the base relations can be promoted to Zassenhaus degree at
least three.  Read
`QUADRATIC1949_SHAFAREVICH_EXCESS_EXACT_OBSTRUCTION.md` and run
`verify_quadratic1949_shafarevich_excess_obstruction.py`.

The live optimization lanes are now the broader real-quadratic/CM base
search and genuinely different inertia/geometry choices; base-relation
cleanup is closed for the current field.

The mixed-inertia lane is also closed at the two certified endpoints.  For
`s_2` inertia squares and `s_4` inertia fourth powers, the exact weighted
Golod--Shafarevich polynomial shows that the maximum useful-square count is
`12430-s_2`, independently of `s_4`.  Fourth-power caps strictly dominate
uncapped inertia, but an all-depth endpoint dual proves that every
square-to-fourth-power downgrade worsens both margins even after crediting
the extra useful ideal.  The same dual excludes arbitrary prime-ideal
reassignment.  Read `QUADRATIC1949_CM_MIXED_INERTIA_OPTIMIZATION.md` and
run `verify_quadratic1949_cm_mixed_inertia_rank225.py`.

The product-disk geometry is sharp within the current averaging framework.
For any bounded body in `C^m`, the geometric mean of its coordinate
projection diameters is at least `4/pi` times the corresponding volume
scale, with equality only for products of disks.  The matching anisotropic
one-sided ideal-packing lemma is also optimally allocated.  Hence, with the
current CM covolume input fixed, nonproduct bodies, unequal embedding
budgets, and correlated rectangular partitions cannot improve the
effective constant `2sqrt(3)/pi`.  Read
`NONPRODUCT_MINKOWSKI_BODY_SHARP_OBSTRUCTION.md` and run
`verify_nonproduct_minkowski_body_obstruction.py`.  Any escape must use
lattice-specific point excess, successive minima/conjugate correlations,
or a better arithmetic covolume input.

## 2026-08-20 combined record — quadratic-1949 plus CM gives 0.49371148

The strongest certified bounds in the repository are now

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49371148}.
\]

Combine the bounded-inertia tower over `E=Q(sqrt(1949))` with the
one-coordinate CM lattice `L(zeta_3)`.  Under the Eisenstein useful-prime
criterion the honest local optimum shifts to the first 227 odd prime ideals:

\[
 d\ge225,\qquad |T|=227,\qquad |\mathcal Q|=12203,
\]

and

\[
 r\le226+227+12203=12656,\qquad4r=225^2-1.
\]

The exact mod-3 Frobenius scan on the 225-dimensional Kummer kernel has zero
rejections.  It explicitly handles the base-ramified ideal above 1,949,
whose residue root is `omega -> 1/2` and whose functional is nonzero on the
kernel.  The last ramified and useful norms are 1,297 and 134,129, and
`log D_L=342.3225986272...`.

The complex covolume and relative-discriminant calculation gives the safe
effective constant

\[
 {2\sqrt3\over\pi}<{71603\over64935}.
\]

At `alpha=0.49371148`, `w_0=43932.44`, the two all-depth endpoint margins
are `0.0001363825...` and `0.0003361149...`; the continuous threshold is
`0.49371147915679...`.  The active right slope remains strictly above every
omitted fourth-and-later depth.

Read `QUADRATIC1949_CM_EISENSTEIN_RECORD.md` and run the canonical
`verify_cm_eisenstein_real_quadratic_1949.py`.  The independent
`verify_quadratic1949_cm_eisenstein.py` checks the neighboring 229-ideal
configuration and the compositum edge case.  An honest nearby scan turns at
227, and the optimistic screen over every positive fundamental
discriminant at most 2,000 still ranks 1,949 first.

This is a new partial upper theorem, not a resolution of #1208; the expected
endpoint remains `1/3+o(1)`.

## 2026-08-20 new record — real-quadratic base gives 0.49371211

The strongest certified bounds in the repository are now

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49371211}.
\]

The new upper construction runs the bounded-inertia/product-disk argument
over

\[
 E=\mathbb Q(\sqrt{1949}).
\]

The field has class number one, a norm-minus-one unit with full signature
rank, and an explicitly certified 227-dimensional totally positive dyadically
unramified Kummer subspace supported on the first 229 odd prime ideals.  With
229 inertia-square caps and 12,425 useful Frobenius-square caps,

\[
 r\le12882,\qquad 4r=227^2-1.
\]

The exact all-depth certificate at `alpha=0.49371211` and
`w_0=44705.08` has endpoint margins `0.0009550783...` and
`0.0019043034...`; the fixed-anchor limiting threshold is
`0.4937121047388...`.  The nearby honest scan of 221--237 ramified ideals
turns at 229.  Read `REAL_QUADRATIC_1949_BOUNDED_INERTIA_RECORD.md` and run
`verify_real_quadratic_1949_bounded_inertia.py`.

An independent CM/Eisenstein refinement is also proved.  Replacing two real
coordinates by one coordinate in `L(zeta_3)` changes the product-disk
constant from `4/pi` to `2sqrt(3)/pi`; covolume, relative discriminant,
projection, divisor switching, and split-prime normalization all survive.
On the rational rank-221 tower it gives the weaker but independent bound
`0.49371364`.  Read `CM_EISENSTEIN_ONE_COORDINATE_REFINEMENT.md` and run
`verify_cm_eisenstein_disk_rank221.py`.  Combining this geometric refinement
with the new real-quadratic arithmetic is the immediate live calculation;
do not quote a combined exponent until its own exact verifier is committed.

This remains a partial upper theorem, not a solution of #1208.  The expected
endpoint is still `1/3+o(1)`.

## 2026-08-20 stocktake — rank-221 assignment locked; pro-3 retired

The strongest certified bounds remain

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49371397}.
\]

Two possible optimization loopholes around the new upper construction are
now closed.  First, an all-depth fractional-knapsack dual proves that, for
the rank-221 bounded-inertia/product-disk certificate, assigning the first
222 odd primes to ramification and the next 11,767 odd primes to useful
Frobenius caps maximizes both endpoint margins even if every unramified odd
prime is optimistically declared useful.  The proof retains every local
depth and the small root-discriminant correction; it is not merely a finite
swap search.  The actual useful prefix has no rejected prime.  Read
`ARBITRARY_PRIME_ASSIGNMENT_DOMINANCE.md` and run
`verify_arbitrary_prime_assignment_dominance.py`.

Second, the pro-3 cubic-cap analogue is arithmetically sound but
noncompetitive.  Its exact all-depth optimum in the audited safe family is
near

\[
 d=38,\qquad N=7138,\qquad \alpha=0.495265158699\ldots,
\]

with the rigorous rounded certificate `0.49526518`.  The losses are
structural: a Frobenius cube guarantees residue degree only up to three,
and ternary layers require the full phase interval `[w,3w]`.  Even the
impossible relaxation deleting every base quadratic relation remains above
`0.495218`, still well behind the pro-2 theorem.  Read
`PRO3_CUBIC_CAP_DOMINANCE_AUDIT.md` and run
`verify_pro3_cubic_cap_dominance.py`.

The tame pro-2 base-relation count also has no hidden deficiency saving:
tame ray class theory makes the abelianization finite, so abelianizing a
minimal presentation forces `r>=d`, while the Shafarevich presentation gives
`r<=d`; hence `r=d`.  A wild-2 replacement has not produced a rigorous
rank/discriminant improvement.

**Honest proof-distance verdict.**  These results materially strengthen and
stress-test the best upper construction, but #1208 is not near completion.
The exponent gap from `0.49371397` down to the conjectural `1/3` is still
large, and the principal lower-bound/ambient shortcuts have explicit
counterexamples.  A full solution still requires a new global geometric
packing theorem or a qualitatively different construction, not a cleanup of
the current rank-221 optimization.  Quadratic-base and CM/Eisenstein
variants are being explored, but neither is counted here until a complete
proof and exact verifier exist.

## 2026-08-20 latest checkpoint — bounded tame inertia gives 0.49371397

The strongest certified bounds in the repository are now

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49371397}.
\]

This is a substantial new upper-frontier improvement over `0.49458516`.
For each odd rational ramified prime `p`, quotient the tame totally-real
pro-2 tower by the closed normal closure of the square of one tame inertia
generator.  Tame pro-2 inertia is procyclic and all primes over `p` are
conjugate, so this one quadratic relator forces every inertia index over `p`
to be at most two.  The relator lies in the Frattini subgroup, hence the
generator rank and useful Frobenius squareclasses survive.  The local tame
relation is compatible because `p` is odd.  Consequently every ramified
prime contributes only `p^(1/2)` to the tower root discriminant.

The globally optimized certificate uses

\[
 d=221,\qquad |T|=222,\qquad |\mathcal Q|=11767,
\]

with the first 222 odd primes ramified and all 222 inertia generators
square-capped.  The relation count is

\[
 r\le221+222+11767=12210,\qquad 4r=221^2-1,
\]

and

\[
 \log D={1\over2}\sum_{p\in T}\log p
 =672.4345398746246682\ldots.
\]

Combining this tower with the positive product-disk packing constant
`4/pi<424/333`, the exact all-depth frontier at
`alpha=0.49371397`, `w_0=84898` has dyadic endpoint margins
`0.005797832833...` and `0.006661701419...`.  The active right slope
`0.019026780776...` is strictly above the largest omitted fourth-depth slope
`0.015366907016...`.  Read
`BOUNDED_INERTIA_PRODUCT_DISK_OPTIMIZATION.md` and run
`verify_bounded_inertia_disk_rank221.py`.  The independent presentation
audit `BOUNDED_TAME_INERTIA_TOWER_IMPROVEMENT.md` and its verifier certify
the slightly rounded `0.493714` endpoint from the same arithmetic data.

The optimization was checked at every rank `20<=d<=400` and diagnostically
through rank 2,000; the tested family turns at rank 221.  At rank 221 every
partial inertia-cap count is worse than using all 222 caps, and tested
contiguous ramification offsets are worse than the initial-prime block.
These are finite optimization audits, not a theorem over every possible
noncontiguous ramification set or number field.  A valid order-four inertia
variant and all mixed order-two/order-four choices at rank 713 were also
checked and are weaker.

The real-cubic base `E=Q(zeta_7)^+` was reoptimized after adding bounded
inertia.  It now certifies `0.4941486`, an improvement over its old value but
still weaker than the rational rank-221 tower.  Read
`REAL_CUBIC_BOUNDED_INERTIA_REOPTIMIZATION.md` and run
`verify_real_cubic_bounded_inertia.py`.

The product-disk ingredient underneath the new theorem remains as follows.
Every algebraic squared distance `eta=u^2+v^2` is strictly positive in every
real embedding, so the divisor switch uses the one-sided packing lemma

\[
 |\mathfrak a\cap[0,Y]^d|
 \le \left(1+\frac{Y}{N(\mathfrak a)^{1/d}}\right)^d
\]

while averaging `Sigma(O_K^2)` over a product of planar disks of area
`R^2` bounds every conjugate squared distance by `(4/pi)R^2`.  The same two
external inputs as in the previous tower proof remain declared: the tame
totally real Shafarevich presentation theorem and the placewise prime-power
construction.

Finite-group endpoint projections have also been closed as a standalone
lower-bound route.  For every modulus `M` and suitable `K`, there are genuine
integer distance-Sidon sets of `Omega(K)` points at critical height
`M^2 K^(3/2+o(1))` whose shadow is exactly balanced on `(Z/MZ)^2`, while
preserving anisotropy, the endpoint cocycle, and the rank-four distance
matrix.  Thus subpolynomial collections of finite-field shadows cannot by
themselves force the cube-root theorem; a modular proof must retain integer
quotients, carries, or actual norm labels across primes.  Read
`FINITE_GROUP_ENDPOINT_PROJECTION_CRITICAL_BARRIER.md`.

**Current strategic verdict.**  #1208 remains open, with a large exponent
gap.  The best recent progress is a material upper-frontier improvement and
a substantially sharper map of what cannot close the lower-bound side.
The ambient centroid gate, its covolume repair, scalar-energy gates,
directional charges, ordinary inverse/BSG extraction, fixed-alphabet
hierarchies, mixed Frobenius powers, and fixed-degree base-field
amplification all have verifier-backed counterexamples or dominance
barriers.  A full solution still needs a new endpoint-sensitive global
packing theorem or a genuinely different upper construction; it is not a
one-lemma cleanup of the present notes.

The latest fixed-alphabet audit makes one construction barrier precise:
equal Gaussian-polynomial norm fibres have subexponential size, and a local
equal-distance rewrite does not survive arbitrary common suffixes because
of the cross term.  Hence finite collision gadgets cannot tensor with
positive Shannon entropy; high energy survives only through exponentially
many globally coordinated short translation components.  Read
`FIXED_ALPHABET_LOCAL_COLLISION_SHANNON_CAPACITY_NO_GO.md` and run its
verifier.

## 2026-08-20 route closure — the ambient centroid gate is false

The unconditional headline remains

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49458539}.
\]

The proposed grid theorem bounding clean equal-centroid mass by
`m^(o(1))(k^3+m^2)` is now **unconditionally false**.  Index-`p` lattice
averaging produces a determinant-`p` transform of the Welch--Costas array
with coefficient height `p^(1/2+o(1))` and only `p^(1+o(1))` norm
collisions.  Sparse deletion leaves a genuine Euclidean distance-Sidon set
of size `p^(1-o(1))` and height `p^(3/2+o(1))`, while its six-distinct
centroid mass is `p^(4-o(1))`.  This violates the ambient gate by a full
`p^(1-o(1))` factor but lies at the expected critical scale, so it does not
disprove #1208.  Read
`BALANCED_COSTAS_NEAR_SEPARATOR_LATTICE_AVERAGING_NO_GO.md`.

Two attempted repairs are also closed.  A half-density parity subset of the
lifted modular parabola has bounded generated-lattice covolume but fourth-
order clean centroid energy, killing global and local covolume-core bounds;
read `LOCAL_COVOLUME_CORE_PARITY_LIFT_NO_GO.md`.  Exact common-content and
normalized-length bookkeeping does delete short and sparse scale layers and
reduces the old gate to dense all-long 12-matching blocks, but the lifted
modular parabola has `Omega(m^2)` mass deep inside that survivor.  Read
`NORMALIZED_LONG_EDGE_QUOTA_AND_BLOCK_SATURATION.md`.  These are structural
diagnostics, not a remaining near-proof, because their parent ambient gate
is false.

On the upper-construction side, arbitrary non-product hierarchical digit
languages with additive energy below `n^(5/2-eta)` still contain
`n^(1/2-o(1))` distance-Sidon subsets.  Slowly growing Gaussian alphabets
and subexponential carry ambiguity do not evade this; complete digit
alphabets collapse exactly to the ordinary square grid.  Read
`HIERARCHICAL_LANGUAGE_ENERGY_CARRY_DICHOTOMY.md`.

**Status of that checkpoint's immediate action:** superseded.  The real
cubic tower has now been made rigorous and reoptimized with bounded inertia;
it gives `0.4941486`, so it remains weaker than the rational rank-221
certificate at the top of this handoff.  Mixed fourth-power caps are also
valid but do not improve the current record.

## 2026-08-20 newest checkpoint — the certified upper frontier is 0.49458539

The strongest construction is now the rank-713 all-depth
quadratic-Frobenius certificate.  It strictly improves the earlier
rank-715 value `0.494586`.  The current unconditional bounds established in
this repository are

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49458539}.
\]

`proof_frobenius_all_depth_rank713.md` gives the proof and
`verify_frobenius_all_depth_rank713.py` checks the 713-dimensional
square-class input, 126,379 useful primes, the one-unit strict
Golod--Shafarevich inequality, every useful-prime Legendre condition, the
globally sorted depth frontier, exclusion of every fourth-and-later depth,
and both dyadic phase endpoints.  A fresh independent rerun on 2026-08-20
passed with margins `0.03009888659...` and `0.02388656735...`.

This construction family is now essentially optimized: the numerical rank
scan turns at 713, and a rigorous slope bound proves that increasing the rank
eventually drives the exponent back toward `1/2`.  A meaningful further
upper-bound improvement therefore needs a better residue-degree mechanism
or a different construction, not merely a larger tame presentation.

The same checkpoint records four structural results relevant to a full
solution:

* minimum-colour proper edge colourings of `K_n`, whenever realized exactly
  by planar distances, force a concyclic configuration and hence retain a
  distance-Sidon subset of order `sqrt(n)`; the one-factorization
  anti-Ramsey route cannot reach the cube-root target;
* pair-sum-separated hierarchical planar embeddings do not tensor local
  collisions: equality fixes one common orthogonal map across all digit
  levels, and scalar digit products contain an explicit square-root Sidon
  subcode.  Deliberately collapsing to consecutive scales also fails for
  every fixed Gaussian digit alphabet with additive energy below
  `|P|^(5/2)`: polynomial norm fibres are subexponential and random
  alteration extracts `n^(1/2-o(1))` distance-Sidon points.  This includes
  the smallest noncollinear alphabet `{0,1,i}`;
* for an exact zero-sum difference triangle, the common content `t` obeys
  `t^2 | D`, and the high-content tail is `O(m^4/T^3)`; only the low-content,
  especially primitive `t=1`, global packing branch survives.  In that
  branch the complete normalized Gaussian cell `(t,a,x)` has only
  `m^o(1)` representations, and every cell with `|ax| <= m^(4/3)` is paid
  by `m^(2+o(1))`.  The precise survivor is low `t` with jointly large
  normalized area and dot product;
* exhaustive separation of the Welch configuration at `p=263` shows that
  the minimum determinant-`263` transform height is exactly 67.  This kills
  the naive finite extrapolation `<=4 sqrt(p)` but leaves the asymptotic
  balanced-separator question open.

Read `PROPER_COLORING_EUCLIDEAN_REALIZATION_BARRIER.md`,
`HIERARCHICAL_PLANAR_GRAM_RANK_NO_GO.md`,
`GAUSSIAN_POLYNOMIAL_NORM_FIBRE_SQRT_EXTRACTION.md`,
`EXACT_CLOSED_METRIC_CONTENT_PRIMITIVE_AREA_GATE.md`, and
`LOW_COMMON_SCALE_NORMALIZED_GAUSSIAN_PRODUCT_GATE.md`, and
`BALANCED_COSTAS_SEPARATOR_EXACT_CUTOFF.md`.  Occurrences below of
`0.49815` and `0.494586` are historical unless explicitly discussing those
older certificates.

## 2026-08-20 route reset — the directional and relaxed ambient gates are retired

The problem remains open.  The latest adversarial pass invalidated the
two strongest sufficient shortcuts from the checkpoint immediately below.

First, the global directional midpoint estimate

\[
 \sum_wH_w\stackrel?\le m^{o(1)}
 \left(k\sum_we_w+\sum_wM_w^2\right)
\]

is false.  A balanced transform

\[
 L_t=\begin{pmatrix}t&-1\\1&t+1\end{pmatrix}
\]

of the modular parabola has `sum H_w = Omega(k^4)` but directional
budget `O(k^3)`.  A dimension-one lower-bound sieve chooses a polynomial-
size parameter `t` for which `t^2+t+1` has no prime factor below `4k^2`;
the identity

\[
 \gcd(L_tw)\mid\gcd(t^2+t+1,a^2+ab+b^2)
\]

then makes every active transformed direction primitive simultaneously.
After excluding the `O(k^4)` distance-collision roots, this gives genuine
Euclidean distance-Sidon sets of height `O(k^17)` whose ratio is
`Omega(k)=Omega(m^(1/17))`.  Hence even an arbitrary `m^o(1)` loss cannot
save the directional gate.  Read
`POLYNOMIAL_HEIGHT_BALANCED_SIEVE_GLOBAL_DIRECTIONAL_NO_GO.md`.

Second, the relaxed closed-fibre quantities are structurally unsafe.
The dyadic `Q` functional is affine quasi-invariant within a factor two,
and flat projective energy `P` is exactly affine invariant.  Every dense
vector-Sidon array in an `O(k)` box has

\[
 Q,\ P,\ \text{and clean centroid mass }\Omega(k^4).
\]

Balanced `O(sqrt(k))` distance-separating transforms of Welch--Costas
arrays have been verified for every tested prime through 251; if such
transforms exist asymptotically, they refute the proposed `Q`, `P`, and
ambient clean-centroid upper gates at critical height by a full power.
That separator theorem is not proved, so this is a conditional barrier,
not a counterexample.  Lefmann--Thiele's known `Omega(m^(2/3))` grid
construction is sparse and does not itself provide the separator.  Read
`CLOSED_FIBRE_Q_AFFINE_DENSE_COSTAS_BARRIER.md`.

The flat energy has also been identified exactly: it counts endpoint
relations

\[
 g(b+c-a-d)=h(f-e)
\]

with arbitrary rational dilation `h/g`; exact centroid closure is only
the special case `|h|=g`.  Flat `P_w` can exceed content-aware `Q_w` by a
factor `Theta(k)` on genuine examples.  Read
`GLOBAL_DIRECTIONAL_PAIR_ENERGY_PROJECTIVE_DILATION_AUDIT.md`.

**Current strategic verdict.**  Do not use pointwise directions, shortest-
direction assignment, global short-direction compensation, flat `P`, or
dyadic-minimum `Q` as the final upper-bound mechanism.  The square-grid
cube-root route has reached a genuine reset: it needs an upper argument
that retains exact metric contents and the full ambient budget, or a new
construction route for the upper bound on `F_2(n)`.  The unconditional
headline remains

\[
 n^{1/3}\ll F_2(n)\ll n^{0.49458539},
\]

with the upper exponent supplied by the independently verified rank-713
quadratic-Frobenius/all-depth certificate.  Closing the gap is still a major
new lemma, not a nearly finished proof.

## 2026-08-20 latest checkpoint — the remaining global packing problem

The problem is still open, but the current proof frontier is much narrower
than the older scalar-charge material later in this file.

For the closed determinant-fibre relaxation,

\[
 \mathcal Q_D(A)=\sum_{w,r}
 \min\left\{\binom{B_w(r)}2,\alpha_{w,D}(r)B_w(r)\right\},
\]

we now have the exact universal estimate

\[
 \sum_D\mathcal Q_D(A)\le \frac{N^2}{2},\qquad N=k(k-1).
\]

Thus the desired gate is proved when `m >= N`, i.e. roughly `m >= k^2`.
The Euclidean finite-field parabola has `sum_D Q_D = Theta(m^2)`, so the
ambient term is sharp.  A critical-height common-endpoint vector-Sidon
layered model has `Q = Omega(k^(7/2))` at `m=O(k^(3/2))`, proving that
endpoint realization, vector uniqueness, and pointwise fibre caps cannot
finish the argument.  It fails exactly Euclidean radial uniqueness;
Euclideanizing it raises the height to `O(k^2)`, where `m^2` pays.  The
remaining theorem must globally pack different direction/residue fibres
using radial uniqueness and height.  A clean sufficient form is

\[
 \sum_{w,r\ne0}\binom{B_w(r)}2
 \le m^{o(1)}(k^3+m^2).
\]

Read `CLOSED_FIBRE_Q_HEIGHT_LAYERED_BARRIER.md`.  The corrected `p=43`
total is `988328`; the former verifier stopped before a final relaxation-
only band of mass eight.  Both the old verifier and prose have been fixed.

There is also an exact midpoint version.  If `T_w` is the set of realized
positive contents in primitive direction `w`, `e_w=|T_w|`,
`M_w=m/||w||_infinity`, and `L(q)` counts clean segments of the unique
pair-sum set with displacement `q`, then

\[
 H_w=4\sum_{g\in T_w}L(gw),\qquad
 \sum_wH_w=3|\mathcal H_A|.
\]

The pointwise estimate `H_w << k e_w + M_w^2` is false by a full factor
`Omega(k)` on genuine polynomial-height sheared modular parabolas.  The
global cross-direction estimate

\[
 \sum_wH_w\stackrel?\ll m^{o(1)}
 \left(k\sum_we_w+\sum_wM_w^2\right)
\]

remains live and would prove the conjectured cube-root upper bound because
`sum e_w = binom(k,2)` and `sum M_w^2 = O(m^2 log m)`.  Its proof must
transfer charge between directions; the modular parabola's excessive long-
direction mass is globally paid by a short horizontal direction.  Read
`DIRECTIONAL_MIDPOINT_POINTWISE_NO_GO_GLOBAL_GATE.md`.

**Immediate next action:** prove or kill the global cross-direction gate,
or equivalently the radial/height packing inequality above.  Do not return
to pointwise direction estimates, fixed scalar charges, Gaussian-cell load
bounds, or cutoff-decaying determinant tails: each has a verified
counterexample elsewhere in this directory.

## 2026-08-20 large-determinant checkpoint — exact closed fibres, Gaussian cells, and the endpoint cocycle

The problem is **not solved**.  The ambient reduction from the preceding
checkpoint has now been sharpened in three mutually consistent ways.  They
all locate the same survivor: large-determinant zero-sum triples in the
complete endpoint difference set, with ambient height and the endpoint
cocycle retained simultaneously.

### A. Exact closed determinant fibres

For a primitive active direction `w`, choose `v_w` with
`det(w,v_w)=1` and write

\[
 S_{w,r}=\{n:nw+rv_w\in(A-A)\setminus\{0\}\}.
\]

If the distinguished edge is `gw`, then the forced third difference is
present exactly when `n,n+g` lie in the same fibre `S_{w,r}`, and the
determinant is exactly `gr`.  Consequently

\[
 3|\mathcal H_A[D,2D)|
 =\sum_{w,r,g}C^{\rm mat}_{w,r}(g),
\]

where `C^mat` retains the six-endpoint matching condition.  Dropping only
that cleanliness gives the rigorous gate

\[
 3H_D\le \mathcal K_D\le\mathcal Q_D
 :=\sum_{w,r}\min\!\left\{\binom{B_w(r)}2,
                    \alpha_{w,D}(r)B_w(r)\right\}.
\]

Abstract summation proves

\[
 H_D\le {1\over3}\min\left\{\binom{k(k-1)}2,
                    \sqrt{6D}[k(k-1)]^{3/2}\right\}.
\]

This closes fixed and subpolynomial determinant bands and adds a `k^4`
high-band cap, but not the hard range.  On the `p=43` Euclidean stress,
closure loses only a factor `1.19--1.39` in the main bands, while the full
relaxation still satisfies `sum_D Q_D=988328<1175^2`.  Thus

\[
 \sum_D\mathcal Q_D\le m^{o(1)}(k^3+m^2)
\]

is the sharpest current sufficient gate.  Read
`LARGE_DETERMINANT_CLOSED_FIBRE_ENERGY_GATE.md` and
`LARGE_DETERMINANT_PROJECTION_DYADIC_GATE.md`.

### B. Gaussian cell rigidity is exact but support-equivalent

For `q_1+q_2+q_3=0`,

\[
 z=q_1\overline{(q_2-q_3)}
  =( |q_3|^2-|q_2|^2)-2i\det(q_1,q_2).
\]

Fixing `z` gives only `m^{o(1)}` endpoint realizations by Gaussian divisor
factorisation.  However reversal preserves `z` and is fixed-point-free, so
every occupied cell already has load at least two.  Therefore bounding the
number of occupied cells is, up to divisor factors, equivalent to the full
ambient hypergraph theorem; fixed-cell rigidity supplies no hidden averaging.
The explicit `p=43` stress has 761112 ordered records on 375096 cells, with
369934 cells of the forced minimum load two.  Moreover an explicit
`O(p^2)`-height Euclidean lift retains `Omega(p^4)=Omega(m^2)` mass after
simultaneously imposing polynomially large norm-gap and determinant cutoffs.
All cutoff-decaying support conjectures are therefore false.  Read
`GAUSSIAN_JOINT_CELL_DIVISOR_SUPPORT_EQUIVALENCE.md` and
`LARGE_GAUSSIAN_CELL_SUPPORT_TAIL_AUDIT.md`.

### C. Harmonic and cocycle formulations

With `S=|hat A|^2-k=hat(1_{D*})`, the whole third-energy problem localises,
up to less than `k^3`, to the large positive spectrum:

\[
 \left|T(D^*)-\int_{S>k}S^3\right|<k^3.
\]

On a finite torus `Q>3m` this gives an exact dyadic large-spectrum gate.
Ordinary Freiman rectification cannot close it: an encoded finite-field
parabola is a one-dimensional Sidon set in an interval of length `O(k^2)`
but has third energy `Omega(k^4)`.  Euclidean radial uniqueness and height
must remain coupled.  Read `LARGE_POSITIVE_SPECTRUM_RECTIFICATION_GATE.md`.

Endpoint cleanliness itself is no longer an issue:

\[
 0\le T(D^*)-C_6(A)\le15k^3.
\]

But radial uniqueness, reversal symmetry, and even formal complete endpoint
labels are insufficient.  A verified radial-transversal model has 83496
zero-sum triples against target 51614, while only six triples obey a genuine
endpoint cocycle.  The load-bearing condition is exactly

\[
 (a-b)+(b-c)=a-c
\]

for one common geometric endpoint realization.  Read
`RADIAL_SCHUR_TRIPLE_ENDPOINT_COCYCLE_BARRIER.md`.

### D. Immediate next action

Attack the closed-fibre gate `sum_D Q_D` with ambient height while retaining
the endpoint cocycle.  The harmonic large-positive-spectrum formulation is
an equivalent cross-check, not a separate shortcut.  Do not return to fixed
scalar charges, radial-only bounds, Freiman rectification, Gaussian cell
multiplicity, or polynomial determinant/norm-gap tails: each now has a
genuine verifier-backed counterexample.

## 2026-08-20 newest checkpoint — derivative amplification is dead; the ambient large-determinant hypergraph is the live frontier

The problem is **not solved**.  The last few hours decisively closed the
remaining colour-sensitive derivative-energy shortcut and produced a much
cleaner direct reformulation.  The surviving task is now one explicit
large-determinant endpoint-hypergraph estimate.

### A. The exact coloured derivative identity is sound, but its needed bound is false

After retaining the affine/component colour, the derivative mass has the
exact form

\[
 W_L=\sum_{h,\theta,B} C_h(\theta,B)
       {K_h(\theta,B)\choose3}
 \le \sum_h\sqrt{\mathfrak A_h\mathfrak B_h}.
\]

This identity is sharp on both the integer parabola and disjoint quadratic
arcs.  However, the marginal parent-energy estimate suggested by this
Cauchy--Schwarz step is false.  There is a one-active-colour polynomial-
height distance-Sidon family with disjoint parent supports and `J` comparable
to `k`, for which the parent energy is one although the proposed bound tends
to zero.

More decisively, plant
`S_L={(r,r^2): 0<=r<2L}` in a box of side `M=64L^2` and extend it by a
high-strip random alteration to an exponent-critical distance-Sidon set of
size `k=M^(2/3-o(1))`.  The planted supports survive and have

\[
 \mathfrak A_{L,L}=\Theta(M^{3/2})
 \quad\hbox{against a proposed }M^{1/6+o(1)}\hbox{ bound},
\]

and the amplified weighted mass is `Theta(M^(5/2))`, above its proposed
`M^(2+o(1))` target.  The actual unweighted derivative triples are only
`Theta(M^2)`, so this is a counterexample to the amplification, **not** to
the desired ambient theorem or to #1208.

Read `COLORED_DERIVATIVE_L2_CORRELATION_GATE.md`,
`SINGLE_ACTIVE_COLOR_PARENT_ENERGY_GATE_BARRIER.md`, and
`CRITICAL_PLANTED_PARABOLA_COLORED_ENERGY_COUNTEREXAMPLE.md`, with their
verifiers.

### B. Exact ambient reformulation

For a distance-Sidon set `A subset [m]^2`, form a 3-uniform hypergraph whose
vertices are directed endpoint edges `(a,b)`.  A hyperedge is a triple of
directed edges with six distinct endpoints and displacement vectors summing
to zero.  Then:

- this hypergraph is linear: every pair of vertices lies in at most one
  hyperedge;
- the six-distinct centroid count is exactly six times its number of
  hyperedges;
- all nonmatching zero-sum triples cost only `O(k^3)`.

Therefore the ambient theorem that would finish #1208 is exactly

\[
 \boxed{|\mathcal H_A|\le m^{o(1)}(k^3+m^2).}
\]

Two substantial branches are now proved:

\[
 |\mathcal H_A^{\rm col}|
   \le {8\over3}(m-1)^2 H_{m-1},
\]

and, for every determinant cutoff `D`,

\[
 |\{e\in\mathcal H_A:0<|\det(e)|\le D\}|
   \ll D(k^2+mk).
\]

Thus the collinear and every subpolynomial-determinant band already satisfy
the desired estimate.  The sole survivor is the **large-determinant matching
hypergraph**.

The sheared finite-field parabola at `p=43` verifies that this tail is real:
it has 126,852 matching hyperedges, only 390 collinear and 5,278 with
determinant at most 10.  Its height `m=1175` makes the `m^2` allowance large
enough, so it is an equality model rather than a counterexample.

Read `AMBIENT_CENTROID_ENDPOINT_DIFFERENCE_HYPERGRAPH_GATE.md` and its
verifier.

### C. Exact current frontier and honest distance

All fixed scalar charges, Gaussian/support-overlap variants, endpoint-wedge
amplifications, uncoloured derivative bounds, and now the colour-sensitive
Cauchy amplification have either a proof in their valid range or a genuine
distance-Sidon counterexample.  This is substantial de-risking: we no longer
have a cloud of plausible-but-false endgames.

What remains is not a routine final estimate.  A full solution requires one
new endpoint-sensitive aggregate theorem controlling the large-determinant
part of `H_A` at `m^{o(1)}(k^3+m^2)` scale.  The natural Gaussian joint cell

\[
 q_1\overline{(q_2-q_3)}
   = (|q_3|^2-|q_2|^2) \mathbin{\pm} 2i\det(q_1,q_2)
\]

has divisor-small multiplicity, but its two-dimensional support is too large
without a size-biased endpoint argument.  This is the immediate research
target.

Honest assessment: the search/exclusion map is advanced, but the proof is
roughly **30--40% complete**, not 90%.  We have isolated the correct-looking
last battlefield; we have not yet proved the theorem fought there.

## 2026-08-20 decisive stocktake — all fixed scalar charges close; two global routes survive

The problem is **not solved**.  This checkpoint closes the full fixed-
coefficient scalar-charge program, corrects the derivative route one level
further, and identifies the ambient theorem that would actually finish
#1208.

### A. Every fixed positive scalar coefficient fails

For any fixed positive integer `C`, the proposed charge

\[
 \Phi_{q,C}(s,t)=\delta(s)+C\delta(t)
\]

cannot satisfy the aggregate estimate needed for the cube-root theorem.
Take a dense `k`-mark collinear Golomb ruler in an interval of length
`O(k^2)`.  Its clean-fibre mass satisfies

\[
 \sum_q h_q=\Omega(k^4),\qquad
 \sum_q h_q^2=\Omega(k^6).
\]

Bernays' value-count theorem for the positive binary quadratic form
`x^2+C y^2` gives only `O_C(k^4/sqrt(log k))` possible charge values.
Fibrewise Cauchy and summation therefore force

\[
 \sum_q(\mathcal M_{q,C}-Nh_q)
   =\Omega_C(k^6\sqrt{\log k}),
\]

whereas the desired right side is only `m^{o(1)}Nk^3=k^{5+o(1)}`.
This includes the attempted inert-prime repair `C=3`.  Exact `C=3`
Ruzsa-ruler ratios already grow from `0.8994` at prime 31 to `3.5132` at
prime 83.

The earlier coefficient-18 failure has a complementary geometric model:
a horizontal copy with norms `36 ell^2` and a translated diagonal copy with
norms `2 ell^2` create an exact dilated swap and an `Omega(k^6)` aggregate.
The 60-point certificate has all 1770 distances and pair sums distinct and
8,150,624 displayed swaps.

Read `RAW_SCALAR_ALL_FIXED_COEFFICIENTS_GOLOMB_COUNTEREXAMPLE.md` and
`RAW_SCALAR_DILATED_SWAP_GOLOMB_COUNTEREXAMPLE.md`, with their verifiers.
All fixed-`C` scalar, adaptive-mod-36, and endpoint-wedge variants are now
barriers/diagnostics, not live closing programs.

### B. The derivative route must retain component colour

For `b` disjoint quadratic arcs, the true child-parent correlation is
`Theta(bL^5)`, while the uncoloured corrected minimum gate is
`Theta(b^3L^5)`.  A 24-point exact distance-Sidon certificate has true mass
96 and uncoloured envelope 1536.  Hence the normalized centre-difference
colour `x=(h,A_c-A_d)`, together with the affine block colour `(theta,eta)`,
is load-bearing.

The exact surviving mass is a colourwise dot product

\[
 W_L=\sum_{\theta,\eta}\sum_{x\ne0}
 r^+_{P_{\theta,\eta}-P_{\theta,\eta}}(x)
 {r_{B_\theta-B_\theta}(x)\choose3}.
\]

Colourwise Cauchy is sharp on both a single parabola and disjoint arcs.  The
remaining theorem is a joint colour-energy bound correlating the parent
quotient second moment with the child's sixth line-occupancy moment, at
scale

\[
 m^{o(1)}{(k^3+m^2)^2\over k^3}.
\]

No target-scale distance-Sidon counterexample is known, but the required
moment bound is not proved.  Read `MULTI_ARC_COMPONENT_COLOR_MIN_GATE_BARRIER.md`
and its verifier.

### C. The cleanest direct finish is now ambient

For a distance-Sidon `A subset [m]^2`, it is enough to prove the six-distinct
equal-centroid/equal-area estimate

\[
 C_6(A)\le m^{o(1)}(k^3+m^2).
\]

Together with triple-sum Cauchy this gives `k<=m^{2/3+o(1)}`; applying it to
the `m` by `m` grid and combining with the known universal lower bound would
resolve #1208 up to subpolynomial factors.

Proved branches include all equal-area records with any cross-parallel side,
the large-height range `m>=k^{5/2}`, nonparabolic fixed-product Pell fibres,
and the few-fibre parabolic/Singer cells.  The unresolved core is the
fully-transverse, graph-like rich derivative-line/cocycle tail.  Equivalently,
centroid collisions form matching hyperedges among zero-sum triples in the
endpoint-labelled difference set, and the missing statement is an
endpoint-sensitive bound on the non-transitive part.  This is one major new
aggregate lemma, not a routine cleanup.

### D. Honest distance from a proof

The exploration and falsification phase is advanced: the principal local,
Fourier, scalar, amplified-endpoint, and uncoloured-correlation shortcuts now
have proofs or explicit counterexamples.  The final theorem is not in hand,
and no chain of standard results currently implies it.  The credible live
program is:

1. prove the colour-sensitive derivative moment bound above, or
2. prove the ambient six-distinct equal-centroid/equal-area bound directly.

The explicit upper construction `F_2(n)<<n^0.494586` and the prime-power
sieve remain separate candidate results; they improve the quantitative
upper side but do not close the `1/3` versus `0.494586` exponent gap.

## 2026-08-20 latest stocktake — amplified endpoints are exhausted; two exact global gates remain

The problem is **not solved**.  The last few hours produced three sharp
corrections and one positive global reduction.  Together they retire the
remaining local endpoint-amplification program and leave two honest global
objects: the original raw scalar aggregate and a corrected one-dimensional
derivative correlation.

### A. Affine offsets close locally; the exact derivative gate keeps both parent counts

For every coherent affine block `lambda_q=theta q+eta`, set

\[
 B=\{(r,F(r))\},\qquad C_\eta=\{(r,F(r)+\eta r)\}.
\]

The parent parameter is an `L`-popular difference of `B-C_eta`, and the
cross energy is at most the ordinary energy of `B`.  Thus the coherent-block
height theorem previously proved for `eta=0` extends unchanged to every
offset.  A canonical pair partition also prevents double counting between
affine blocks.

The next tempting envelope, based only on `Q_R(h)T(h)`, is nevertheless
false by a factor `sqrt(k)` on the genuine integer parabola at
`L=sqrt(k)`.  The exact repaired theorem is

\[
 \boxed{
 W_L\le \sum_{h\ne0}
 \min\!\left\{M_L(h),{Q_R(h)\over(L-1)^2}\right\}T(h)}.
\]

Here `M_L(h)` is the actual autocorrelation of the number of parent patches,
`Q_R(h)` is the ambient endpoint-pair envelope, and `T(h)` is the child
triple mass.  The minimum is sharp on the parabola.  The residual problem is
to show that all three quantities cannot align too often unless the
quadratic-height budget pays for them.

Read `AFFINE_OFFSET_TWO_LAYER_POPULARITY_GLOBAL_PARTITION.md` and
`ONE_DIMENSIONAL_MIN_WEIGHT_GATE_PARABOLA_BARRIER.md`, with their verifiers.

### B. Every endpoint-wedge amplification of the high scalar band is now closed

A genuine polynomial-height parabolic rectangle gives two synchronized
`K_{L,L}` edge graphs at one scalar.  Coupled to the dense Golomb clean core,
it has

\[
 c(p)=\Theta(k^2),\quad O(p)=\Theta(k^3),\quad
 U=\Theta(k^2),\quad W_r=\Theta(k^3).
\]

Consequently even the direct outer-normalized endpoint-wedge mass is
`Theta(k^6)`, a factor `k` above its proposed `Nk^3=Theta(k^5)` bound.  The
arbitrary-size construction is justified by an explicit independent-
parameter support lemma and a checked role table; the finite verifier has
`k=92`, `c=320`, `O=6169`, `U=64`, and `W=448` in each orientation.

This does **not** refute the original scalar aggregate: the same family has
raw selected charge only `Theta(k^4)`, below `Nk^3=Theta(k^5)`.  The correct
high-band restart is therefore the unamplified master target

\[
 \boxed{\sum_q \mathcal X_q\le m^{o(1)}Nk^3.}
\]

Read `OUTER_NORMALIZED_PARABOLIC_RECTANGLE_COUNTEREXAMPLE.md` and its
verifier.

### C. The adaptive mod-36 branch is sharp and can be completely collision-free

There are genuine polynomial-height distance-Sidon families with
`R=Theta(k)` selected isolated occurrences in one literal `q`, one metric
gap, and one mod-36 source-area class, with target load `U_N(r)=k` and unit
area-cell multiplicity, but whose normalized Gaussian supports are pairwise
disjoint.  The actual adaptive mass satisfies

\[
 4k^2\le H_Q<4k^2+N,qquad X_{36}>H_Q/200.
\]

All centroid companion sets remain singleton sunflowers.  Hence
`X_36<=m^{o(1)}H_Q` remains plausible and is sharp in scale, but no proof can
come from support intersections or Gaussian collision energy.  The disjoint
branch must be charged directly and globally.

There is now an exact direct packing theorem for that branch.  If
`mu_max` is the maximum number of tail translations reusing one source pair,
and `Delta` is the maximum global depth of the full residual supports, then

\[
 \boxed{X_{36}\le153\,m^{o(1)}m^2\mu_{\max}\Delta.}
\]

Thus globally disjoint supports (`Delta=1`) are paid whenever same-source
translation reuse is subpolynomial in the dense centroid regime.  A genuine
multi-`q` equality model shows that the `H_Q` scale is still sharp with
`mu_max=Delta=1`; the precise residual is therefore high same-source reuse,
not disjointness itself.

Read `ADAPTIVE_MOD36_GLOBAL_DISJOINT_TRANSLATE_SATURATION.md`,
`ADAPTIVE_MOD36_DISJOINT_UNION_PACKING.md`, and their verifiers.

### D. Immediate proof frontier

1. Attack the original raw scalar aggregate directly, without endpoint-wedge
   amplification.
2. Prove a size-biased estimate for the corrected derivative sum involving
   `min(M_L,Q_R/(L-1)^2)T`.
3. Control the same-source translation-reuse factor `mu_max` in the
   adaptive mod-36 packing theorem (or charge its high-reuse branch through
   common-`q` endpoint/centroid structure).
4. Treat the integer parabola and parabolic rectangle as equality models:
   any proposed estimate that loses a factor of `k` on either should be
   rejected before further development.

## 2026-08-20 newest checkpoint — local endgames close; only global normalized masses remain

The problem is **not solved**.  This checkpoint proves substantive local
theorems in all three live lanes, while genuine polynomial-height equality
models remove two more amplified shortcuts.  The surviving statements are
now necessarily global and fully normalized.

### A. Coherent derivative blocks are height-controlled

For an affine parameter block `lambda_q=theta q+eta`, quadratic
normalization gives `B_theta={(r,f(r)-theta r^2/2)}`.  If `R` is the largest
normalized parameter-difference multiplicity and

\[
 H=\left\lfloor{2(m-1)\over\|w\|_\infty|\theta|}\right\rfloor,
 \qquad D(H)=\sum_{n\le H}\left\lfloor{H\over n}\right\rfloor,
\]

then

\[
 E_+(B_\theta)\le2k^2+4kD(H),\qquad
 W\le {Rk\over6}\bigl(2k^2+4kD(H)\bigr).
\]

For a through-origin block whose parents are `L`-popular differences, the
stronger bound

\[
 \boxed{W\le {4\over3}{k^3D(H)(k+2D(H))\over L^2}}
\]

holds.  It pays the entire integer-parabola equality model at the sharp
corrected scale, up to subpolynomial logarithms.  The remaining derivative
problem is global: sum overlapping affine blocks, particularly
`eta!=0`, without repeatedly charging the same endpoint structure.

Read `SUPPORT_WEIGHTED_HEISENBERG_PARABOLA_BARRIER.md` and its verifier.

### B. The high-codegree route must remain outer-normalized

A `Theta(k)`-arm determinant-qualified metric star can have
`W_r=Theta(k^2)` at the scalar exposed by the dense Golomb clean core.  It
gives

\[
 c(p)=\Theta(k^2),\quad O(p)=\Theta(k^3),\quad
 T(C)=\Theta(k^2),\quad W_r=\Theta(k^2).
\]

Consequently the pre-normalized/once-amplified mass is `Theta(k^7)`, which
disproves the proposed `Nk^4=Theta(k^6)` target.  The direct outer-normalized
mass

\[
 \sum_C{T(C)\over c(p_C)}W_{r(p_C),L}
\]

is `Theta(k^5)=Theta(Nk^3)` and survives sharply.  A terminal `ell`-pool
must therefore carry `c(p)^(-ell)`, not `c(p)^(1-ell)`.

There is also a genuine positive theorem.  If `L` fixed-gap arm types are
paired through the same endpoint reservoir of size at least two, then

\[
 \boxed{L\le\tau(|r|)=m^{o(1)}.}
\]

Thus every complete shared-endpoint bipartite scalar block has wedge mass
at most `m^{o(1)}k^2`.  The only survivor is an irregular partner graph
which avoids all large synchronized common-endpoint blocks.

Read `ACTUAL_CODEGREE_NORMALIZED_LARGE_STAR_BARRIER.md`,
`ACTUAL_CODEGREE_NORMALIZED_THREE_TRANSLATION_SWITCH.md`, and
`SHARED_ENDPOINT_BIPARTITE_SCALAR_DIVISOR_NO_GO.md`, with their verifiers.

### C. Adaptive top--tail tails admit a mod-36 refinement, but not local overlap

Both source and target doubled areas are even, so the Gaussian residual
`G=a+18d` satisfies `G congruent a (mod 36)`.  Partitioning each fibre into
the eighteen possible even source-area residues and deleting the first
`b_q` loads in each class costs fewer than `36k^2` occurrences.  Every
retained tail occurrence then has congruence-compatible top witnesses, and
the shifted residual-support intersection is an exact full Gaussian
collision.

This does not finish the lane.  A verified 176-point integral
distance-Sidon set has a same-`q`, same-gap, same-residue pair of target load
`k` whose normalized residual supports are disjoint.  Hence forced local
support intersection is false even after the congruence refinement.  The
remaining theorem must bound the global shifted-support incidence mass,
including disjoint translates, while retaining the centroid companion sets.

Read `ADAPTIVE_QUOTA_CROSS_RESIDUAL_MOD36_GATE.md` and its verifier.

### D. Immediate actions

1. Prove a global bounded-overlap/decomposition theorem for coherent affine
   parameter blocks, including nonzero offsets.
2. Bound the fully outer-normalized `1/c(p)` scalar mass for irregular
   partner graphs, using the shared-endpoint divisor theorem as the dense
   branch.
3. Bound the mod-36 adaptive shifted-support incidence mass with literal
   common translations and centroid data.
4. Continue constructing polynomial-height equality models before
   promoting any intermediate amplification to a closing theorem.

## 2026-08-20 newest checkpoint — sharp normalization replaces two false gates

The problem is **not solved**.  Two verifier-backed corrections replace the
last checkpoint's most tempting global estimates with sharp normalized
targets.  Both corrections come with genuine polynomial-height
distance-Sidon equality models, so the removed losses cannot be repaired by
better bookkeeping.

### A. Derivative/Heisenberg route: the record-scale weighted bound is false

For the integer parabola

\[
 A_L=\{(r,r^2):0\le r<2L\},
\]

a new Pell-gap argument proves distance-Sidonicity for every `L`.  In the
derivative band `1<=q<=L`, the record count is `Theta(L^4)` but the exact
support-weighted Heisenberg mass is

\[
 W_L=\sum_{h=1}^{L-1}(L-h){2L-h\choose3}
 \sim {49\over120}L^5.
\]

Since `k=2L` and `m=Theta(L^2)`, this disproves
`W\lesssim m^{o(1)}(k^3+m^2)` by a factor `Theta(k)`.  The correct sharp
sufficient target is

\[
 \boxed{W\lesssim m^{o(1)}{(k^3+m^2)^2\over k^3}.}
\]

The parabola attains this scale up to a constant.  More generally, on an
affine slope block `lambda_q=theta q+eta`, subtracting the coherent quadratic
term converts every child occupancy into an ordinary difference
multiplicity of

\[
 B_\theta=\{(r,f(r)-\theta r^2/2)\}.
\]

Thus the remaining theorem is an additive popular-difference dichotomy:
coherent quadratic blocks must be paid by `m^2`, while distributed blocks
must be forced toward the `k^3` scale.

Read `SUPPORT_WEIGHTED_HEISENBERG_PARABOLA_BARRIER.md` and its verifier.

### B. High scalar route: actual-codegree normalization repairs false pooling

For a source pair `p` with common-clean codegree `c(p)`, insert the exact
fractional partition

\[
 \sum_{q_0\in Q_p}{1\over c(p)}=1.
\]

This gives a lossless decomposition of the high-codegree one-role mass into
one fully transverse and fifteen canonically disjoint local channels, with
the literal third translation outermost and weight `1/c(p)` retained.  The
correct normalized `ell`-pool is

\[
 \boxed{
 \mathfrak P_\ell^{\rm norm}
 =\sum_C c(p_C)^{1-\ell}{T(C)\choose\ell}w(C),}
\]

which is constant-equivalent to the once-amplified mass and always targets
`Nk^4`.  It avoids the artificial powers disproved by the Golomb
multi-wedge construction.  The 102-point verifier checks the exact
partition of all `6169*320` base/third-translation records and the normalized
mass identities.

The exact remaining high-band gate is an outer-`q_0` endpoint/scalar bound
that simultaneously retains `1/c(p)`, the one-role base, and the physical
metric weight `W_(r(p),L)`.

Read `ACTUAL_CODEGREE_NORMALIZED_THREE_TRANSLATION_SWITCH.md` and its
verifier.

### C. Live endgames

1. Prove the sharp normalized Heisenberg bound above by splitting coherent
   quadratic blocks from distributed short-path blocks, or refute it.
2. Bound the exact adaptive top-vs-tail metric functional, retaining the
   common translation, centroid singleton intersections, and signed-area
   support.
3. Bound the actual-codegree-normalized outer-translation scalar mass at
   the `Nk^4` amplified scale, or construct a genuine counterexample.
4. Continue verifier-backed adversarial tests before promoting any gate to
   a proof of #1208.

## 2026-08-20 newest checkpoint — pooling is false; derivative and adaptive gates sharpen

The problem is **not solved**.  Three verifier-backed results materially
change the current architecture.  The derivative route closes a new
high-richness range; the adaptive route acquires an exact top--tail
functional; and a polynomial-height construction disproves every fixed
synchronized-pooling target.  The original scalar aggregate is not
contradicted.

### A. Derivative route: reverse-slope packing closes `L \gtrsim sqrt(mk)`

For a dyadic family of derivative lines with
`L<=|S_ell|<2L`, let `rho_L` be the maximum number of parent-line pairs
producing one child record with its shift, slope, intercept, and tail triple
all fixed.  Arithmetic slope counting and same-slope tail-pair packing give

\[
 \boxed{
 \rho_L\le
 \min\left\{k,
 {8(m-1)^2k(k-1)\over L(L-1)^3}\right\}.}
\]

Together with the exact parent-overlap inequality, this closes every
dominant band with `L\gtrsim sqrt(mk)` up to subpolynomial factors.  Combining
it with `T_L\ll m^2k^2/L` shrinks the `k^3`-dominated residual to

\[
 \boxed{
 \sqrt{k}<L<\min\{m^2/k,\sqrt{mk}\}\,m^{o(1)}.}
\]

The patch group is exactly the three-dimensional Heisenberg group.  The
Murphy--Wheeler energy theorem has a lossless characteristic-zero reduction
and applies with explicit center and abelian-coset loads, but ordinary group
energy does not control the necessary support-weighted correlation

\[
 \sum_g r_{\Gamma_L\Gamma_L^{-1}}(g){K_g\choose3}.
\]

Thus the precise next input is a support-sensitive Heisenberg incidence
bound.  A 219-point integral distance-Sidon stress shows that several reverse
preimages may live on isolated parameter edges, so local path extraction is
not enough.

Read `DYADIC_REVERSE_SLOPE_PACKING_HEISENBERG_AUDIT.md` and its verifier.

### B. Adaptive low band: exact quota-cross repair

If the selected loads in one fibre are sorted
`U_(q,1)>=...>=U_(q,e_q)` and the quota discards the first `b_q`, then the
tail has the exact representation

\[
 \boxed{
 \sum_{j>b_q}U_{q,j}
 ={1\over b_q}\sum_{i\le b_q<j}
      \min(U_{q,i},U_{q,j}).}
\]

The earlier all-pairs functional unnecessarily included top--top and
tail--tail pairs.  The correct inverse object is one top occurrence and one
tail occurrence sharing the same literal translation.

Clean fibres also have an exact centroid-set representation.  Equal-sum
triple classes are matchings; head-isolation is precisely a singleton
intersection of the two companion sets.  This does not by itself give a
saving: for every `n` there is a polynomial-height integral distance-Sidon
translation chain with `k=2n+4`, `h_q=n`, in which all displayed start pairs
have `q` as an isolated common translation.  Its companion sets form a
sunflower and the singleton-intersection graph is `K_n`.  The construction
does not make all metric gaps rich, so the actual adaptive estimate remains
open; metric loads and top--tail status are indispensable.

Read `ADAPTIVE_CENTROID_SINGLETON_CROSS_PAIR_GATE.md` and its verifier.

### C. High band: every fixed synchronized pooling order is false

The dense Golomb clean core admits not merely one but `Theta(k)` disjoint
determinant-qualified physical wedges at the same scalar shift.  With
`c(p)=Theta(k^2)`, `Theta(k^3)` one-role bases, and transverse pools of size
`Theta(k^2)`, this gives for every fixed `ell>=2`

\[
 \boxed{
 \mathfrak P_\ell(W_{\cdot,N})=\Omega(k^{2\ell+4}),}
\]

whereas the proposed pooled target was
`m^{o(1)}Nk^{ell+3}=k^{ell+5+o(1)}`.  The violation is a factor
`k^{ell-1-o(1)}`.  The construction is an integral polynomial-height
distance-Sidon set; the 102-point verifier checks six wedges at the same
shift and all 5,151 distances and pair sums.

This **does not** disprove the original scalar aggregate

\[
 \sum_q\mathcal X_q\le m^{o(1)}Nk^3.
\]

On the certificate the selected raw distance-gap multiplicities are only
`R_D(r)=R_D(-r)=12`.  The false step is binomial amplification: an
ultra-high-codegree pool is counted many times and only normalized by the
ambient threshold `k`.  The once-amplified mass is exactly at its allowed
scale; every additional fixed pooling order creates an artificial power
loss.  The high route must return to the unpooled/original aggregate or use
normalization by the actual codegree.

Read `SYNCHRONIZED_GLOBAL_MULTI_WEDGE_GOLOMB_COUNTEREXAMPLE.md` and its
verifier.

### D. Immediate next actions

1. Prove a support-weighted Heisenberg point--plane incidence estimate in
   the narrowed derivative window, or build a critical counterexample.
2. Bound the exact adaptive top--tail metric functional, retaining centroid
   singleton intersections and signed-area support.
3. Rebuild the high-codegree scalar proof without fixed-order binomial
   pooling; the Golomb multi-wedge family is the sharp overcount stress.
4. Continue exact verifier and polynomial-height counterexample audits for
   every proposed closing lemma.

## 2026-08-20 latest correction — the pointwise high-band gate is false; the global gate is sharp

The problem is **not solved**.  A genuine polynomial-height construction now
disproves the pointwise fixed-wedge estimate that the preceding checkpoint
listed as the high-band target.  This is a substantive correction, not a
failure of the whole high-band strategy: the example exactly saturates the
still-live global allowance.

### A. Dense Golomb rulers concentrate the entire high-band budget at one wedge

For a dense polynomial-span collinear Golomb ruler, triple-sum Cauchy gives

\[
 H=\sum_q h_q=\Omega(k^4).
\]

The exact second-moment switch

\[
 \sum_{s,t}c(s,t)=\sum_qh_q^2
\]

then forces an off-diagonal source pair `p=(s,t)` with
`c(p)=Omega(k^2)`.  The endpoint-degree inequality and the universal
replacement bound `rho(p)<=k-2` force `Omega(k^3)` one-role bases.  Since a
base excludes at most `15k-36` pool translations, all of these bases are
literally transverse-rich once `k` is large.

A seven-point off-line gadget, after an even scaling of the ruler, exposes
the same pair to one determinant-qualified physical metric wedge at
polynomial height.  Thus for a band `K=Theta(k^2)` one has

\[
 \boxed{\mathcal R_K(w)=\Omega(k^3)},
 \qquad {k^4\over K^2}=O(1).
\]

Consequently the proposed pointwise estimate

\[
 \mathcal R_K(w)\le m^{o(1)}{k^4\over K^2}
\]

is false, even with the exact clean equations, the `Xi_K` extension
predicate, scalar selector, and determinant cutoff retained.  The finite
certificate has

\[
 (k,N,c,K,R,\min T,B_2)
 =(67,2211,320,268,6169,182,139373896),
\]

and the verifier checks every distance, pair sum, clean fibre, transverse
extension, and metric-gadget identity.

Crucially, dyadic reversibility gives concentrated mass

\[
 \Phi_{2,L,K}(w)\asymp K^2\mathcal R_K(w)=\Theta(k^7),
\]

which is exactly the global allowance `Nk^5=Theta(k^7)`.  Therefore the
counterexample **does not refute the global synchronized aggregate**.  It
shows instead that any proof must permit a single physical wedge to consume
essentially the entire budget.  Pointwise reciprocal-tail and uniform-wedge
arguments are retired.

Read `SYNCHRONIZED_FIXED_WEDGE_DYADIC_GOLOMB_COUNTEREXAMPLE.md` and
`verify_synchronized_fixed_wedge_dyadic_golomb_counterexample.py`.

### B. Current three-frontier proof program

1. **Parabolic/equal-area frontier.**  Generic derivative-line mass, all
   richness `L<=sqrt(k)`, few-fibre directions, corresponding-parallel sides,
   and several coherent quadratic cells are paid.  The residual is a family
   of `L>sqrt(k)` derivative patches with polynomial slope entropy and abundant
   three-tail overlaps, recursively closed under the exact child-patch
   operation.  A new common-core theorem closes a substantial part of this
   residual: if patches with shifts `Q` share a tail core `S` of size `L`, and
   `B` is the largest collinear block of shift--slope parameters, then
   `L|Q|<=kB`.  Threatening common cores therefore force one affine parameter
   line, at most `k/L` intercept defects per difference, and exact quadratic
   recurrences along repeated defects.  Arithmetic also gives at most
   `8(m-1)^2/(L-1)^2` slopes at richness `L` and dyadic mass
   `T_L<<m^2k^2/L`.  The survivor is distributed triple overlap with no large
   common core, in `sqrt(k)<L\lesssim m^2/k`, whose reverse multiplicity is
   spread over many short parameter paths.  The needed finish is an
   additive-growth or height-increment theorem for that distributed system.
2. **Low common-translation frontier.**  Adaptive quotas and the exact paired
   functional reduce the problem to global endpoint reuse across translations.
   Pointwise residual-cell compression is sharp and cannot help.  A two-role
   Cartesian reuse pattern is impossible by pair-sum uniqueness, while an
   abstract nongeometric design loses a full factor `k`.  The missing theorem
   must extract approximate separability or charge genuinely nonseparable
   endpoint designs.
3. **High-codegree frontier.**  Work only with the global scalar/synchronized
   aggregate.  The new Golomb example is the equality model: a viable argument
   must pay its one-wedge concentration at the global `Nk^5` scale and gain on
   mass not organized by such a dense collinear clean core.

### C. Immediate next actions

1. Prove or refute recursive child-patch closure in the `L>sqrt(k)` derivative
   system, retaining exact slopes, shifts, and height.
2. Bound the adaptive paired endpoint functional by a separable/nonseparable
   dichotomy.
3. Decompose the global synchronized aggregate around the Golomb equality
   model; do **not** return to a pointwise fixed-wedge bound.
4. Cross-audit any proposed closing lemma against all polynomial-height
   counterexamples and its exact verifier before promoting it to the handoff.

For the new derivative reduction read
`COMMON_CORE_SPECIAL_AFFINE_ENERGY_DICHOTOMY.md` and
`verify_common_core_special_affine_energy_dichotomy.py`.

## 2026-08-20 current checkpoint — curvature closes coherent graph cells; the two aggregate gates are sharp

The problem is **not solved**.  Three new theorem notes and three verifiers
have been independently rerun from the root session and pass.  They narrow
each live endgame and show that its conjectured final scale is sharp.

### A. Many-fibre parabolic directions: derivative incidence plus curvature

In a singleton-fibre direction write

\[
 x_r=r z_w+f(r)w,
 \qquad d_c(r)=f(r+c)-f(r).
\]

Trace-`2` records are exactly nonhorizontal collinear triples in the
derivative graphs `P_c={(r,d_c(r))}`.  Szemerédi--Trotter pays the generic
part at `O(k^3 log k)`; only rich derivative lines remain.  If a rich line
contains a path of `L` shift-`c` edges with slope `lambda!=0`, then

\[
 m-1\ge { |\lambda c|\,\|w\|_\infty\over2}
          \lfloor L/2\rfloor^2.
\]

This closes long paths, whole interval cells, and a genuine matching-heavy
case: a `t`-by-`t` rectangle of individually disjoint derivative matchings
forces an ordinary first derivative to be affine on a length-`2t` interval,
so `f` is quadratic there and its `Theta(t^4)` records are `O(m^2)`.

The exact residual is irregular partial rich-line support with many short
components which avoids such a Cartesian rectangle.  The cross-shift
cocycle remains available:

\[
 d_{c+d}(r)=d_c(r)+d_d(r+c).
\]

A second theorem now exploits that cocycle for the **full family** of rich
lines.  Two equal-slope patches at different shifts meet in at most one
tail; at the same shift, distinct parallel lines are disjoint.  Therefore,
for `J` distinct slopes,

\[
 \sum_\ell n_{c_\ell}L_\ell^2<{3\over2}Jk^3.
\]

If all tail-set pairs meet in at most two points, then
`sum_ell binom(L_ell,3)<=binom(k,3)`, so the entire actual record mass is
already cubic.  Any three-point overlap between unequal-slope patches
forces a child derivative line at the exact difference shift and difference
slope.  Finally the dyadic incidence estimate

\[
 T_w(L)\ll k^3+k^2L^2
\]

closes every richness scale `L<=sqrt(k)`.  Thus the surviving system is now
precisely: `L>sqrt(k)`, polynomial slope entropy, and abundant three-tail
overlaps recursively closed under the child operation.  Pure matching
patches really occur in genuine distance-Sidon sets, so this recursive
closure is load-bearing.

Read `PARABOLIC_GRAPH_DERIVATIVE_CURVATURE_GATE.md` and
`PARABOLIC_PARTIAL_DERIVATIVE_COCYCLE_PACKING.md`.

### B. Low band: the two residuals form a vertical tensor

For one surviving adaptive-tail occurrence, the affine-quadratic residual
is constant through all determinant-qualified target completions.  The
Gaussian residual merely indexes their signed areas.  Each joint residual
cell has divisor-bounded `m^{o(1)}` load, but one occurrence can occupy
`U_L(r)m^{-o(1)}` different cells.  Thus pointwise joint-cell compression
is impossible.

More sharply, a polynomial-height genuine distance-Sidon family survives
the adaptive quota with

\[
 X_{\rm ad}\ge cH_Q
\]

for an absolute `c>0`, while its completions occupy linearly many distinct
area cells.  Hence the hoped-for `X_ad<=m^{o(1)}H_Q` is sharp, not false.
What remains is a global size-biased packing/reuse theorem across different
occurrences and translations.

There is now an exact paired formulation of that theorem.  If the selected
loads for one translation are `U_(q,1)>=...` and `b_q` is its quota, then

\[
 \sum_{i>b_q}U_{q,i}
 \le {1\over b_q}\sum_{i<j}\min(U_{q,i},U_{q,j}).
\]

Therefore

\[
 X_{\rm ad}\le {H_Q\over k^2}\mathcal P_{\rm ad},
 \qquad
 \mathcal P_{\rm ad}=
 \sum_q{1\over h_q}\sum_{i<j}\min(U_{q,i},U_{q,j}),
\]

and `P_ad<=m^{o(1)}k^2` is sufficient.  The most economical endpoint reuse
is genuinely forbidden: a `P`-by-`Q` (`Q>=4`) occurrence grid whose two
clean target roles both factor as `{X_i,Y_j}` forces two different endpoint
pairs to have the same sum.  Conversely, an explicit abstract model has
`P_ad=Theta(k^3)` despite quotas, matching anchors, low codegree, disjoint
roles, and singleton area cells.  It is not geometrically realizable and
shows that the remaining theorem must extract approximately separable
two-role endpoint blocks or charge genuinely nonseparable designs.

Read `LOW_BAND_ADAPTIVE_TWO_RESIDUAL_TENSOR_BARRIER.md` and
`ADAPTIVE_AREA_CELL_CARTESIAN_REUSE_GATE.md`.

### C. High band: dyadic pooling is reversible

On a source-codegree band `K<=c(p)<2K`, let `R_K(w)` be the fixed-metric-
wedge selected mass of transverse-rich one-role bases.  The synchronized
pair mass satisfies the exact comparison

\[
 {K^2\over16}R_K(w)
 \le \Phi_{2,L,K}(w)
 <2K^2R_K(w).
\]

Consequently the local quartic gate is equivalent to

\[
 R_K(w)\le m^{o(1)}{k^4\over K^2}.
\]

Pooling supplies two powers of `K`, but forgetting the pool endpoints costs
exactly those two powers.  An explicit indexed endpoint model saturates
quartic scale with `98.4%` anchor-disjoint rich bases, so anchor-degree and
quota arguments cannot work after the clean affine equations are erased.
Gaussian determinant cells also stop at quartic scale.  The live theorem
must couple the rigid good-start pencil and the retained clean rows directly
to the physical metric wedge.

Read `SYNCHRONIZED_CLIQUE_DYADIC_REVERSIBILITY_BARRIER.md`.

### D. Immediate next actions

1. Control the recursive rich child-patch system at `L>sqrt(k)`, using
   additive growth or a height increment while retaining affine line labels.
2. Bound the paired functional `P_ad`, by separable two-role extraction or
   a direct endpoint cost for nonseparable target designs.
3. Prove the dyadic fixed-wedge rich-base estimate from the literal clean
   affine equations; graph-only relaxations are now ruled out.

## 2026-08-20 active checkpoint — sharp parabolic cells paid; adaptive tails isolated

The problem is **not solved**.  Five new theorem notes and five primary
verifiers have been independently rerun from the root session and pass.
They give genuine global progress on all three live fronts.

### A. Parabolic endpoint-product incidence: local sharpness and a closed global branch

For a primitive direction `w`, transverse shift `c`, and fibre occupancies
`a_r`, put `b_r=a_r a_(r+c)`.  Distance-Sidonicity makes the longitudinal
coordinates in the endpoint-product set distinct, giving the exact
three-level bound

\[
 T(r_0,r_1,r_2)\le
 \min\{b_{r_0}b_{r_1},b_{r_1}b_{r_2},b_{r_2}b_{r_0}\}.
\]

This cannot be improved by a power.  A Singer perfect-difference-set lift
produces genuine distance-Sidon sets with `k asymp q`, `m asymp q^2`, and
`Omega(q^4)=Omega(m^2)` clean fully transverse trace-2 records inside one
three-level cell.  The ambient `m^2` term is therefore exactly necessary.

The corresponding local-min envelope satisfies `S_w<=k^4/6` by Young
convolution.  If `R_w` is the number of occupied parallel fibres and
`R_w<=R_0`, then the directional Golomb budget gives

\[
 \sum_{w:R_w\le R_0}S_w
 \le {32\over3}R_0^2(m-1)^2H_{m-1}.
\]

Hence **every `m^{o(1)}`-fibre direction is now closed** at the required
`m^{2+o(1)}` scale, including the sharp Singer family.  The exact residual
is many-fibre/graph-like directions.  The integer parabola shows why this
is real: one direction has zero parallel-pair energy but
`S_w=2 binom(k,4)`, paid only because its height is `m=k^2`.

Read:

* `PARABOLIC_ENDPOINT_PRODUCT_SINGER_AMBIENT_SHARPNESS.md`;
* `PARABOLIC_LOCAL_MIN_FEW_FIBRE_GLOBAL_SUM.md`.

### B. Low common-`q` band: an adaptive translation quota removes the planted obstruction

For each clean translation put `h_q=|H_q|`, `H=sum h_q`, and

\[
 b_q=\left\lceil {k^2h_q\over H}\right\rceil.
\]

After discarding the `b_q` largest target-rich isolated occurrences for
each `q`, let `X_ad` be the remaining `U_L`-weighted mass.  The exact quota
identity gives

\[
 I^Q(w)\le2k^2+{X_{\rm ad}(w)\over T}.
\]

Thus `X_ad<=m^{o(1)}H` would finish the isolated local gate.  This quota is
necessary: a polynomial-height family disproves every one-free-per-`q` or
pointwise `h_q/k` version.  On closure `k=50`, however, only 15 records
survive the correct quota and their decorated mass is `1073`, versus
`H=26532`.  Each survivor retains both the affine quadratic residual and
the Gaussian scalar-area factorization.

Read `LOW_BAND_ISOLATED_ADAPTIVE_TRANSLATION_QUOTA_GATE.md`.

### C. High band: bounded pooling extraction is dead, but the clique mass localizes exactly

Pooling `ell` transverse third translations yields a factor
`k^{-(ell-1)}` and three synchronized endpoint four-sum clique identities.
Nevertheless the literal `k=48,c=49` scalar certificate contains a pool of
16 fully transverse translations whose **entire** endpoint union exposes
no equal-area witness.  Bounded-pool extraction to special-affine energy is
therefore false.

For `ell=2`, the surviving synchronized clique mass has a lossless fixed
metric-wedge expansion

\[
 P_2(V)=\sum_w\Phi_{2,L}(w),
\]

over exactly `N(k-2)` physical endpoint wedges.  The local quartic estimate

\[
 \Phi_{2,L}(w)\le m^{o(1)}k^4
\]

is sufficient.  Its source coefficient has an exact four-clean-fibre
expansion retaining the base pair, the pooled pair, the scalar shift, and
the determinant cutoff.  No counterexample to this quartic local gate is
known.

Read:

* `HIGHER_POOLING_FOUR_SUM_PARALLELOGRAM_GATE.md`;
* `SYNCHRONIZED_CLIQUE_FIXED_METRIC_WEDGE_LOCALIZATION.md`.

### D. Current residuals

The next proof attempt should target one of three exact statements:

1. control many-fibre/graph-like parabolic directions using the
   longitudinal coordinates discarded by the local-min envelope;
2. prove the adaptive decorated tail `X_ad<=m^{o(1)}H`; or
3. prove the synchronized fixed-wedge quartic bound above.

All three preserve the endpoint information that the preceding barriers
show to be indispensable.

## 2026-08-20 newest checkpoint — local shortcuts closed; exact aggregate gates survive

The problem is **not solved**.  Three new theorem notes and three independent
verification scripts were added after commit `6a67df9`; all six artifacts
pass both compilation and exact finite checks.  This checkpoint does not
weaken the two surviving endgames.  It rules out three attractive but false
ways of proving them and identifies the information that a successful
argument must retain.

### A. Literal high codegree does not give a local equal-area witness

The earlier scalar-to-area counterexample had source codegree below the live
threshold.  That loophole is now closed.  There is a genuine integral
distance-Sidon set with

\[
 k=48,\qquad c(p)=49\ge k,\qquad V(p)=V(p^{\rm op})=1,
\]

and 36 fully transverse third translations in one amplified record, but only
24 unordered six-distinct equal-area pairs in the entire set.  Of the 36
individual third-translation endpoint systems, 31 expose no area pair; of
the 630 two-system unions, 404 expose none.  Thus neither one translation nor
a pair of translations can support the hoped-for recordwise charge, even at
literal `c(p)>=k` and with the determinant-qualified scalar weight installed.

The exact always-valid two-translation output is instead a family of
eight-endpoint four-sum parallelograms.  Any scalar-to-affine bridge must pool
three or more translations, or charge those four-sum objects directly.

Read `HIGH_CODEGREE_TRANSVERSE_EQUAL_AREA_RANK_FLAT_BARRIER.md`.

### B. The low-band matching survivor is not controlled by path/rank algebra

For the exact common-`q` low-band weight, the anchor-isolated translations of
each source pair form directed paths and cycles.  A single head-isolated
translation already forces the two clean role-target edges to be disjoint,
and subtracting the two clean rows produces an exact sparse affine dependency
annihilating the columns `1,X,Y`.

This does not preserve the scalar bucket.  On the quadratic column the exact
residual is

\[
2d_{p,q}\!\cdot\!\rho=
 \delta(\tau_q(s))-\delta(\tau_q(t))
 -\bigl(\delta(s)-\delta(t)\bigr)
 +2q\!\cdot\!(\sigma_s-\sigma_t),
\]

so the clean-target gap and a `q`-dependent pair-sum term remain.  Path
telescoping is also quantitatively vacuous: on the principal `k=50` closure
stress, 517 of the 523 isolated occurrences are singleton path components.
There is still no counterexample to the desired common-`q` matching bound;
the exact residual theorem is a target-rich quadratic lift, not ordinary
matching rank.

Read `LOW_BAND_ISOLATED_MATCHING_RANK_BARRIER.md`.

### C. Raw parabolic collinearity is false; the corrected two-coordinate gate is exact

For trace `+2`, collinearity of displacement vectors, and for trace `-2`,
collinearity of endpoint sums, is far too weak.  An explicit six-row
quadratic Golomb-ruler family has `k=6h`, height `m=O(h^2)`, and `h^6`
ordered raw collinear triples with six distinct endpoint labels and two
noncollinear endpoint triangles.  This exceeds `k^3+m^2=O(h^4)` by a
quadratic factor.

The equal-area condition supplies exactly the missing coordinate.  Fix a
primitive displacement direction `w`, choose `z_w` with
`det(w,z_w)=1`, and write a correspondence as

\[
 y-x=c z_w+s w,\qquad r=\det(w,x).
\]

For fixed `(w,c)`, a genuine trace-2 parabolic record is precisely a
collinear triple in the `(r,s)` plane on a line `s=alpha+lambda r`, with
three distinct `r` values in the fully transverse case.  The trace `-2`
case has the analogous endpoint-sum lift.  This is now the correct affine
incidence target: prove an aggregate rich-line bound for the endpoint-product
sets `P_{w,c}`, not a raw collinearity theorem for `A-A` or `A+A`.

Read `PARABOLIC_DISPLACEMENT_COLLINEARITY_GOLOMB_BARRIER.md`.

### D. Current proof frontier

The direct grid proof still reduces to two credible theorem-sized gates:

1. **Low scalar/common-`q`:** bound the anchor-isolated, scalar-selected
   many-matching mass while retaining the quadratic residual above.
2. **Fully transverse equal-area:** prove the six-label
   `m^{o(1)}(k^3+m^2)` estimate, with the parabolic part recast as aggregate
   line energy in the endpoint-product sets `P_{w,c}`.

The newest work shows that local extraction, pairwise supersaturation, raw
collinearity, path telescoping, and affine rank alone cannot prove these
gates.  It does **not** provide a counterexample to either gate.  The next
attack should be aggregate and endpoint-sensitive from the outset.

## 2026-08-20 late checkpoint — exact weights restored; two matching cores remain

The problem is **not solved**.  Five new theorem notes and five independent
verification scripts were added after commit `b434b42`; all ten artifacts
pass.  This cycle corrected one misleading negative signal, ruled out a
record-by-record bridge between the two main approaches, and reduced both
surviving fronts to explicit matching-like aggregate statements.

### A. Low scalar band: the apparent blow-up was an artefact

The broad ambient moment `J_L` really does overcount by nearly a factor `k`,
but it is not the quantity in the clean-fibre argument.  Restoring the exact
common-translation weight gives

\[
 Z^Q_{<k,L}=\sum_r C^Q_{<k}(r)W_{r,L},
 \qquad
 C^Q_{<k}(r)=
 \sum_{\substack{s\ne s'\\
 \delta(s)-\delta(s')=-18r\\c_Q(s,s')<k}}c_Q(s,s').
 \tag{L.0}
\]

This has three lossless expansions: by the literal common translation `q`,
by source determinant cells, and by physical target wedges.  On closure
through `k=100`, the rich part of (L.0) is at most `0.02730` of the sufficient
scale `N(H_Q+k^3)`.  The polynomial rich-pencil example that killed the
ambient fixed-wedge conjecture has **zero** rich exact common-`q` mass.

For a fixed physical wedge, the exact local weight splits as

\[
 F^Q_{<k,L,T}(w)
 \le I^Q(w)+2A^Q_{\rm head}(w)+2A^Q_{\rm tail}(w).       \tag{L.1}
\]

The two `A` terms have exact fibre-intersection switches.  The survivor
`I^Q` is anchor-isolated: on closure `k=50`, literal anchor matchings supply
`496/662` of the maximum local weight.  No counterexample to

\[
 I^Q(w)\le m^{o(1)}(H_Q/k+k^2)                           \tag{L.2}
\]

is known; the normalized closure ratio is `0.548` at `k=100`.  Thus the low
band is now one precise many-matching aggregation theorem, not an ambient
distance-gap incidence problem.

Read:

* `LOW_BAND_COMMON_Q_WEDGE_RESTORATION.md`;
* `LOW_BAND_COMMON_Q_ANCHOR_ISOLATION_DICHOTOMY.md`.

### B. High scalar versus equal-area: no local bridge

The full endpoint equations of one amplified transverse scalar record imply
two equal-centroid/four-sum identities, but not an equal-area identity.  A
28-point integral distance-Sidon certificate has a determinant-qualified
transverse record of positive scalar weight and **no** six-distinct
equal-area pair.  This kills every record-by-record charge from the amplified
scalar system to the special-affine energy.

The certificate has source codegree `3<k`, so a collective high-codegree
bridge remains possible.  Such a proof must compare many third translations
inside the same `c(p)>=k` source pair; the local coordinate identities alone
cannot supply it.

Read `AMPLIFIED_TRANSVERSE_EQUAL_AREA_LOCAL_BRIDGE_BARRIER.md`.

### C. Fully transverse equal-area branch: six-label incidence is the core

For integer trace away from `+/-2`, fixing the special-affine map and the
product `Q_M(e_1)Q_M(e_3)` gives only `m^{o(1)}` records across all
translations.  Using all three invariant-form values is stronger: the joint
products `(q_1q_2,q_2q_3,q_3q_1)` determine only `m^{o(1)}` possible cross
matrices.

At trace `+/-2`, the value triples are signed-squareclass triples
`q_i=s r_i^2` with `r_1+r_2+r_3=0`, so there are only `O(m^2 log m)` cells.
Fixing a source triangle and one cell determines at most one target triangle.
However a polynomial-height trace-2 construction shows that one cell may
have linear degree and one map may carry `Theta(k^3)` records.  The diagonal
`k^3` allowance is therefore genuinely necessary.

The remaining parabolic problem is a bipartite six-label incidence bound
between at most `k^3` ordered source triangles and `O(m^2 log m)` value cells.
The dominant hard maps have overlap exactly three, so rich-map tails and
pointwise Pell bounds cannot close this branch.

Read:

* `INTEGER_TRACE_PRODUCT_PARABOLIC_AGGREGATE_GATE.md`;
* `THREE_SIDE_INVARIANT_COUPLING_PARABOLIC_SHARPNESS_GATE.md`.

### D. Exact live proof obligations

The current direct grid attack closes if either of these theorem-sized gates
is proved:

1. **Common-`q` matching gate:** prove the local or aggregate version of
   (L.2), paying many anchor-isolated scalar-selected source pairs.
2. **Six-label affine gate:** prove the corrected fully transverse
   equal-area bound `m^{o(1)}(k^3+m^2)` for the overlap-three,
   low-denominator/parabolic incidence core.

The high-codegree scalar route is still reduced to the amplified masses in
(A.0) below, but the new local counterexample shows that any connection to
the affine gate must be collective.  This is meaningful compression, not a
completed proof: no exponent beyond the already certified candidate upper
bound `0.494586` is claimed by this checkpoint.

## 2026-08-20 latest stocktake — two sharpened endgames and one retired bridge

The problem is **not solved**.  The latest cycle added eight standalone notes
and eight exact verification scripts, all of which pass.  It materially
sharpened the high-codegree scalar route and the independent equal-area route,
but it also killed the most optimistic localization of the low-codegree
scalar route.  The correct restart point is now as follows.

### A. High scalar band: third-translation amplification is load-bearing

A fixed backward scalar cell can have `Theta(k)` shifts even inside the
four-distinct star-to-`H_g` core.  Therefore neither a fixed-cell divisor
bound nor a two-translation endpoint argument can close the high band.

For every live source pair with `c(p)>=k`, adjoining a literal third clean
translation gives a factor-`1/k` amplification.  Each amplified record obeys
an exact local dichotomy: either it has at least `c(p)/2` fully transverse
third translations, or one of fifteen endpoint channels has load at least
`c(p)/30`.  The two shared-head channels admit a genuine double-closure
switch with at most four exceptions per source pair.  The analogous
shared-tail assertion is false: shifted starts land only in
`B_h=H_h disjoint-union partial B_h`, and the boundary really occurs.

The current exact residual is

\[
 D_{\rm one}^{\ge k}
 \le {2\over k}\mathfrak T+{60\over k}\mathfrak C
      +{30\over k}\mathfrak R_{13},                 \tag{A.0}
\]

where `T` is the fully transverse three-translation mass, `C` is the
double-closed shared-head mass, and `R_13` consists of two tail `B_h` lifts,
four cross-anchor chains, and seven target-star channels.  This is a real
power-saving reduction: the remaining task is to prove an
`m^(o(1)) N k^4` aggregate estimate for these endpoint-decorated masses.

Read:

* `SCALAR_BACKWARD_CELL_HIGH_CODEGREE_AMPLIFICATION.md`;
* `AMPLIFIED_THIRD_TRANSLATION_LOCAL_TRANSVERSE_DICHOTOMY.md`; and
* `AMPLIFIED_SHARED_ANCHOR_CHANNEL_CLOSURE.md`.

### B. Low scalar band: retire the broad `J_L` bridge

The physical fixed-wedge localization is exact, but the hoped-for pointwise
bound is false by a full factor `k`.  A polynomial-height perpendicular
two-axis distance-Sidon family has

\[
 F_{N,k}(w)=\Omega(k^2),\qquad
 J_N^{\rm rich}=\Omega(Nk^2).
\]

More seriously, the broad sufficient estimate
`J_L <= m^(o(1)) N k^2` is no longer a credible endgame without extra
common-translation structure.  On the near-critical closure prefixes, with
`L=floor(N/k)`, the normalized ratios `J_L/(Nk^2)` at
`k=20,30,...,120` are

\[
 0.960,2.113,3.514,4.545,5.477,6.576,6.842,7.485,8.410,9.299,9.706.
\]

Even restricting to target gaps with `U_L(r)>=k` gives ratios rising from
`0.707` to `7.193`.  Finite data do not prove a power-law counterexample,
but the profile is consistent with the broad moment paying almost an extra
factor `k`.  Do not replace the exact low-band weight `C_{<k}(r)` by the much
larger marginal multiplicity `R_D(-18r)` in the next attack.  The remaining
low-band theorem must preserve the common clean translation `q`.

Read:

* `LOW_BAND_FIXED_WEDGE_WEIGHT_GATE.md`; and
* `LOW_BAND_FIXED_WEDGE_RICH_PENCIL_COUNTEREXAMPLE.md`.

### C. Equal-area route: the fully transverse core is now arithmetic

The corrected ambient equal-area gate remains live.  Cyclic reindexing shows
that every record with *any* parallel cross-side pair is already controlled
by the proved corresponding-parallel estimate.  Thus the residual may assume
all nine cross determinants are nonzero.  A fixed nonzero area has load
`m^(o(1))k^2` by a top-line incidence/divisor argument, which proves the full
corrected gate in the range `m>=k^(5/2)`.

For a fully transverse special-affine overlap `g(x)=Mx+t`, the cross-
determinant array gives

\[
 K_{11}K_{22}-K_{12}K_{21}=d^2,
 \qquad K_{12}-K_{21}=d\,\operatorname{tr}M.
\]

If `tr(M)=p/q` in lowest terms, then `q` divides the index of the compatibility
lattice and

\[
 |A\cap g^{-1}A|
 \ll m^{3/2}q^{-3/4}+m^{1/2}+1.             \tag{C.0}
\]

Hence only low-denominator maps can be very rich.  For integer trace `T`,
the compatibility lattice carries the integral invariant form
`Q_M(v)=det(v,Mv)` of discriminant `T^2-4`; away from `T=+/-2`, every fixed
nonzero form value has only `m^(o(1))` compatible vectors, and the triangle
side labels obey the Pell-type identity

\[
 (q_1+q_3-q_2)^2+(4-T^2)d^2=4q_1q_3.       \tag{C.1}
\]

The unresolved area theorem is now an **aggregate** bound for low-denominator
fully transverse maps, especially integer trace and the parabolic cases
`T=+/-2`.  Pointwise Pell multiplicity is already controlled; the remaining
loss is the range of the two invariant-form/side-label parameters.

Read:

* `FULLY_TRANSVERSE_EQUAL_AREA_INCIDENCE_GATE.md`;
* `SPECIAL_AFFINE_TRACE_DENOMINATOR_LATTICE_GATE.md`; and
* `INTEGER_TRACE_INVARIANT_FORM_PELL_GATE.md`.

### D. Honest distance-to-proof assessment

The last few hours produced substantial mathematical progress, but not the
missing theorem.  We now have two credible proof fronts:

1. an endpoint-preserving scalar front, whose high band is reduced to (A.0)
   and whose low band must retain exact common-`q` weights; and
2. a special-affine equal-area front, reduced to aggregate low-denominator,
   fully transverse overlaps.

Both are theorem-sized gaps.  We are much closer to knowing **exactly what
must be proved** and what cannot work, but we are not yet close enough to
forecast a completion.  The best next move is to attack the integer-trace
aggregate in (C.1), while searching for a direct coupling between the fully
transverse three-translation mass in (A.0) and the same special-affine
geometry.  On the scalar low band, return to `C_{<k}` rather than `J_L`.

## 2026-08-20 continuation — lossless scalar split and reopened area route

The problem is still open, but the direct proof frontier is substantially
narrower than at the 2026-08-19 checkpoint.  This continuation added sixteen
standalone notes and sixteen verification scripts; every verifier and syntax
check passed before the checkpoint commit.

### A. The high-codegree scalar split no longer loses a power

For a source pair `p`, write `c(p)` for its common-clean-fibre codegree and
`rho(p)` for its rigid endpoint-replacement multiplicity.  The new sharp
star-constrained graph lemma proves, whenever `c(p)>=k`, that the one-role
target-wedge count `O(p)` satisfies

\[
 O(p)\ge c(p)-\rho(p).
\]

Consequently, for every nonnegative scalar weight `V`,

\[
 c(p)V(p)\le \rho(p)V(p)+O(p)V(p).                 \tag{A.1}
\]

This replaces the previous `sqrt(k)` rigid-branch loss and `k/K`
non-rigid loss by a constant-one completion.  The remaining high-codegree
mass is exactly the sum of a replacement term and a one-role wedge term.
`TARGET_STAR_REVERSE_EXTENSION_SWITCH.md` then rewrites both, preserving the
original clean translation `q`, as interactions between the canonical
`(k-2)`-point star

\[
 P_g=\{x+z:z\in A\setminus\{x,y\}\},\qquad g=y-x,
\]

and either the clean fibre `H_g` or an explicit boundary of size at most
`2(k-2)`.  The corresponding unweighted budgets are

\[
 D_{\rm rep}(1)\le2(k-2)H,
 \qquad
 D_{\rm one}(1)\le4(k-2)(N-1)H.
\]

The exact remaining high-band input is therefore a scalar-weighted backward
translation correlation involving

\[
 \delta(v-q)-\delta(w-q),
\]

with one endpoint in `P_g` and the other in `H_g` or its linear-size
boundary.  Endpoint support alone does not control this weight because it
varies with `q`.

Read:

* `HIGH_CODEGREE_REPLACEMENT_COMPLETION.md`;
* `TARGET_STAR_REVERSE_EXTENSION_SWITCH.md`;
* `METRIC_SCALAR_WEIGHTED_COMMON_TRANSLATION_DICHOTOMY.md`; and
* `SHARED_HEAD_FIBRE_INTERSECTION_SWITCH.md`.

### B. The low-codegree band is an exact two-scale weighted problem

For the part with `c(p)<k`, the sharp pointwise reduction is

\[
 C_{<k}(r)\le(k-1)R_D(-18r).                      \tag{B.1}
\]

This factor `k` is pointwise sharp even for a genuine polynomial-height
distance-Sidon set whose entire anchor graph is a matching.  If `U_L(r)` is
the determinant-qualified target-gap multiplicity and `W_{r,L}` its endpoint
wedge count, put

\[
 B_L=\sum_rR_D(-18r)U_L(r),\qquad
 J_L=\sum_rR_D(-18r)W_{r,L}.
\]

For `T>=k`, the exact endpoint-incidence calculation gives

\[
 \sum_{U_L(r)\ge T}R_D(-18r)
 \le
 \min\left\{\frac{B_L}{T},\frac{kJ_L}{T^2}\right\}.       \tag{B.2}
\]

Either `B_L` or `J_L` bounded by `m^(o(1))Nk^2` would close this band.  The
circle and line lifts are exact, but ordinary unweighted incidence theorems
are tautological here: all the difficulty is in the multiplicities.  A
genuine determinant-rich pencil attains `J_N=Theta(Nk^2)`, so the desired
scale is sharp rather than wasteful.

Read:

* `LOW_CODEGREE_ANCHOR_MATCHING_TWO_SCALE_BARRIER.md`;
* `LOW_BAND_TWO_SCALE_ENDPOINT_INCIDENCE_AUDIT.md`;
* `METRIC_SCALAR_ENDPOINT_RICH_TAIL.md`; and
* `TWO_SCALE_DISTANCE_GAP_CORRELATION_NO_GO.md`.

### C. The corrected equal-area route is live, with one full branch proved

The earlier examples with `Omega(k^4)` equal-area energy do **not** refute
the ambient-corrected sufficient estimate

\[
 E_{\triangle}^{(6,\ne0)}
 \le m^{o(1)}(k^3+m^2).                           \tag{C.1}
\]

Their polynomial Euclideanization height makes the `m^2` term dominant.
More importantly, `CORRECTED_EQUAL_AREA_AMBIENT_GATE.md` now proves the
entire part of the energy in which at least one pair of corresponding sides
is parallel:

\[
 E_{\triangle,\parallel}
 \le m^{o(1)}\bigl(k^3+m^2\log(2m)\bigr).          \tag{C.2}
\]

The proof reorganizes the longitudinal occupancy by divisors.  For a
primitive direction `u`, a signed edge length `t`, and line coordinate
`lambda`, an area has the form `z=t(lambda-sigma_t)`.  Divisor Cauchy,
`sum_L n_L^2=k+2e_u`, and `sum_u e_u^2=O(m^2 log m)` give (C.2).

Thus the equal-area route has one precise remaining gate: bound the
six-distinct equal-area pairs for which all three corresponding side pairs
are nonparallel.  Equivalently, control rich special-affine overlaps whose
contributing triangle avoids every real eigendirection.  This is currently
the cleanest independent alternative to the scalar weighted-tail route.

### D. Honest status after this continuation

This continuation did not solve #1208.  It did, however:

1. remove every polynomial bookkeeping loss from the high-codegree scalar
   decomposition;
2. identify the low band with the sharp two-scale moments (B.2);
3. prove a complete, nontrivial branch of the corrected equal-area route;
4. supply genuine distance-Sidon counterexamples to several tempting but
   false marginal, hub, matching, `L^2`, and raw two-scale estimates; and
5. leave two explicit theorem-sized targets rather than a diffuse inverse
   problem.

The next attack should prioritize the all-nonparallel special-affine area
gate, while retaining the scalar reverse-extension correlation as the main
fallback.  Do not return to an unweighted distance-gap, source-moment,
generic incidence, degree-balancing, or determinant-only argument without
using one of the new exact endpoint couplings; the new barriers rule those
out at the required exponent.

## 1. Bottom line

The problem is not solved.  The published/preprint window is

\[
 n^{1/3}\ll F_2(n)\ll n^{1/2-\varepsilon}.
\]

The project has two distinct achievements:

1. a candidate explicit partial upper improvement
   \(F_2(n)\ll n^{0.494586}\), with a finite certificate, a checked symbolic
   master inequality, and a now-completed audit of the published arithmetic
   tower input; and
2. a much more ambitious direct route which would prove the expected
   square-grid order \(F_2(n)=n^{1/3+o(1)}\) if one remaining
   endpoint-overlap theorem can be established.

Do not describe either item as a solution of #1208.  Read
`HANDOFF_20260813.md` for the literature chronology and first prime-power
certificate, then `HANDOFF_20260817.md` for the full upper-construction and
direct-attack history.  This file is the current delta and restart point.

### Critical correction: the two vector charges are false

The latest adversarial pass found a genuine asymptotic integral
distance-Sidon counterexample to both the dilated pair-sum charge and the
Gaussian oriented-edge-vector charge described later in this file.  Take a
dense Golomb ruler, split it between two nonperpendicular arms whose
directions are resonant under `3(I+J)`, and choose a generic polynomial-height
integral translation.  Triple-sum pigeonholing supplies a clean fibre of
size `Omega(s^2)`, while the vector charge has `Omega(s^4)` records on only
`O(s^2)` keys, hence energy `Omega(s^6)` instead of the proposed
`s^(4+o(1))` upper bound.

This is not an abstract pseudomodel: the union is a genuine distance-Sidon
set, and the finite verifier checks the orientation signs, all distances,
clean fibres, and energy.  It also kills the compound norm of the Gaussian
key.  It does **not** kill the separate positive distance-label charge
`delta(s)+18 delta(t)`, which becomes the positive quadratic form
`2(a^2+9b^2)` on the resonant subsystem.  Therefore all later references in
this handoff to either vector estimate as a live final gate are superseded.
The metric scalar correlation is the cleanest surviving direct target.

### Latest scalar checkpoint: the hard core is now sharply isolated

The last scalar pass produced two genuine positive reductions.  For a clean
fibre `H_q` of size `h`, with `N=binom(k,2)`, the surviving sufficient bound
remains

\[
 \mathcal M_{q,18}\le m^{o(1)}N(h+k).
\]

First, partition every squared-distance label by its squarefree kernel.  A
uniform imaginary-quadratic divisor argument proves that all collisions
which stay in one source squareclass and one target squareclass have total
mass `m^(o(1))hN`.  This includes every parallel--parallel collision and
rigorously explains why the resonant ruler construction that kills the
vector gates does not kill the scalar gate.  After the already-proved
three-label cleanup, only four-distinct-edge, squareclass-transverse
collisions remain.

Second, factor a pair of edge vectors through its four endpoint-realized
cross differences.  Fixing the squared-radius difference and signed doubled
area fixes a Gaussian product, so it has only divisor-many realizations.
Consequently

\[
 T_q^{(4)}\bigl(|2\det(v,v')|\le L\bigr)
 \le (2L+1)m^{o(1)}h^2.
\]

Taking `L=floor(N/h)` pays for this whole branch inside `m^(o(1))hN`.
Therefore any remaining polynomial obstruction must simultaneously have:

1. four distinct edge labels;
2. a squareclass change on the source or target side;
3. nonparallel target edges with `|2 det(v,v')|>N/h`;
4. all endpoint cross-differences realized in the single complete
   difference set `A-A`; and
5. the exact clean-fibre translate constraints.

This is the narrowest rigorous statement of the missing theorem so far.
It is still open: the scalar equation fixes the real part of a Gaussian
product but leaves up to `Theta(m^2)` signed areas, and summing them naively
loses the cube-root budget.

Three complementary audits prevent false shortcuts.  The joint trace--area
key has divisor-size fibres, but a radial-transversal model shows that joint
near-injectivity does not control either marginal energy.  Natural endpoint
Fourier radializations require an `Omega(N)` positivity shift on genuine
distance-Sidon sets, so positive definiteness does not close the scalar
gate.  Finally, universal matrix keys and all internal/internal ruler-arm
blocks are already divisor-scale; finite-field parabolas and every tested
two-, three-, and four-arm construction pass the scalar bound.  The only
natural ruler stress not proved in full is the cross-edge block between
genuinely transverse arms.

Read, in order:

* `METRIC_SCALAR_SQUARECLASS_TRANSVERSE_GATE.md`;
* `METRIC_SCALAR_CROSS_EDGE_DETERMINANT_BRANCH.md`;
* `METRIC_TRACE_AREA_HYBRID_AUDIT.md`;
* `METRIC_SCALAR_FOURIER_ENDPOINT_NO_GO.md`; and
* `METRIC_SCALAR_UNIVERSAL_MATRIX_AND_RULER_STRESS.md`.

Each has a standalone verifier.  All five passed on 2026-08-19.  The next
live move is not another generic charge: it is an endpoint-sensitive bound
for the large-area squareclass-transverse core.

Two immediate follow-up ideas have already been adversarially closed.  The
literal matrix-projection inverse is false even for integral rank-one
positive matrices with injective trace.  It can be dressed with genuine
clean endpoints in a polynomial-height distance-Sidon set, but only at
`h=Theta(k)`, where its `k^(3-o(1))` energy is exactly absorbed by the
necessary `N(h+k)` term.  Ordinary BSG extracts only a sub-`k` matching and
therefore cannot create endpoint reuse.  See
`METRIC_MATRIX_PROJECTION_INVERSE_AUDIT.md`.

Likewise, all same-norm Gaussian multiplier copies of
`|widehat(1_A)|^2-k` are exactly orthogonal, but the sharp support-packing
bound is only `r(n)k(k-1) << m^2 n`; since `r(n)=n^(o(1))`, optimizing never
beats the linear `k=O(m)` scale.  See
`GAUSSIAN_MULTIPLIER_ORTHOGONALITY_AUDIT.md` and its verifier.

The remaining bridge must therefore aggregate many heavy fibres while
retaining shared endpoint incidence.  A successful inverse statement has
to reach a superlinear set of reused endpoints; cone geometry, quotient
energy, a single planted fibre, or second-moment Fourier packing alone is
provably insufficient.

### Consolidated large-area / many-fibre checkpoint

The sharpest sufficient statement is now an aggregate theorem.  Put

\[
 H=\sum_qh_q,\qquad
 \mathcal X_q=\mathcal M_{q,18}-Nh_q,
 \qquad N=\binom k2.
\]

It is enough to prove

\[
 \boxed{\sum_q\mathcal X_q\le m^{o(1)}Nk^3.}              \tag{A}
\]

Indeed, fibrewise Cauchy over the `O(m^2)` scalar keys and Cauchy over the
at most `k(k-1)` realized differences give

\[
 H^2\le m^{2+o(1)}(H+k^3),
\]

and hence `H<=m^(2+o(1))+k^(3+o(1))`, exactly the ambient centroid estimate
needed for `k<=m^(2/3+o(1))`.

There is an exact many-fibre identity.  If

\[
 c(s,s')=\#\{q:s,s'\in H_q\},
 \qquad
 R_D(r)=\#\{(t,t'): \delta(t)-\delta(t')=r\},
\]

then

\[
 \sum_q\mathcal M_{q,18}
 =\sum_{s,s'}c(s,s')
   R_D\!\left({\delta(s')-\delta(s)\over18}\right),
 \qquad
 \sum_{s,s'}c(s,s')^2
 =\sum_{q,q'}|H_q\cap H_{q'}|^2.               \tag{B}
\]

The repeated-label, squareclass-resonant, adaptive low-target-area, and
adaptive low Gaussian-residual branches all sum to
`m^(o(1))NH`.  Thus (A) is reduced exactly to the aggregate four-edge,
squareclass-transverse, large-area/large-residual core.  This is recorded in
`METRIC_SCALAR_AGGREGATE_MANY_FIBRE_AUDIT.md` and
`METRIC_SCALAR_LARGE_AREA_GAUSSIAN_RESIDUAL_AUDIT.md`.

The latest endpoint attacks identify both the leverage and its present
limit.  For a source matching `F` in one clean fibre, let `d_T` be the
degree vector of its translated target graph, let `1_V` be the source
endpoint indicator, and put `z=d_T-1_V`.  If `W_F` counts target wedges and
`O_F` target incidences escaping the source support, then

\[
 \sum_{x\in A}z_x=0,\qquad
 \sum_{x\in A}z_x\,x=Lq,\qquad
 \boxed{\|z\|_2^2=2(W_F+O_F)}.                 \tag{C}
\]

So a perfect source matching cannot translate to the same degree vector.
Every target `C_4` also creates an eight-source-endpoint pair-sum
parallelogram.  Nevertheless, a genuine collinear distance-Sidon family
has a clean source matching and translated target matching of size
`L=(k-2)/4` with zero target wedges.  Matching partitions therefore force
only `Omega(h_q/k)` defect, one factor `k` short.  Moreover the signed
common-translation design matrix has only the unavoidable affine nullity on
genuine transformed-parabola fibres; sparse design-rank bounds are already
saturated.  See `MATCHING_BLOCK_TRANSLATION_LEVERAGE.md` and
`COMMON_TRANSLATION_DESIGN_MATRIX_MATCHING_DEFECT_AUDIT.md`.

The best adversarial search still finds no scalar counterexample.  Across
all exact stresses, the largest pointwise ratio is

\[
 \max_q {\mathcal M_{q,18}\over N(h_q+k)}
 =0.822031442\ldots,
\]

on an affine deformation of the 43-point parabola; its aggregate excess is
only `0.041410 Nk^3`.  Multi-arm stresses dilute rather than amplify.  This
is evidence for (A), not a proof.  The precise remaining task is to combine
scalar-energy concentration with the common-translation rows; endpoint
reuse, wedges, rank, BSG, determinant largeness, or uniform nine-anchor
quadratic symmetrization separately are now all known to be insufficient.
See `METRIC_SCALAR_ADVERSARIAL_STRESS.md` and
`EQUAL_CENTROID_NINE_ANCHOR_SYMMETRY_AUDIT.md`.

### Latest endpoint-weighted checkpoint

The aggregate scalar core now has a precise reciprocal-tail formulation.
Put

\[
 C(r)=\sum_q R_{B_q}(-18r),\qquad
 S_2=\sum_qh_q(h_q-1),\qquad
 L_*={Nk^3\over S_2}.
\]

For a single fibre, every nonzero target gap with
`R_D(r)<=N/h_q` contributes less than `Nh_q`.  Aggregately, all gaps with
`R_D(r)<=L_*` contribute at most `Nk^3`.  Thus it is enough to prove the
dyadic anti-correlation estimate

\[
 \boxed{
 \sum_{\lambda<R_D(r)\le2\lambda}C(r)
 \le m^{o(1)}{Nk^3\over\lambda}.}              \tag{D}
\]

Equivalently, a cumulative reciprocal weighted tail with the same scale
would suffice.  This is the cleanest surviving statement of the missing
many-fibre theorem.  See `FIXED_DISTANCE_GAP_WEIGHTED_TAIL_AUDIT.md` and
`METRIC_SCALAR_GAP_CODEGREE_BARRIER.md`.

Several tempting weakenings of (D) are now rigorously dead.  A fixed
nonzero squared-distance gap can have multiplicity
`k^2/8=(1/4+o(1))N` in an infinite family of genuine integral
distance-Sidon sets.  Those sets can nevertheless have no clean fibres at
all, so raw gap popularity and clean codegree are genuinely different
statistics.  Conversely, polynomial-height genuine examples can have both
`h_q=Omega(k^2)` and a nonzero target gap of multiplicity `k^(1-o(1))`;
the two large marginals still need not align.  Hence only a joint,
endpoint-sensitive anti-correlation can close the proof.

Determinant decoration removes another full branch.  For two oriented edge
vectors `u,v`, with

\[
 r=|u|^2-|v|^2,\qquad d=2\det(u,v),
\]

the exact Gaussian factorization

\[
 (u-v)\overline{(u+v)}=r-id
\]

gives

\[
 R_D(r,d)\le4\tau(r^2+d^2)^2=m^{o(1)}.
\]

It follows that the entire adaptive low-determinant part of the aggregate
scalar energy is `m^(o(1))NH`; every surviving rich norm gap must spread
over many high-determinant cells.  The remaining difficulty is not the
number of Gaussian factorizations but their clean-fibre weights: a checked
parabola cell has only seven source edge-pairs but total shared-fibre weight
219.  See `METRIC_SCALAR_DETERMINANT_WEIGHTED_TAIL.md`.

Two further endpoint decompositions sharpen what cannot work.  Within one
scalar bucket of load `L`, the exact identity

\[
 L(L-1)=2W_S+2W_T+B_{\rm bi}
\]

splits off source and clean-target wedges, leaving collision pairs disjoint
on both sides.  A verified 74-point integral distance-Sidon set has one
load-12 bucket whose 132 off-diagonal collisions all lie in this bi-disjoint
term, while both role graphs are matchings and all associated wedge/C4
counts vanish.  Thus collisionwise charging to target C4s or their
eight-endpoint parallelograms is false.  See
`METRIC_SCALAR_TARGET_C4_BARRIER.md`.

An alternative surviving gate keeps both trace
`T=|u|^2+18|v|^2` and signed area `A=det(u,v)`, then charges each record to
the less popular marginal.  Joint `(T,A)` cells have divisor-size
multiplicity.  If this adaptive envelope were polynomially too large, its
trace--area support graph would contain a subgraph of polynomial minimum
degree.  Every genuine stress remains below 0.82 of the weak budget, while
the endpoint-free radial transversal fails polynomially.  Therefore a
possible equivalent closing theorem is: the clean common-translation
endpoint equations forbid such a dense trace--area support patch.  See
`ADAPTIVE_TRACE_AREA_ENDPOINT_CHARGE.md`.

There is one exact balanced-degree theorem behind this gate.  Partition a
clean fibre into `B` blocks which are matchings in both the source and
clean-target endpoint graphs, and retain the block id in the adaptive
trace--area charge.  If `h>k`, `B=O(h/k)`, and the sum of the blockwise
envelopes is diagonal-scale, then

\[
 (hN)^2\ll Bm^2\sum_b\mathcal B_{q,b}
\]

cancels the fibre size and gives `kN<=m^(2+o(1))`, hence the cube-root
bound.  Greedy role coloring gives `B<=4Delta-3`, so this closes the
balanced endpoint-degree regime `Delta=O(h/k)`, conditional only on the
blockwise envelope estimate.  The condition is genuinely structural:
distance-Sidon source stars can require `B=h`, a 38-point genuine fibre has
an exact `K_(3,3)` trace--area support, and abstract projective-plane
supports show that minimum degree/DRC alone need not even create a C4.
See `TRACE_AREA_BIMATCHING_BLOCK_GATE.md`.

The current research frontier is consequently very specific: prove (D),
or exclude the equivalent dense endpoint-decorated trace--area support
patch.  Low multiplicity, squareclasses, small area, small Gaussian
residual, small determinant, wedges/C4s, ordinary design rank, and all
unweighted rich-gap estimates have already been completely accounted for
or refuted by genuine distance-Sidon examples.

### Calibrated stocktake

After this checkpoint the repository contains 134 commits touching the
#1208 work, 311 top-level research/verification files, and 133 verifier
programs.  Those
counts measure search coverage, not proximity to a theorem.  The durable
mathematical progress is:

1. the explicit candidate upper exponent `0.494586`, with the symbolic,
   numerical, and rank-715 arithmetic inputs audited;
2. a complete exact reduction of the square-grid upper problem to clean
   pair-sum fibres, including `C_6(A)=4 sum_q |H_q|`;
3. a surviving scalar sufficient theorem

   `M_(q,18) <= m^(o(1)) binom(k,2)(|H_q|+k)`,

   whose collinear branch is proved and whose repeated-edge-label part is
   already diagonal-scale; and
4. a substantial barrier library showing why generic hypergraphs,
   low-codegree arguments, ordinary additive energy, BSG/DRC, dense-patch
   extraction, scalar Bessel positivity, fixed bilinear charges, and now the
   two natural vector charges cannot by themselves solve the problem.

Logically, a full square-grid upper bound is one theorem away.  Practically,
that theorem controls a four-distinct-distance dilation correlation and is
still of open-problem strength.  The project is therefore far beyond initial
exploration and has several potentially publishable partial results, but it
is not in a final-proof-polishing phase.  Do not attach a percentage to the
remaining work: one new inverse or correlation idea could close it, while
the current toolkit alone does not.

### Current checkpoint: what the last few hours actually changed

The newest direct reduction is substantially simpler than the preceding
joint-spectrum gate.  For a distance-Sidon
`A subset [0,m]^2`, `|A|=k`, put

\[
 E_3^+(A)=\int|\widehat{1_A}|^6.
\]

Origin localization gives `E_3^+(A)>=k^6/(512m^2)`.  Consequently the
single ambient estimate

\[
 E_3^+(A)\le k^{3+o(1)}+m^{2+o(1)}
\]

would already give `k<=m^(2/3+o(1))` and resolve the expected exponent in
#1208.  It also implies the ambient cross-sum gate by the exact inequality
`E^+(A+JA)<=k^2 E_3^+(A)`.

After deleting the automatic repeated-label configurations, this scalar
statement has an exact finite form.  If `t_s` is the number of unordered
three-subsets of `A` with vector sum `s`, vector-Sidonicity makes every
common-sum class a matching, and the six-distinct ordered count is

\[
 C_6(A)=36\sum_s t_s(t_s-1).
\]

Thus the new missing theorem is the ambient equal-centroid bound

\[
 \sum_s t_s(t_s-1)
 \le k^{3+o(1)}+m^{2+o(1)}.
\]

Every stored obstruction passes at the sharper experimental `m^2` scale.
On closure `k=40,80,120`, the exact values of `C_6/m^2` are
`0.9990..., 1.0533..., 1.0861...`; the transformed 127-point finite-field
parabola, whose ordinary third energy is of order `k^4`, is paid for by its
quadratic height.  The natural relative-inertia charge of one pair of
equal-centroid triangles lands in an `O(m^2)` integer interval and has
maximum loads only `3,1,2,3` on closure 120, the perpendicular ruler,
Costas 22, and the parabola image.  No multiplicity theorem for that charge
is yet proved, so this is a sharply calibrated gate, not the solution.
Read `AMBIENT_THIRD_ENERGY_CENTROID_GATE.md` and run
`verify_ambient_third_energy_centroid_gate.py`.

The centroid gate now has a more local exact formulation.  Let

\[
 \Sigma=A\mathbin{\oplus}A
 =\{a+b:\{a,b\}\in\tbinom A2\},
 \qquad R_\Sigma(q)=|\Sigma\cap(\Sigma+q)|.
\]

Vector-Sidonicity makes every pair sum unique.  If `q=a-b` is a realized
directed difference, exactly `k-2` arrows in this translate overlap are the
forced star `b+c -> a+c`.  Put

\[
 \widetilde h(q)=R_\Sigma(q)-(k-2).
\]

Let `h_6(q)` retain only arrows whose two pair endpoints are disjoint from
one another and from `{a,b}`.  Then the six-distinct third-energy count is
exactly

\[
 C_6(A)=4\sum_{q\in(A-A)^*}h_6(q),
 \qquad 0\le h_6(q)\le\widetilde h(q).
\]

Consequently the pointwise internal-shift theorem

\[
 \widetilde h(q)\le k^{o(1)}(1+m^2/k^2)          \tag{1.1}
\]

would prove the cube-root bound.  This is currently the shortest local
target: after subtracting the unavoidable third-point star, a translation
of the uniquely represented pair-midpoint set should have only
reciprocal-density excess.  All stored stresses pass the corresponding
clean bound at constant-one scale; the exact verifier also separates the
small repeated-label remainder, after an earlier draft incorrectly folded
that remainder into the clean identity.  Read
`INTERNAL_SHIFT_PAIR_SUM_GATE.md`.  Estimate (1.1) remains unproved.

There is now an explicit amplification of that local gate.  With the same
`Sigma` and clean start set `H_q`, put `Lambda=3(I+J)` and charge

\[
 \Psi_q(s,t)=s+\Lambda t,
 \qquad (s,t)\in H_q\times\Sigma.
\]

This has `|H_q| binom(k,2)` records and only `O(m^2)` possible integer
keys.  Its collision energy is exactly

\[
 \mathcal E_q=
 \sum_w r_{\Sigma-\Sigma}(w)r_{H_q-H_q}(-\Lambda w),
\]

with diagonal contribution exactly `|H_q| binom(k,2)`.  Therefore the
off-diagonal resonance estimate

\[
 \sum_{w\ne0}r_{\Sigma-\Sigma}(w)
                 r_{H_q-H_q}(-\Lambda w)
 \le k^{o(1)}|H_q|\binom k2                       \tag{1.2}
\]

would prove the pointwise gate and hence the cube-root bound.  This is not
just a maximum-load reformulation: it isolates one coupled dilation moment,
and the resonance-free branch is exactly injective.  On the 120-point
closure its normalized energy is `1.034764...` with maximum load four; on a
43-point distance-separated finite-field parabola it is `1.016747...` with
maximum load two.  All eight exact stresses pass.  Read
`DILATED_INTERNAL_PAIR_SUM_CHARGE.md` and run its verifier.  Estimate (1.2)
is unproved; the hard branch is a low-index rank-two resonance model, while
generic affine images are nearly or exactly injective.

Two exact results now delimit that gate.

First, it is proved in every projection-sparse branch.  For any nonzero
real functional `pi`, put `R_pi=|pi(Sigma)|`.  The two functionals `pi` and
`pi o Lambda` are independent, so inside one charge fibre the two labels
`(pi(s),pi(t))` recover `(s,t)`.  Hence

\[
 \sum_z|\Psi_q^{-1}(z)|^2\le R_\pi^2|H_q|\binom k2. \tag{1.3}
\]

If `A` lies on `r` parallel lines, then `R_pi<=r(r+1)/2`; in particular a
subpolynomial parallel-line cover satisfies the full missing estimate.  The
quantitative consequence is
`k << m^(2/3)(1+R_pi^(1/3)) << m^(2/3)(1+r^(2/3))`.  Read
`DILATED_INTERNAL_PAIR_SUM_PROJECTION_BRANCH.md`; its verifier checks every
realized clean fibre on the stored finite stresses.

Second, the same gate is false by a quadratic factor if Euclidean radial
uniqueness is weakened to vector-Sidonicity.  The integer lift
`A_p={(x,x^2 mod p):0<=x<p}` is vector-Sidon and has unique pair sums, but
third-energy pigeonholing gives some clean fibre `|H_q|>>p^2`.  Since the
charge has `Theta(p^4)` records and only `O(p^2)` ambient keys, its normalized
energy is `Omega(p^2)`.  Exact rows through `p=61` show the growth.  Thus a
proof must turn many compatible charge collisions into a genuine equality
of two endpoint distances; additive/Freiman/design information alone cannot
close it.  Read `FINITE_FIELD_PARABOLA_DILATED_CHARGE_BARRIER.md` and its
verifier.

There is also now a rigorous low-codegree supersaturation branch.  An
off-diagonal charge collision expands, using canonical pair endpoints, to
the eight-role Gaussian relation

\[
 c+d-c'-d'+3(1+i)(x+y-x'-y')=0.                 \tag{1.4}
\]

Stratify by equality patterns among the eight labels and let `T_q` be the
largest pattern-specific codegree of two prescribed role-label columns.
The Dvir--Saraf--Wigderson design theorem gives

\[
 |\mathcal C_q|\le C k^2T_q.                    \tag{1.5}
\]

Thus `T_q<=|H_q|k^(o(1))` proves the desired charge energy.  Any
fixed-power counterexample must instead concentrate polynomially many
collisions through the same two endpoint roles and labels.  All stored
distance-Sidon stresses satisfy `T_q<=|H_q|`.  Read
`DILATED_CHARGE_DESIGN_CODEGREE_BRANCH.md` and run its exact verifier.  The
remaining theorem is now specifically metric control of that endpoint-rich
two-role core.  Globally it is enough to prove the weaker sharp local bound

\[
 T_q\le (|H_q|+k)k^{o(1)},                      \tag{1.6}
\]

because fibres with `|H_q|<=k` contribute only `O(k^3)` clean records and
need no amplification.

The rank-715 construction has passed its missing arithmetic audit.  The
tame totally-real presentation quoted in the human-verified unit-distance
paper gives generator rank 715 and base relation rank at most 715.  Adding
127,091 Frobenius-square relations leaves

\[
 r\le127806<715^2/4,
\]

so Golod--Shafarevich gives an infinite quotient.  An elementary normal
series lemma supplies Galois layers of every sufficiently large dyadic
degree with kernels inside the Frattini subgroup; consequently the selected
`3 mod 4` primes retain residue degree exactly two in every layer.  The full
prime/rank/local-slope/exponent verifier passes with endpoint margins larger
than three.  Subject to the standard cited presentation theorem, the
candidate partial result

\[
 F_2(n)\ll n^{0.494586}
\]

is therefore a validated theorem candidate rather than a numerically
interesting but arithmetically unaudited certificate.  It is still
unreviewed and is not the solution of #1208.  Read
`RANK715_ARITHMETIC_INPUT_AUDIT.md` and
`proof_frobenius_all_depth_rank715.md`.

The cleanest new direct gate packages the endpoint problem as additive
energy.  For a distance-Sidon set
`A subset [0,m]^2`, `|A|=k`, quarter-turn `J`, and `B=A+JA`, the
representation `a+Jb` is unique, hence `|B|=k^2`, and exactly

\[
 E^+(B)=\int|\widehat{1_A}(\theta)|^4
                |\widehat{1_A}(J\theta)|^4\,d\theta
       \ge {k^8\over1024m^2}.
\]

Thus the single ambient-sensitive upper estimate

\[
 E^+(A+JA)\le k^{5+o(1)}+m^{2+o(1)}k^2
\]

would give `k<=m^(2/3+o(1))` and resolve the expected exponent in #1208.
Unlike the older false size-only mixed-energy conjecture, this formulation
survives the perpendicular Golomb-ruler family: its large energy is paid by
the ambient `m^2 k^2` term.  The exact verifier checks the Fourier
coefficient identity, origin lower bound, cross-sum directness, and five
stress families.  This upper estimate remains unproved.  Read
`AMBIENT_CROSS_SUM_ENERGY_GATE.md` and run its verifier.

The ambient gate has now also been stress-tested exactly on the complete
120-point transverse-closure witness.  An integer-array computation, rather
than a floating transform, gives

\[
 m=1514,\qquad E^+(A+JA)=24{,}957{,}897{,}968,
 \qquad \max_{t\ne0}r_{A+JA}(t)=1071.
\]

The corresponding aggregate ratio is
`E/(k^5+m^2k^2)=0.431120...`, while the pointwise ratio is
`max r/(k+m^2/k^2)=3.83623...`.  Thus the strongest known closure stress
still fits the aggregate conjecture comfortably, even though its worst
individual fibre has grown substantially.  This is calibration only; it
does not prove the ambient estimate.  Compile and run
`verify_ambient_cross_sum_full_closure.cpp` for the exact certificate.

There is now a Fourier-side localization of the same obstruction.  Write

\[
 \lambda=\widehat{1_D}=|\widehat{1_A}|^2-(k-1),
 \qquad \mu=\lambda\circ J.
\]

Parseval gives `int lambda^2=int mu^2=N`, hence
`int|lambda mu|<=N`.  It follows immediately that the region
`|lambda mu|<=N^(3/2)` contributes at most `N^(5/2)` to
`E_perp(D)=int lambda^2 mu^2`.  The endpoint floor
`lambda,mu>=-(k-1)>=-sqrt(N)` and the upper bound `lambda,mu<=N` show that
the complementary region necessarily has

\[
 \lambda>\sqrt N,\qquad\mu>\sqrt N,
 \qquad\lambda\mu>N^{3/2}.
\]

There is a stronger balanced truncation.  If either factor has magnitude at
most `N^(3/4)`, Parseval bounds that whole region by `N^(5/2)`; the negative
floor then makes both factors positive on the complement.  Consequently the
only unresolved region is

\[
 \lambda(\theta)>N^{3/4},\qquad
 \lambda(J\theta)>N^{3/4}.
\]

Thus every low-product layer, every negative layer, every one-sided-large
layer, and every unbalanced positive layer is now eliminated.  Equivalently,
a counterexample must have both `|hat 1_A(theta)|` and
`|hat 1_A(J theta)|` above `k^(3/4+o(1))`.  This is the preferred interface
for the endpoint Fourier compensation programme.  Read
`JOINT_POSITIVE_SPECTRUM_GATE.md`; its perpendicular large-spectrum theorem
remains unproved.

That ambient gate is now reduced to one sharply delimited population.  Put
`D=A-A`, `N=|D|=k(k-1)+1`,

\[
 R(q)=|D\cap(D+q)|,\qquad
 E_\perp(D)=\sum_qR(q)R(Jq).
\]

An exact autocorrelation expansion gives

\[
 E^+(A+JA)=E_\perp(D)-N^2+(2k^2-k)^2
       +4(k-1)\sum_{q\in D\setminus\{0\}}R(Jq),
\]

so the weighted correction is automatically `O(k^5)`.  Independently,
every energy tuple in which any two of the eight endpoint roles coincide
contributes at most `56k^5` in total.  Finally all shifts for which either
`R(q)<=sqrt(N)` or `R(Jq)<=sqrt(N)` cost at most
`N^2+2N^(5/2)`.  Therefore the full cube-root conclusion is reduced to the
single eight-distinct, jointly rich estimate

\[
 \sum_{\substack{q\ne0\\R(q)>\sqrt N,\ R(Jq)>\sqrt N}}
 R(q)R(Jq)
 \le N^{5/2+o(1)}+m^{2+o(1)}N.
\]

This is a meaningful narrowing, not the missing estimate itself.  It shows
that diagonal configurations, repeated endpoints, and all low-overlap
translations are not the obstruction; only simultaneous rich reuse in a
direction and its quarter-turn remains.  Read
`CROSS_SUM_DIAGONAL_AND_RICH_TAIL_REDUCTION.md` and run its exact-integer
verifier.

The cleanest aggregate interface is now the opposite-endpoint charge

\[
 \Xi(u,s,q,q')=(w-(I+J)q,\,2u+q')\in D\times(D+D).
\]

It is exactly injective inside each rich fibre.  If `E_F` is the image of
one fibre, then `E_F` is exactly a biclique `V_F x T_F` with its canonical
matching deleted, and

\[
 \sum\nu^2=\mathcal O_K+\sum_{F\ne G}|E_F\cap E_G|.
\]

Thus all remaining multiplicity is purely cross-fibre.  Pairwise
intersections reduce to a product of two coupled translated overlaps of the
rich-fibre sets.  A subpolynomial size-biased bound for that coupled overlap
would give `O_K<=N^(1+o(1))S` and finish the adaptive tail.  The charge is
exactly injective on the full Sidon-ruler midpoint obstruction and nearly
injective on the quadratic gadget, so it survives the counterexamples that
killed the older midpoint charge.  Abstract radial transversals still have
polynomial load, showing that `D=A-A` remains load-bearing.  Read
`SEVEN_INCIDENCE_OPPOSITE_ENDPOINT_CHARGE.md` and run its verifier.

The inverse/extraction audit sharpened that last point.  Sparse oblique
shears can retain radial uniqueness, all three exact affine fibre copies,
and adaptive popularity without containing any polynomial complete lattice
patch.  Ordinary BSG, DRC, Varnavides, or density-to-patch extraction cannot
close the gate.  The one conspicuous property those models lack is complete
difference endpoint realizability: for a genuine `D=A-A`,

\[
 \widehat{1_D}(\xi)=|\widehat{1_A}(\xi)|^2-(|A|-1)
 \ge-(|A|-1).
\]

The finite adaptive shear stress violates this lower bound by a linear
factor.  The compensation half of the resulting spectral route is now
rigorous.  If \(0\le g\le1_D\), \(r=1_D-g\), and

\[
 \Delta_g(\theta)=
 \bigl(-(|A|-1)-\operatorname{Re}\widehat g(\theta)\bigr)_+,
\]

then pointwise \(\Delta_g\le|\widehat r|\), and Parseval gives

\[
 \int_{\mathbb T^2}\Delta_g^2\le\sum_xr(x)^2
 \le |D|-\sum_xg(x).
\]

For an endpoint piece \(E\subset[-M,M]^2\) with
\(\operatorname{Re}\widehat{1_E}(\theta_0)\le-\eta|E|\), this yields the
explicit height-sensitive bound

\[
 |D\setminus E|\ge{\eta^2\over16\pi^2M^2}
 \left({\eta|E|\over2}-(|A|-1)\right)_+^2.
\]

Thus a large negative structured core now has a quantified complement cost.
The remaining open extraction step is to derive such a negative core from
excessive cross-fibre load and then charge its compensation to the ordinary
sumset budget.  Read ENDPOINT_FOURIER_COMPENSATION_LEMMA.md and run its
verifier.

There is now a smaller exact extraction interface.  Start with the fixed
\(D^2\) charge and count ordered collisions in one charge cell, so that
\(M_K=\sum\lambda^2\).  For two colliding records \(\gamma,\gamma'\), with
the seven roles \(R_0,\ldots,R_6\), the new endpoint cross-switched charge is

\[
 \Theta(\gamma,\gamma')=
 \left(
 m(R_0(\gamma))-m(R_3(\gamma')),
 \chi(R_4(\gamma),R_2(\gamma'))
 \right).
\]

Here \(\chi\) stores the nonzero difference of the two endpoint heads, or
stores their common head literally.  When the midpoint difference is zero,
the decorated vectors are equal up to sign; the refined route stores that
literal vector and the sign instead of merging the entire zero class.  Its
target has at most \(6NS\) cells, and every key recovers the two selected
literal heads of \(A\).  If \(\mu\) is its load, the single size-biased
estimate

\[
 \sum\mu^2\le N^{o(1)}M_K
\]

implies \(M_K\le N^{1+o(1)}S\), and the preceding \(D^2\) charge then gives
the full adaptive-tail bound.  Exact size-biased loads on the displayed
genuine stresses range from \(1.0066\ldots\) to \(5.5582\ldots\); the
within-cell maximum is at most six through Costas 37.  At Costas 31 the
zero-route refinement lowers the global maximum from 256 to 63.  Global
maximum load still grows, so the moment form is essential.  Read
ENDPOINT_CROSS_SWITCHED_COLLISION_CHARGE.md and run its verifier.  The
moment theorem remains unproved, but its fixed key is now the most localized
endpoint-sensitive extraction target.

There is now a quantified fallback between the verified charge and the
cube-root theorem.  If the refined pointwise load satisfies only

\[
 \max\mu\le N^{\rho+o(1)},
\]

then the same two Cauchy--Schwarz steps give

\[
 \mathcal E_\perp(D)\le N^{1+\rho/4+o(1)}S.
\]

For a distance-Sidon subset of the \(m\times m\) grid this implies

\[
 |A|\le m^{2/(3-\rho/4)+o(1)}.
\]

In particular, the weaker pointwise target
\(\max\mu\le|A|^{1+o(1)}=N^{1/2+o(1)}\) already gives the ambient-grid
exponent \(8/23=0.347826\ldots\), compared with the desired \(1/3\).
The finite stresses are consistent with a linear endpoint load, but the
obvious inversion is false: even after conditioning on both selected endpoint
tails, transformed Costas sets of sizes 13, 17, and 19 have residual
multiplicities 3, 5, and 8.  Read
ENDPOINT_LOAD_EXPONENT_INTERPOLATION.md and run its verifier.  This does not
prove a new unconditional exponent; it supplies a rigorous intermediate
win condition and rules out the simplest bounded-to-one proof.

There is also a new coarser second-collision gate with a larger permitted
load.  Charge a pair of records in one first-stage cell literally to

\[
 \Theta_0(\gamma,\gamma')=(R_0(\gamma),R_3(\gamma'))\in D^2
\]

and write \(\nu\) for its load.  The estimate

\[
 \sum\nu^2\le (S/N)N^{o(1)}M_K
\]

already implies \(M_K\le N^{1+o(1)}S\), hence closes the cube-root tail.
This is an exact \(K=S/N\)-scaled product theorem for two coupled
cross-cell overlaps.  It is also exactly what results if the midpoint
difference and endpoint heads are taken from the same roles: those data
recover both literal decorated vectors, after one common-tail route, so no
extra endpoint information remains.  The normalized moment is at most
\(4.73\) on the stored complete-difference stresses through Costas 23, but
is approximately \(43,417,2331\) on radial transversals of sides 4, 6, 8.
Thus this formulation gives a very sharp complete-difference separation,
but the \(K\)-scaled overlap theorem is unproved.  Read
SAME_MIDPOINT_LITERAL_D2_COLLISION_GATE.md and run its verifier.

The literal gate now has an exact endpoint refinement.  For a literal key
\((x,y)\), refine each collision by the complementary head pair
\((\alpha,\beta)\), write \(\sigma(x,y,\alpha,\beta)\) for the joint load,
and let \(H_{x,y}\) count the head pairs actually used.  Then

\[
 \sum\nu^2\le
 \mathcal W:=\sum_{x,y}H_{x,y}\sum_{\alpha,\beta}\sigma^2.
\]

It is therefore enough to prove the two more localized estimates

\[
 \sum\sigma^2\le N^{o(1)}M_K,
 \qquad
 \mathcal W\le K N^{o(1)}\sum\sigma^2.
\]

The first is endpoint completion; the second is ordinary-support expansion
of the size-biased head set.  Through Costas 23 their normalized factors are
at most \(1.854\) and \(3.448\), respectively.  The maximum joint load still
grows (to 18), and the maximum pointwise head diversity grows to 91, so both
statements genuinely need their size-biased forms.  This is the sharpest
current decomposition of the \(K\)-scaled theorem.

The local endpoint-completion half has now been refined again.  Charge a
collision literally to

\[
 (R_0(\gamma),R_3(\gamma'),R_4(\gamma),R_2(\gamma'))
 =:(x,y,d,f)
\]

and write \(\tau\) for its load.  A fixed key has no hidden cell variables:
it is parametrized by two popular shifts \((p,q)\), while the second pair is
the explicit affine transform

\[
 p'=p+J(d+f-x-y-q),\qquad q'=p'+x+q-f.
\]

Both literal projections are exactly injective inside one first-stage cell.
Moreover the endpoint-completion moment \(\sum\sigma^2\) is bounded by a
size-biased tail-diversity majorant over \(\sum\tau^2\).  On genuine Costas
stresses through size 36, the factors \((\sum\tau^2)/M_K\) and the remaining
tail factor are at most `1.8055` and `1.4137`; on radial transversals the
first factor grows `2.94, 8.55, 21.19`.  Thus the preferred local target is
now the four-literal cross-cell moment plus tail diversity, while the
separate size-biased head-diversity theorem above remains necessary for the
full \(K\)-scaled gate.  Read `FOUR_LITERAL_ENDPOINT_COMPLETION_GATE.md` and
run its verifier.

A tempting direct compression of the same literals is now audited and
should not be retried without a new ingredient.  The key
\((d+f,\chi(d,f))\) has the optimal \(O(NS)\) target and recovers \((d,f)\)
with ambiguity at most two, by additive Sidonicity of the endpoint tails.
It nevertheless forgets how those literals attach to \((x,y)\): its
size-biased load grows from `5.52` at Costas 11 to `69.76` at Costas 23 and
`131.58` at Costas 29, with maximum load already `1124` at Costas 31.
Bounded ambiguity of the literal pair is therefore not bounded ambiguity of
the collision; the outer literals or the cross-switched midpoint coordinate
must remain present.

The four-literal collision now has an exact fractional-basis majorant.
For two preimages with offset `(r,s)`, retain the four popular shifts

\[
 r,\quad s,\quad r-Js,\quad r+(I-J)s.
\]

After these four forms are fixed, the full-rank graph on the remaining
`D`-forms is exactly `K_(3,5)`; every valid real `12 x 12` determinant has
absolute value one.  Removing the duplicate `s` displacement leaves an
optimal minimax `K_(3,4)` fractional basis and proves

\[
\begin{split}
 C(r,s)\le{}&R_P(r)R_P(s)R_P(r-Js)R_P(r+(I-J)s)\\
 &\cdot[R_D(s)R_D(r)R_D(Js-r)]^{1/3}\\
 &\cdot[R_D(r-s)R_D(r-(I+J)s)R_D(Jr)R_D(Jr+(I+J)s)]^{1/4}.
\end{split}
\]

The verifier checks the exact rank graph, reconstruction, and the
twelfth-power integer certificate on closure 40 and Costas 17/23.  The
summation audit is also decisive: even after restricting to offsets that
actually occur, the stronger minimum-basis sum exceeds `M_K` by factors
between `2.8e5` and `1.24e7` on these stresses.  Raw Hölder gives only
`|P|^6 N^2`, with `|P|<=min(S,N^3/S)`, far above the supercritical scale.
Thus the theorem is an inverse certificate for seven simultaneous rich
overlaps, not a standalone summation proof.  The next refinement must keep
canonical endpoint heads, Fourier compensation, or the assumption
`M_K>>S^2`.  Read `FOUR_LITERAL_FRACTIONAL_BASIS_GATE.md` and run its
verifier.

Finally, the smallest 32-record full eight-corner model was reclassified.
Its eight corner colours use only four distinct perfect matchings, so pairs
of records share two corner keys.  That cannot happen for distinct
relations in a distance-Sidon set.  The verifier now checks the exact shifts
`(28,2,28,2,21,21,22,22)`.  The canonical 256-record cube has eight
edge-disjoint matchings and remains the serious linear stress; its universal
Gaussian solution forces 96 repeated squared distances.  Consequently no
full-core counterexample is known, but no general peelability theorem is
proved either.

### New exact branch result: all generic abelian corner cores fail

The edge-disjoint translation search is now exhaustive.  A connected system
of eight commuting corner matchings is determined, up to basis change, by a
subspace of `F_2^8`.  The new integer-only verifier enumerates all 417,198
nonzero subspaces and all 50,864 simple eight-shift systems.

After discarding 22,894 systems whose six endpoint labels do not distinguish
the relation records, 27,970 full cores remain.  The universal Gaussian
solution canonically identifies point labels which are equal in every
specialization.  After that coalescing, 27,389 systems lose the full core;
each of the remaining 581 forces the formal parallelogram identity

\[
 f(x+d)-f(x)=f(x+h+d)-f(x+h)
\]

on four distinct endpoint labels.  Thus there is no generic
component-faithful distance-Sidon realization of any abelian translation
core, including cores with corner degree larger than two.  Among the 332
original exact-degree-two systems, the split is 219 empty after coalescing
and 113 forced collisions.

The former scope caveat about further nongeneric endpoint identifications is
now removed by a second exact closure.  Arbitrary corner matchings still
need not commute, so noncommuting curvature remains the live branch.  Read
`EDGE_DISJOINT_TRANSLATION_CORE_OBSTRUCTION.md` and
`NONGENERIC_TRANSLATION_CORE_OBSTRUCTION.md` and run their exact verifiers.

### New exact closure: nongeneric abelian cores also fail

For each of the 27,389 canonically empty systems, the common six-role
Fourier kernel is nontrivial; distinct translation records are therefore
forced to have the same six endpoints and cannot be distinct relations.
For each of the other 581 systems, an exact `Q(i)` partition search now
allows arbitrary additional endpoint identifications, including
cross-role identifications.  Whenever two universal squared-distance forms
coincide, distance-Sidonicity gives exactly three repairs: identify the two
unordered pairs in either orientation, or collapse both pairs to zero.
Forced point equalities are then propagated and states with a zero relation
edge, duplicate record, or two shared corner keys are rejected.

The complete search has only 793 partition states, at most five for any one
system.  Its terminal profile is 42 record collapses and 609 corner-linearity
failures; there are no survivors.  Thus **no connected commuting
edge-disjoint eight-corner core has any distance-Sidon realization**, even
under nongeneric specialization.  The search is exact Gaussian-rational
arithmetic, not modular or floating point.  This collapses the previous
dichotomy to noncommuting defects alone.

### Infinite noncommuting branch: generalized-dihedral cores collapse

The most natural transitive nonabelian replacement is also now eliminated.
For arbitrary `p,q,r>=2`, put

\[
 H=\mathbb Z_p\times\mathbb Z_q\times\mathbb Z_r
\]

and take the eight reflections `((i,j,k),1)` in the generalized dihedral
group `H semidirect {+-1}`. Right multiplication gives a linear full
eight-corner core: every corner fibre has size exactly two, the eight
matching colours are edge-disjoint, and the six formal endpoint labels
recover all `2pqr` records. The group is genuinely noncommuting as soon as
one modulus exceeds two. For pairwise-coprime moduli this is the ordinary
dihedral CRT construction with reflection indices

\[
 s_{ijk}=R^{iqr+jpr+kpq}S
\].

The endpoint face cosets are the three independent cyclic coordinates.
Evaluating the Gaussian relation on the two reflection layers and applying
separation of variables shows that every endpoint function has constant first
difference by a unit step on its cyclic factor, hence is constant.  All
records collapse to one relation.  This is an infinite exact theorem, not a
finite search. It removes the obvious generalized-dihedral/Cayley
countermodel, including non-coprime product groups, but arbitrary
noncommuting cores remain. Read
`DIHEDRAL_CRT_FULL_CORE_OBSTRUCTION.md` and run its verifier.

### Exact stability bridge: only commutator defects survive

The commuting audit now has an exact interface with arbitrary perfect
corner matchings.  For eight fixed-point-free involutions `tau_i` on a
record set `X`, put

\[
 B=\{x:\tau_i\tau_jx\ne\tau_j\tau_ix
       \text{ for some }i<j\}.
\]

All but at most `256|B|` records decompose into invariant orbits on which
the eight matchings commute.  The proof takes the union of the 256 canonical
word-preimages of `B`; outside it, adjacent swaps reduce every word to the
canonical elementary-abelian action.  Fixed-point-free edge-disjoint
matchings give eight distinct nonzero shifts on every stable orbit.

Combining this with the complete generic-plus-nongeneric translation audit
gives the sharper full-core inequality

\[
 |X|\le256|B|,
\]

because every stable orbit is now impossible.  The remaining task is to
charge commutator defects by the cubic size-biased budget.  For an internally
`t`-rich layer, arbitrary near-perfect pairing in each corner fibre leaves at
most `8|X|/t` fixed records.  The same canonical-word proof gives

\[
 |X|\le256|B|+2048|X|/t;
\]

hence `|B|>=|X|/512` for `t>=4096`.  This resolves the odd-fibre cleanup for
internally rich cores.  Extracting such a core from the externally popular
tail, or charging its boundary, remains open.

The exact 64-record controls give `(bad, contaminated)=(0,0)` for the
translation core and `(32,64)` after one admissible two-edge switch.  Thus
one local defect can contaminate a whole bounded orbit; the defect set must
be charged rather than merely deleted.  Read
`CORNER_MATCHING_COMMUTATOR_DICHOTOMY.md` and run its verifier.

The cheapest *unfiltered* pointwise defect charge is false.  Adjacent corner
colours share two selected endpoints; after fixing them, the ambient fibre
is an intersection of `A-A` with a translate of `A+JA`.  Perpendicular
integral Golomb-ruler sets give a genuine distance-Sidon family with one
such intersection of size `Omega(|A|^2)`.  This large fibre is entirely
non-transverse: the exact eight-point control has restricted unfiltered load
8, full load 12, and transverse load zero.  Restoring transversality does
not make the tested pointwise problem benign, however.  The independent
45-point source-heavy certificate in `TRANSVERSE_ROW_SOURCE_C4_GATE.md` has
filtered load `250=0.82817... k^(3/2)` at a fixed shared-endpoint pair.  This
is a finite warning, not an asymptotic counterfamily.  The rigorous safe
target is therefore the aggregate row--source moment/C4 bound; no
asymptotic filtered maximum theorem or counterexample is known.  See
`ADJACENT_CORNER_FIBRE_BARRIER.md`.

### Latest delta: the adaptive cross-pair `D^2` charge

The six-anchor route below remains valid, but a newer charge is the current
lead.  For a rich fibre `F=(u,s)`, `w=s-u`, define

\[
 X_F(q)=(u+q,w-q,w-(I+J)q)\in D^3.
\]

Every cross pair `(X_F(q)_i,X_F(p)_j)` is a fibre-injective charge into
`D^2`.  The clean fixed route

\[
 \Psi_{02}(F,q,p)=(u+q,w-(I+J)p)
\]

has load `lambda`.  The single estimate

\[
 \boxed{\sum\lambda^2\le K N^{o(1)}\mathcal O_K,
 \qquad K=S/N}                                      \tag{1.1}
\]

implies `O_K<=N^(1+o(1))S` by Cauchy in the `N^2` target.  It handles all
configurations without a common-endpoint split and permits average load
`K`, rather than `N^o(1)`.  The symmetric minimum over all nine cross pairs
is still stronger.

The incidence triple has the exact common factor

\[
 \Phi(X)=(x_0+x_1,x_2+(I+J)x_0)=(s,s+Ju),
\]

which recovers the fibre.  A full nine-projection orthogonal array is
therefore impossible; the missing step is a quantitative sparse
density-increment theorem that uses the canonical endpoints of `D=A-A`.
Abstract radial transversals falsify the same estimate by growing factors.

The exact extended verifier passes through the low-support Costas `p=47`
row: `(N,S,mass,moment,max)=(2071,23427,25194336,361029280,71)`, with
`moment/(K mass)=1.266785` and `moment/S^2=0.657823`.  Thus constant one is
false, but the exponent-level target survives.  Read
`ADAPTIVE_CROSS_PAIR_D2_CHARGE_GATE.md` and run
`verify_adaptive_cross_pair_d2_charge.py --extended`.

### Newer delta: seven-point normal form and transpose graph

The fixed charge cell now has the exact two-variable form

\[
 b,\ b+t,\ b+t+Je,\quad
 \ell,\ \ell+e,\ \ell+e+t,\ \ell+(I+J)t\in D,
\]

with adaptive-popular shifts `p=-Je` and `q=-Je-t`.  Transpose every record
to the simple bipartite edge

\[
 (b,\ell)\in D^2
 \quad\longleftrightarrow\quad
 (s,s-q)\in(D+D)^2.
\]

The endpoints recover `(u,s,q,p)` exactly, and the left-degree second moment
is precisely the missing fixed-route moment.  Every four-cycle forces

\[
 p_{ij}=p_{00}+i\alpha+j\eta,
\]

so the four secondary popular shifts form an additive parallelogram while
all seven endpoint decorations remain present.  This does not yet close the
gate: the projection from cycles to popular-shift parallelograms has maximum
multiplicity `50` on Costas 17 and `1959` on Costas 23.  Thus a popular-set
additive-energy bound alone is too lossy; the next theorem must retain the
canonical `A`-endpoints.  Read
`ADAPTIVE_CROSS_PAIR_TRANSPOSE_GRAPH.md`.  The default and extended runs of
`verify_adaptive_cross_pair_transpose_graph.py` pass, the latter checking
`676822` Costas-23 four-cycles.

### Latest delta: swap-cell component envelope

The `p<->q` involution makes the fixed charge into an undirected
multigraph.  A record and its swap join

\[
 (b,\ell)\quad\hbox{to}\quad(b+t,\ell+(I+J)t),
\]

so every component has the exact invariant

\[
 z=\ell-(I+J)b.
\]

If `h_z` is the number of active vertices in component `z` and `m_e` is a
parallel-edge multiplicity, then the fixed-route moment `M` obeys

\[
 M\le\mathcal B_{\rm sw}
 :=2\sum_z(h_z-1)\sum_{e\in E_z}m_e^2.
\]

This is an unconditional componentwise Cauchy theorem.  Moreover, fixing an
edge from `(b,ell)` to `(b+t,ell+(I+J)t)` leaves exactly the three-copy
parameter

\[
 b+t+Je,\quad\ell+e,\quad\ell+e+t\in D,
\]

with popular shifts `p=-Je`, `q=-Je-t`.  Thus the remaining theorem is the
size-biased product estimate

\[
 \mathcal B_{\rm sw}\le K N^{o(1)}\mathcal O_K,
\]

coupling active affine-component size to repeated three-copy reuse.  The
exact normalized envelope is `0.84085` on Costas 23, versus `3.9202`,
`5.9834`, and `10.9172` on radial sides 4--6.  Read
`SWAP_CELL_COMPONENT_GATE.md` and run
`verify_swap_cell_component_gate.py`.

### Latest correction: use size-biased swap orientation, not maximum degeneracy

Orient the swap multigraph by its weighted degeneracy `d_sw`.  Charge both
ordered records represented by an edge copy to its tail, with one bit to
distinguish their orientations.  This is fibrewise injective, lands in
`2N^2` cells, and has the exact bound

\[
 Q_{\rm sw}=2\sum_vd^+(v)^2
 \le d_{\rm sw}\mathcal O_K.
\]

Therefore the previously highlighted sufficient strengthening was

\[
 \boxed{d_{\rm sw}\le K N^{o(1)}}.
\]

It is no longer the preferred target.  Exact larger stresses give
`d_sw/K=1.996,1.631,2.358,2.695,3.624` on Costas primes
`29,37,41,43,47`; the last uses the low-support determinant-47 transform.
This increasing pointwise ratio is compatible with, but does not support,
an `N^(o(1))` theorem.

The charge argument only needs the strictly weaker size-biased statement

\[
 \boxed{2\sum_v d^+(v)^2\le K N^{o(1)}\mathcal O_K}.
\]

Here the edge copies may be given any orientation; the canonical peeling
orientation already has normalized values `0.500,0.352,0.361,0.440,0.739`
on those five larger Costas rows.  It therefore tolerates small dense cores
that carry negligible total mass.  This averaged orientation-energy bound,
or the fixed cross-pair moment (1.1), is the corrected lead gate.  Read
`SWAP_CELL_DEGENERACY_CHARGE.md` and run
`verify_swap_cell_degeneracy_charge.py --extended`.

The same note now records the symmetric endpoint-core form.  In a component
`z`, an edge copy between vertices `b,b'` is uniquely labelled by `a in D`,
with

\[
 q=b-a,\quad p=b'-a,\quad r=z+J(b+b'-a),
\]

and all seven members are

\[
 a,b,b',b+r,b'+r,z+(I+J)b,z+(I+J)b'\in D.
\]

This is the preferred restart form for proving the size-biased theorem.

A final coarse-relaxation audit shows why all seven memberships must remain
coupled.  If the two translated-pair conditions `b+r,b'+r in D` are dropped,
the resulting popular-correlation majorant exceeds the target normalization
by factors `2710`, `3204`, and `21873` on the exact Costas primes 11, 17,
and 23.  Thus a black-box additive-energy estimate for the adaptive popular
set cannot close the gate.  Section 7.1 of
`SWAP_CELL_DEGENERACY_CHARGE.md` gives the formula and the verifier checks
the three exact values.

### Latest support-gate audit: quadratic fibres require cubic compensation

A second exact closing target is now isolated:

\[
 |2A-(I+J)A|\ge |A|^{3-o(1)}.
\]

Because the left side is `O(m^2)` in an `m` by `m` grid, this would also
give `|A|<=m^(2/3+o(1))`.  Costas, closure, and perpendicular-ruler stresses
all have support of constant order `|A|^3`.

The naive pointwise route to this support estimate is decisively false.
For arbitrarily large integral planar distance-Sidon sets `A`, there is a
nonzero `d in D=A-A` with

\[
 |D\cap(D+(I+J)d)|=\Omega(|A|^2).
\]

The construction is an `s`-mark diagonal Golomb ruler plus one horizontal
edge whose length is a popular difference of `R-R`.  Crucially, the same
diagonal arm injects `s^2(s+1)/2=Theta(|A|^3)` triples into
`2A-(I+J)A`.  Thus heavy dilation fibres must be paid for by transverse
support growth; no subquadratic maximum-overlap theorem can work.  This is
the support analogue of the correction from maximum swap degeneracy to the
size-biased orientation-energy gate.  Read
`DILATED_PAIR_SUM_HEAVY_FIBRE_BARRIER.md` and run
`verify_dilated_pair_sum_heavy_fibre_barrier.py`.

### Cubic support is proved in the high-resonance-index affine branch

The alternate support target is now established for the main affine
low-doubling adversaries.  If `A=T A_0`, where `A_0` is vector-Sidon, put

\[
 \Gamma_T=\{z:T^{-1}JTz\in\mathbb Z^2\},\qquad
 H=|((A_0-A_0)\setminus\{0\})\cap\Gamma_T|.
\]

An exact collision calculation proves

\[
 |2A-(I+J)A|\ge
 {|A|^6\over2|A|^3-|A|^2+H(|A|^2+|A|-1)}.
\]

Thus `H<=|A|^(1+o(1))` gives cubic support.  The resonance-index formula
and a lattice-point count give

\[
 H\ll M^2/I_T+M,
 \qquad I_T=[\mathbb Z^2:\Gamma_T],
\]

for `A_0 subset [0,M]^2`.  In particular every determinant-prime affine
Welch--Costas stress has `H=O(|A|)` and satisfies the uniform explicit bound

\[
 |2A-(I+J)A|\ge |A|^3/7.
\]

This closes the generic/high-index affine branch.  The remaining support
problem is localized to low-index, nearly quarter-turn-stable additive
models.  Read `DILATED_PAIR_SUM_RESONANCE_BRANCH.md` and run
`verify_dilated_pair_sum_resonance_branch.py`.

The same note also closes the line-covered inverse branch.  If `A` has `h`
points on one affine line, those points alone give exactly
`h^2(h+1)/2` distinct values in `2A-(I+J)A`.  Hence any cover of `A` by
`|A|^(o(1))` lines already proves cubic support.  The remaining low-index
model must therefore be genuinely two-dimensional as well as sparse.

### Latest exact decomposition: spread or a Gaussian-lattice hard core

The low-index affine branch now has an exact structural decomposition.
For `A=T A_0`, put `R=T^(-1)JT` and

\[
 \Gamma=\{z\in\mathbb Z^2:Rz\in\mathbb Z^2\}.
\]

Partition `A_0` into `Gamma`-cosets with occupancies `k_C`, and let
`h=max k_C`.  Vector-Sidonicity gives the exact resonance identity

\[
 H=\sum_C k_C(k_C-1),
\]

so the collision theorem immediately yields

\[
 \boxed{|2A-(I+J)A|\ge |A|^3/(2h)}.
\]

Thus subpolynomial occupancy closes the spread branch.  In the
complementary branch, `Lambda=T Gamma` is exactly quarter-turn invariant,
so it is a rotated and scaled Gaussian lattice.  More strongly, if
`A_C=T(A_0 cap C)`, then the internal supports of distinct pieces lie in
distinct `Lambda`-cosets and therefore

\[
 \boxed{|2A-(I+J)A|\ge
        \sum_C|2A_C-(I+J)A_C|}.
\]

There is no overlap loss.  This localizes every remaining affine
counterexample to one or more literal smaller copies of the original
Gaussian-lattice problem.  It does not finish the proof: without an
ambient-sensitive density increment the largest core simply recurses to
the hard case.  Read `RESONANCE_COSET_GAUSSIAN_CORE_DICHOTOMY.md` and run
`verify_resonance_coset_gaussian_core.py`.

### Endpoint switching gives exact self-reproducing neighborhoods

There is now a compact graph form of the density-increment problem.  Put
`U=A+J(A-A)` and, for each `d in D=A-A`, take the block

\[
 B_d=A+Jd\subset U.
\]

The blocks are edge-disjoint copies of `K_k`.  If

\[
 r_x=|\{(a,d):x=a+Jd\}|
\]

and `A_x` is the set of the corresponding first endpoints, then the closed
neighborhood of `x` in the block-shadow graph is exactly

\[
 \boxed{N[x]=x+(A-A_x),\qquad |N[x]|=1+(k-1)r_x.}
\]

Thus a fibre of multiplicity `h` reproduces `h` endpoint stars with exact
local expansion `1+h(k-1)`; the only collisions are at their common centre.
This makes heavy-fibre compensation fully explicit.  The remaining global
theorem is now the assertion that these endpoint-decorated neighborhoods
cannot overlap with projective-plane efficiency.  A general linear block
system can, so the proof must retain the translate, quarter-turn, and
complete-difference decorations.  Read
`ROTATED_TRANSLATE_SELF_REPRODUCTION_GATE.md` and run
`verify_rotated_translate_self_reproduction.py`.

The same note now removes the projective-plane baseline exactly.  For
distinct outputs `x,y`, put `delta=y-x`.  Apart from their unique common
block, every common neighbor is in bijection with a pair
`(p,q) in A_x times A_y` for which

\[
 \delta+p-q=a-b\in(A-A)\setminus\{0\},
\]

with the two noncentral endpoint conditions `a!=p`, `b!=q`.  Consequently

\[
 |N[x]\cap N[y]|=k\,1_{x\sim y}+|\mathcal T_{x,y}|.
\]

The missing density increment is therefore precisely a size-biased
aggregate bound for this shifted six-endpoint count, not a bound for the
unavoidable common `K_k` overlap.

There is an exact warning: the *unweighted* aggregate is self-dual,

\[
 \sum_{\{x,y\}}|\mathcal T_{x,y}|
 =(k-1)^2\sum_z{r_z\choose2}.
\]

Every unordered pair of blocks through `z` permits exactly `(k-1)^2`
noncentral endpoint switches, and these give all transverse wedges.  Thus
ordinary inclusion--exclusion on the expanded neighborhoods simply
reproduces the original collision moment.  The next step must orient the
wedges and control a convex size-biased load, discard a structured part and
charge it to new support, or exploit genuine shrinkage of the Gaussian-core
lattice.  Do not pursue an unweighted neighborhood-overlap sum.

The exact oriented restart is now also written.  Let `H_A` be the graph on
blocks, joining two blocks when they meet, let

\[
 I=|E(H_A)|=\sum_x{r_x\choose2},
\]

and let `S_x` be the pencil of blocks through `x`.  Orient `H_A` and put

\[
 c_x=|\{d\to e:d\in S_x, e\notin S_x\}|.
\]

Propagating one base orientation across its full `(k-1)` by `(k-1)`
endpoint-switch biclique gives the exact load `(k-1)c_x` at `x`.  Therefore

\[
 |A+J(A-A)|\ge{(k-1)^2I^2\over\sum_xc_x^2}.
\]

The full support theorem follows in the collision-dominant branch from the
single pencil-orientation estimate

\[
 \boxed{\sum_xc_x^2\le I^2/k^{1-o(1)}}.
\]

This is the current smallest endpoint-sensitive density-increment gate.  A
random orientation has expected cost `sum_x(b_x^2+b_x)/4`, where `b_x` is
the unoriented pencil boundary, so independent balancing alone is
insufficient; the gain must come from a correlated orientation or a
structural split.

There is now a sharp warning about what that structural split must retain.
The punctured affine-line system in
`AFFINE_DOUBLE_ENDPOINT_ORIENTATION_BARRIER.md` has two orthogonal proper
endpoint colourings, a third proper incidence label, every pencil is a
three-coordinate matching, and every base edge has a unique pencil centre.
After deleting its diagonal cells and adjoining one isolated zero block, it
also has exactly `k(k-1)+1` blocks, every block has size `k`, and every
pencil has size at most `k`, matching the actual incidence parameters.
Nevertheless every
orientation satisfies

\[
 {k\sum_xc_x^2\over I^2}\gg k.
\]

Thus neither endpoint ordering, nor even the abstract three-coordinate
matching design, can prove the gate.  The translate identity `B_d=A+Jd`, radial
uniqueness, and complete-difference realization must remain coupled.

The same barrier note isolates one exact way characteristic zero intervenes.
Every nonidentity zero-intercept affine pencil is full and has incidence
label `p=q`.  A real full fibre of this form would give a permutation
`sigma` of `A` satisfying

\[
 \sigma(r)-x={(I-J)\over2}(r-x),
\]

a strict contraction.  Hence it is impossible unless `A` is a singleton.
This does not handle the six-distinct hard mass, but it gives a concrete
stability direction: an affine-like core must avoid every contracting
pencil or force exponential coordinate height.

The next abstraction is also settled sharply in
`THREE_PROJECTION_FIBRE_BARRIER.md`.  Real nonzero fibres

\[
 \mathcal F_x=\{(p,r,q):x=p+J(r-q),\ r\ne q\}
\]

are three-coordinate matchings, and two distinct fibres meet in at most one
cell in each of the projections `(p,r)`, `(p,q)`, and `(r,q)`.  This is an
exact consequence of oriented-difference uniqueness.  It is still not
enough: an explicit finite-field family has `Theta(k^2)` disjoint fibres,
total mass `Theta(k^3)`, and all three cross-fibre projection intersections
at most one.  Thus projective-plane efficiency survives every matching and
projection-linearity property isolated so far.  The simultaneous
characteristic-zero equation, not just its three projections, is the
minimum remaining input.

`FIBRE_CENTROID_STABILITY.md` now extracts the first quantitative consequence
of that simultaneous equation.  If a fibre has size `h=k-t` and coordinate
complements `P^c,R^c,Q^c`, then exactly

\[
 h(x-\mu)=-\sum_{P^c}(p-\mu)
 +J\left(\sum_{Q^c}(q-\mu)-\sum_{R^c}(r-\mu)\right).
\]

For `A` in a square of side `m`, all such outputs lie within
`3sqrt(2)mt/(k-t)` of the centroid.  Hence there is at most one full fibre,
and the number of fibres of size at least `k-t` is at most
`(6sqrt(2)mt/(k-t)+2)^2`.  This rigorously kills the extreme affine-plane
tail but does not yet reach the medium-rich six-distinct fibres; a
multiscale/density-increment extension is still required.

`FIBRE_PATH_CIRCULATION.md` extends the localization to genuinely medium
fibres whenever their endpoint matching is close to a circulation.  Direct
the edges `q->r` in one fibre and put

\[
 s_x=|Q_x\setminus R_x|=|R_x\setminus Q_x|.
\]

The components are directed paths and cycles, and exactly

\[
 h x=\sum_{p\in P_x}p+
 J\left(\sum_{R_x\setminus Q_x}r-
        \sum_{Q_x\setminus R_x}q\right).
\]

Thus `|x-mu_(P_x)|<=sqrt(2)m s_x/h`; every cycle label set has centroid
exactly `x`, and every path of length `ell` localizes `x` to within
`sqrt(2)m/ell` of its label centroid.  The total internal circulation has
the exact global budget

\[
 \sum_x(h_x-s_x)\le k(k-1)^2.
\]

This is the first medium-fibre consequence of the literal real equation.
It splits the next increment into high-circulation fibres, which have a
cubic global charge, and high-boundary fibres, where many path endpoints
must pay ambient height.  The exact verifier passes all 5,528 fibres of the
20-point closure witness.

`BALANCED_FOURIER_HARMONIC_GATE.md` records a tempting spectral restart that
is now **disproved**.  With `H=|hat(1_A)|^2` and `H_J=H circ J`, define

\[
 \mathfrak B(A)=\int {H^2H_J^2\over H+H_J}.
\]

The origin box proves `B(A)>=k^6/(2048m^2)`, so
`B(A)<=k^(3+o(1))` would finish the cube-root grid theorem.  Exact second
moments control every imbalanced dyadic level, initially leaving only the
balanced joint-large-spectrum power

\[
 |\{H\asymp2^ik,\ H_J\asymp2^jk\}|
 \le k^{o(1)}2^{-i-j-\min(i,j)}.
\]

The exact second moments do prove this when `i>=2j` or `j>=2i`.  Collinear
sets satisfy the valid bound `B(A)<4k^3`, and a cover by `r` parallel lines
gives `B(A)<4r^3k^3`.  But two *perpendicular* ruler arms create balanced
strips of width `s^(-2-o(1))`.  The scalable integral distance-Sidon family
from `PERPENDICULAR_RULER_OBSTRUCTION.md` satisfies

\[
 \mathfrak B(A_s)\ge s^{4-o(1)}=|A_s|^{4-o(1)},
\]

which is exponent-sharp and kills the gate by a full power.  The earlier
finite ratios below `1.10k^3` were pre-asymptotic: the rigorous lower bound
has a small constant and lives on Fourier strips represented by only one or
two rows in those computations.  A spectral successor must distinguish two
biases supported by the same two-dimensional endpoint core from biases
supplied by different perpendicular arms.

### Latest delta: size-biased eight-corner core

`SIZE_BIASED_EIGHT_CORNER_GATE.md` returns to the global transverse relation

\[
 d=f+Je,
 \qquad d,e,f\in A-A,
 \qquad d\cdot e\ne0.
\]

Decorate the three directed edges by their unique endpoint pairs.  Every
relation has eight corner projections selecting one endpoint of each pair.
If `deg_epsilon` is the corresponding projection degree, put

\[
 \delta(\rho)=\min_\epsilon
 \deg_\epsilon(\pi_\epsilon\rho),
 \qquad
 \mathcal C(A)=\sum_\rho\delta(\rho).
\]

The old pointwise claim `max delta=N^(o(1))` is false, but the proof only
needs the size-biased estimate

\[
 \boxed{\mathcal C(A)\le |A|^{3+o(1)}.}
\]

Equivalently, if `R_(>=t)` consists of the relations popular to level `t`
in all eight projections, it suffices to prove

\[
 |R_{\ge t}|\le |A|^{3+o(1)}/t
\]

uniformly.  The layer-cake identity is exact.  On the 30-, 45-, 60-, 90-,
and 120-point closure witnesses, `(max delta,C/k^3)` is respectively

\[
 (5,1.5443),\ (6,2.0990),\ (8,2.2123),\
 (9,2.7686),\ (12,3.5780).
\]

This is compatible with a logarithmic loss while the local maximum grows.
It is a new sufficient gate, not a proof.

The tempting route from `FIBRE_PATH_CIRCULATION.md` to this estimate has
also been audited sharply.  The stored closures put every relation at a
path endpoint in one of four translation-invariant corner fibres.  That is
not a theorem: a new explicit 22-point integer distance-Sidon set contains
one transverse base relation and eight certified completions which make it
internal in all four fibres.  All 231 squared distances are distinct.  Thus
path boundary cannot replace the global rich-core estimate; the aggregate
endpoint cost remains load-bearing.  The new verifier checks both the
22-point barrier and every profile above exactly.

### Newest correction: four fixed-role corners do have stopping sets

The simultaneous peeling phenomenon seen on all stored closures does **not**
survive after reducing from eight corners to the four corners which fix one
endpoint role.  `FOUR_CORNER_STOPPING_SET_BARRIER.md` gives an explicit
31-point integer distance-Sidon set and sixteen transverse relations with a
common `c_0=p` such that every key `(a_i,b_j,p)` occurs exactly twice.

The construction was first found as four edge-disjoint perfect matchings on
sixteen relation records.  Its 31 point labels are Gaussian-linear forms,
and all 465 formal squared-distance polynomials are distinct.  The stored
integral specialization has all positive distances distinct.  On its full
transverse relation set the exact core profile is

\[
 (|R|,|\operatorname{core}_{0,1,2,3}|,
 |\operatorname{core}_{4,5,6,7}|,
 |\operatorname{core}_{0,\ldots,7}|)
 =(584,32,32,0).
\]

Thus the attractive `4k^3` fixed-endpoint peeling proof is false.  The full
eight-corner core remains empty on this new adversary as well as every older
stress, so an eight-sided peeling or size-biased tail theorem remains live.
Do not invoke Latin-bitrade torsion to rule out the four-corner system: the
explicit generic realization is a positive-genus trade counterexample.
Run `verify_four_corner_stopping_set_barrier.py` for the exact certificate.

### Full eight-corner linear audit: combinatorial cores exist, geometry kills them

`FULL_EIGHT_CORNER_LINEAR_CORE_AUDIT.md` tests the natural next conjecture.
A full eight-corner stopping set is **not** forbidden by the bare fibre
axioms alone.  There is an exact 32-record translation core over `F_2^5`:
every corner key occurs twice and all six endpoint labels jointly recover
the record.  However, its eight corner colours use only four distinct
perfect matchings, so it is non-linear and already impossible for a
distance-Sidon relation system.  Its universal `Q(i)` solution has 24
distinct point labels and also forces sixteen repeated squared-distance
polynomials.  The canonical 256-record cube has eight edge-disjoint
matchings and forces 96 repetitions.

An apparent zero-collision 128-record model was invalid: its six endpoint
maps have total rank six instead of seven, so the records collapse in pairs
to only 64 distinct relations.  The search now checks total rank before
accepting a hit, and the verifier stores this as a regression.

The precise surviving possibility is therefore:

> every full eight-corner stopping set which satisfies the Gaussian relation
> and has distinct relation records forces a repeated Euclidean distance.

This remains unproved outside the audited linear models.  Even if true, it
only supplies the transverse cubic estimate; the size-biased tail remains
the safer target because it permits small stopping sets.  Run
`verify_full_eight_corner_linear_core_audit.py` for all exact ranks, equations,
and Hermitian norm identities.

## 2. What was accomplished in the latest attack

For a distance-Sidon set `A` in an `m` by `m` grid, put

\[
 D=A-A,\qquad N=|D|,\qquad S=|D+D|,
\]

and let `J` be quarter-turn.  The earlier reductions show that the grid
upper bound follows from the adaptive off-diagonal estimate

\[
 \mathcal O_K\le N^{1+o(1)}S.                 \tag{2.1}
\]

The last several hours replaced a loose cross-fibre charge first by an exact
two-anchor graph and then by a swap-coupled six-anchor charge.

For each normal seven-incidence configuration, endpoint realization gives

\[
 \omega=(\epsilon(c),m(u)-m(c)),\qquad
 \ell=w-(I+J)q,\qquad z=w-q.
\]

For fixed `omega`, make a bipartite multigraph `G_omega` with an edge
`(ell,z)` for every configuration.  Orient each edge toward its endpoint of
smaller global degree, with a fixed tie rule.  This charge is injective
inside every rich fibre.  Together with two exact common-endpoint routes it
lands in at most `10NS` labelled keys.

The decisive exact inequality is

\[
 \sum\lambda_N^2\le
 \mathcal B_N:=\sum_\omega\sum_{e=(\ell,z)\in G_\omega}
 \min\{d_L(\ell),d_R(z)\}.                    \tag{2.2}
\]

This is not an estimate: it follows directly from the orientation.  It
removes the one-sided star obstruction which defeated the prior charge.
Any large normal second moment must now contain many edges with both anchor
degrees large in the same endpoint-midpoint graph.

The two fixed-anchor collision systems are also exact.  If two preimages
differ by `(eta,rho,pi)` in their base and two popular shifts, then the
left-anchor displacements are

\[
 \eta,\ \eta+\rho,\ \eta+\pi,\ 0,\ J\rho,
 (I+J)\rho-\pi,\ (I+J)(\rho-\pi),              \tag{2.3}
\]

and the right-anchor displacements are

\[
 \eta,\ \eta+\rho,\ \eta+\pi,\ 0,\ \rho-\pi,
 -J\rho,\ \rho-(I+J)\pi.                      \tag{2.4}
\]

The common endpoint decoration forces `eta`; it is not a free affine
variable.  Both adaptive-popular shifts remain present.

Every ordered configuration `(q,p)` also contains its swapped partner
`(p,q)`.  Retaining both endpoint labels produces four `w`-anchor cells,
and the two moving heads produce two more cells.  Charging to the least
popular of these six candidates gives a new exact envelope

\[
 \sum\lambda_6^2\le\mathcal B_6\le\mathcal B_N.               \tag{2.5}
\]

The complete charge remains fibrewise injective and now lands in at most
`18NS` labelled keys.  The larger constant is irrelevant; the substantial
gain is that a counterexample must be simultaneously dense under the swap
involution, both endpoint labels, four paired anchors, and two heads.

## 3. The older six-anchor theorem still missing

The strongest endpoint-normal target from the preceding route is

\[
 \boxed{\mathcal B_6\le N^{o(1)}|C_N|.}        \tag{3.1}
\]

where `C_N` is the multiset of normal configurations.  The two
common-endpoint charge routes need the analogous size-biased load bound.
Together these imply

\[
 \sum\lambda^2\le N^{o(1)}\sum\lambda,
\]

and Cauchy in an `18NS` target gives (2.1), hence

\[
 |A|\le m^{2/3+o(1)}
\]

and the full expected upper order for #1208.

No proof of (3.1) is known.  It remains a valid fallback, but the cross-pair
gate (1.1) is now preferable because it permits the adaptive factor `K` and
also absorbs the common-endpoint branch.  The older sufficient target for
`B_N` and its arboricity reduction remain valid as well, since `B_6<=B_N`.

For the older target there is a sharper sufficient gate.  Let `a(G_omega)` be
the multigraph arboricity.  Orienting a forest decomposition gives

\[
 \mathcal B_N\le
 2\sum_\omega a(G_\omega)|E(G_\omega)|.         \tag{3.2}
\]

Hence subpolynomial size-biased arboricity still suffices.  Moreover,
if `r_{ell,z}` is the parallel-edge multiplicity, then exactly

\[
 \mathcal B_N=
 \sum_{\omega,\ell,z}r_{\ell,z}^2+\mathcal M_3, \tag{3.3}
\]

where `M_3` is a selectable family of genuine three-edge paths, with one
nonparallel continuation at each endpoint of a central edge.  Read
`BALANCED_ANCHOR_ARBORICITY_GATE.md` for the proof and exact path forms.

## 4. Verification status

Run

```bash
python3 phase2/loop/erdos1208/verify_hybrid_endpoint_opposite_charge.py \
  --extended --literal-max
```

It verifies all charge identities, fibrewise injectivity, the original,
swapped, and fixed-head collision systems, the exact six-anchor degree
envelope (2.5), the common-endpoint resonance identities, and the negative
literal-charge control.

The complete stored suite passes for the swap-coupled charge.  Selected
profiles
`(mass,image,second moment,max load)` are:

| family | profile | size-biased load |
|---|---:|---:|
| closure 40 | `(370516,369663,372254,4)` | `1.00469076` |
| closure 80 | `(357094,356998,357286,2)` | `1.00053767` |
| Costas 23 | `(498674,493947,508164,3)` | `1.01903047` |
| Costas 41 | `(4629690,4525615,4843428,4)` | `1.04616681` |
| Costas 43 | `(8451318,8271632,8815150,4)` | `1.04305033` |

The previous one-anchor Costas-43 profile had load `1.7644` and maximum
`12`; the two-anchor version had `1.2223` and `7`.  The improvement is
evidence that swap coupling removes the observed concentrations, not
evidence that (3.1) holds asymptotically.  Read
`SWAP_COUPLED_SIX_ANCHOR_CHARGE.md` for the new exact reduction.

## 5. Other durable progress and barriers

The direct attack has accumulated substantially more than the final gate:

- Complete rectangular, Gaussian-ideal, and arbitrary oblique lattice
  fibres were classified.  The sharp universal height threshold is
  `M >> r^(3/2)`, exactly the exponent needed at the cube-root scale.
- Sparse oblique shear examples show that even near-full density, three
  affine fibre copies, radial uniqueness, and adaptive bidirectional
  popularity do not recover a polynomial complete patch.  Complete
  difference endpoint realization is indispensable.
- The orthogonal-energy problem was localized to a support-adaptive rich
  tail.  High-support Ruzsa branches, collinear branches, algebraic-curve
  branches, generic affine images, and large resonance-index branches were
  separated or closed.
- Opposite-endpoint charges, exact popular-pair rectangles, an eight-form
  fractional-basis collision majorant, and endpoint-switched two-moment
  charges remain valid fallback interfaces.
- The common-endpoint fallback is not arbitrary: a repeated local midpoint
  class forces three affine copies in `A` and squared-distance ratio
  `1:1/4:5/4`.
- Generic hypergraph codegrees, anti-Ramsey proper coloring, raw distance
  energy, DRC, additive BSG, ordinary third energy, two-sided sumset
  products, scalar Bessel positivity, full directional PSD relaxations,
  and density-to-complete-patch extraction were all shown to bottom out at
  the grid or admit explicit countermodels.  Do not restart these lanes
  without a new endpoint-sensitive ingredient.
- The positive-definite audit is now exponent-sharp as a barrier: exact
  mixed quarter-turn moments, universal radial kernels, support-packing
  duals, and scalar Bessel shell inequalities all admit genuine examples
  or pseudoconfigurations too large for a cube-root proof.  Any successful
  spectral argument must use nonlinear endpoint compatibility, not only
  positivity or the occupied-radius set.
- The prime-power upper framework has a local entropy barrier and will not
  reach exponent `1/3`; its value is the separate explicit partial result
  `0.494586`.

## 6. Best next move

First attack the explicit dilated resonance estimate (1.2).  It is the
current sharpest mechanism for the internal pair-sum shift estimate (1.1):
the zero-resonance branch is already exact, and all remaining loss is the
single off-diagonal sum in (1.2).  A high-energy inverse theorem should
produce a rank-one or rank-two model for the pair-sum set.  The rank-one
branch is now closed by the projection theorem (1.3).  The finite-field
parabola barrier proves that the rank-two branch must retain Euclidean norm
uniqueness, rather than stopping at ordinary small doubling, vector-Sidon
pair sums, or design rank.  The clean next statement is a supersaturation
theorem: a polynomial excess in (1.2) forces two distinct endpoint
differences of equal Euclidean norm.  For a distance-Sidon set that
conclusion is impossible.  By (1.5), it is enough to prove this only after
two endpoint roles and labels have acquired codegree
`>(|H_q|+k)k^epsilon`; the diffuse and small-fibre branches are already
closed.

Equivalently, one may prove the weaker aggregate clean form

\[
 \sum_{q\in(A-A)^*}h_6(q)
 \le k^{o(1)}(k^2+m^2).                         \tag{6.1}
\]

This is now the shortest exact closing gate.  It has already subtracted the
`k-2` forced star at every internal shift, and vector-Sidonicity turns every
remaining clean arrow into one equal-centroid collision.  A successful
argument must use Euclidean norm injectivity as well as vector-Sidonicity:
an unstretched finite-field parabola has the same additive pair-sum
structure but is false at the desired geometric scale.  The natural target
is an injection or bounded-multiplicity charge from a clean arrow together
with two independent endpoints of `A` into an `O(m^2)` ambient record.
Simple translated-copy records in `A+JA` are not injective on the stored
closure and Costas stresses, so this last step still requires a new
quadratic endpoint invariant.

The fixed jointly rich-tail estimate remains the best developed fallback:

\[
 \sum_{\substack{q\ne0\\R(q)>\sqrt N,\ R(Jq)>\sqrt N}}
 R(q)R(Jq)
 \le N^{5/2+o(1)}+m^{2+o(1)}N.                \tag{6.2}
\]

Do not spend effort on the zero shift, repeated
endpoints, or a translation that is light in either orthogonal direction;
those cases have rigorous fifth-power bounds.  The remaining records have
eight distinct endpoints and simultaneous rich reuse under both `q` and
`Jq`.  Decompose this population into an approximately one-dimensional
part, paid by `m^2N`, and a genuinely two-dimensional endpoint-reuse part,
paid by `N^(5/2+o(1))`.  The commutator, opposite-endpoint, and
Fourier-compensation tools are candidate inputs to that decomposition.

In parallel, retain the earlier endpoint-sensitive density increment inside
the Gaussian core and the older `K`-scaled cross-pair estimate as the more
detailed aggregate closing gate.

At the support-adaptive resolution, the corresponding target remains the
endpoint cross-switched collision moment
\(\sum\mu^2\le N^{o(1)}\sum\mu\).  A fixed nonzero head route recovers two
ordered endpoints by vector-Sidonicity, while the common-head route records
the shared endpoint.  Treat this as the detailed mechanism for (6.2), not
as a separate objective: dyadically prune a hypothetical heavy rich-tail
key by the remaining midpoint-difference representations, and apply the
Fourier compensation lemma only after those two heads have been fixed.

1. Use the exact resonance-coset decomposition to prune to a large
   quarter-turn-stable lattice core.  Track its index, scale, and ambient
   endpoint contribution; a recursion on core size alone is too weak.
2. On the surviving core, use the exact neighborhoods
   `N[x]=x+(A-A_x)`.  Prove a size-biased overlap bound after removing the
   unique common-block contribution.  The unweighted overlap total is
   exactly self-dual and gives no gain, so an orientation or convex pruning
   is mandatory; this is the graph version of the endpoint density
   increment.
3. Prefer the pencil-cut formulation: orient the block-intersection graph
   so that `sum_x c_x^2<=I^2/k^(1-o(1))`, but retain the full translate and
   complete-difference geometry.  The affine endpoint barrier proves that
   even two endpoint colourings plus a third proper incidence label miss by
   a full factor `k`; a random orientation likewise reaches only the
   fractional boundary cost.
4. Write every record as two incidence triples `X,Y in D^3` with
   `Phi(X)=Phi(Y)`.  Dyadically prune on the nine cross-coordinate
   codegrees at the natural threshold `tK`.
5. Use the covariance identity for the two components of `Phi` to force a
   nonquasirandom cross projection in every nonconstant-fibre core.
6. Turn that correlation into a density increment while retaining all nine
   cross projections.  Dense regularity is insufficient; the loss must be
   polynomially controlled at the sparse `K` scale.
7. The radial transversals show that the affine/common-factor system alone
   is false.  The increment must use the endpoint identity
   `widehat(1_D)=|widehat(1_A)|^2-(|A|-1)` or an equivalent combinatorial
   endpoint-switch statement.
8. The fixed-route collision displacements are
   `delta,0,delta+pi,delta+(I+J)pi,Jpi,(I+J)(delta+pi),0`.
   Any inverse theorem must retain all five nonzero translated copies.
9. If the fixed route develops a genuine counterexample, use the symmetric
   minimum over all nine `D^2` charges, then combine it with the six
   endpoint-anchor candidates by normalized minimum degree.
10. Do **not** attack the scalar balanced Fourier tail: perpendicular rulers
    disprove it by a full power.  Any spectral successor must decompose the
    contributing endpoint pieces before balancing the two orthogonal
    biases.  The physical-space `FIBRE_PATH_CIRCULATION.md` split remains
    valid: a medium fibre either has chargeable circulation or exposes many
    path endpoints whose lattice height must be tracked.
11. In the transverse formulation, prefer the size-biased eight-corner tail
    `|R_(>=t)|<=k^(3+o(1))/t` over the false pointwise corner bound.  Do not
    assume that one of the four same-sign corner fibres exposes the relation
    at a path endpoint: the 22-point internal-fibre certificate disproves
    that shortcut.  A proof must charge a *population* of simultaneously
    rich relations, not one locally affine relation.

The practical progress estimate is: the construction/verification
architecture is mature, and the full conjecture is reduced to one crisp
new combinatorial inequality, but that inequality is of genuinely
open-problem strength.  We are meaningfully closer to a proof route, not
close enough to predict completion.

## 7. Late delta: a genuinely metric scalar charge

The newest quadratic endpoint invariant is now formalized.  For the unique
edge `{a,b}` represented by a pair sum `s=a+b`, put

\[
 \delta(s)=|a-b|^2.
\]

Distance-Sidonicity makes `delta` injective.  For a clean fibre `H_q`, the
scalar charge

\[
 \Phi_{q,C}(s,t)=\delta(s)+C\delta(t),
 \qquad (s,t)\in H_q\times\Sigma,
\]

has only `O_C(m^2)` integer keys.  Its exact energy is

\[
 \mathcal M_{q,C}
 =\sum_r r_{\delta(H_q)-\delta(H_q)}(Cr)
          r_{\delta(\Sigma)-\delta(\Sigma)}(-r),
\]

with diagonal exactly `|H_q| binom(k,2)`.  Therefore the near-diagonal
estimate

\[
 \mathcal M_{q,C}\le m^{o(1)}|H_q|\binom k2
\]

would prove the cube-root theorem.  It is rigorous for collinear sets by
the divisor bound for `x^2+C y^2`.

Globally it is enough to prove the weaker sharp version

\[
 \mathcal M_{q,C}\le m^{o(1)}\binom k2(|H_q|+k),
\]

because all fibres with `|H_q|<=k` contribute only `O(k^3)` clean starts
and require no amplification.

The repeated-edge-label part is now completely removed.  For `C!=1`, an
off-diagonal scalar collision using only three distinct unordered edges must
repeat one edge across a source/arbitrary role.  Choosing the two source
edges determines the remaining distance label, so all four possible repeat
patterns contribute at most `4|H_q|^2<=4|H_q|N`.  Hence every unresolved
scalar collision has four distinct distance labels.  In the stored closure
data, most of this residual already has eight distinct edge endpoints.

All stored stresses pass for `C=18`.  The normalized energies are
`1.0414,1.1478,1.1130,1.1290` on closures `30,40,80,120`, `1.00165` on the
perpendicular ruler, `1.06723` on Costas 22, and `1.01762` on the transformed
parabola 43.  A new scalable stress is the exact distance-Sidon integer
parabola `{(j,j^2)}`: the distance `u^2(1+v^2)` recovers `uv` as its square
root floor and then recovers the endpoint pair.  Scalar-charge loads stay
at most three through size 50.  Read `METRIC_SCALAR_PAIR_SUM_CHARGE.md` and
run its verifier.  The scalar energy theorem remains unproved.

A nearby direction-sensitive charge has been killed completely.  For every
fixed integer `C`,

\[
 B_C(u,v)=u\cdot v+C\det(u,v)
\]

admits an infinite distance-Sidon two-arm obstruction.  A fixed six-point
clean gadget supplies one source vector `u`; a large Golomb-ruler arm is
placed in the integral null direction of `B_C(u,.)`.  The zero fibre then
has `Theta(k^2)` records and the normalized energy is `Omega(k^2)`.  The
finite 14-point certificates for `C=1,3,18,43` all have profile
`(mass,energy,max load)=(91,1183,28)`.  Read
`BILINEAR_EDGE_CHARGE_BARRIER.md`.  Do not restart fixed dot/determinant
charges; the distance-label charge survives precisely because ruler edge
lengths remain distinct.

## 8. Latest delta: a Gaussian charge on oriented edge displacements

There is now a second genuinely metric-aware direct gate which is cleaner
on the stored stresses.  For each uniquely represented pair sum `s`, order
its two endpoints lexicographically and let `u(s)` be the resulting edge
displacement.  For a clean fibre `H_q`, put

\[
 \Gamma_q(s,t)=u(s)+3(I+J)u(t),
 \qquad(s,t)\in H_q\times\Sigma.
\]

Writing `U=u(Sigma)` and `U_q=u(H_q)`, its exact energy is

\[
 \mathcal G_q
 =\sum_w r_{U-U}(w)r_{U_q-U_q}(-3(I+J)w),
\]

with diagonal exactly `|H_q| binom(k,2)` and at most `(14m+1)^2` vector
keys.  Therefore

\[
 \mathcal G_q
 \le m^{o(1)}\binom k2(|H_q|+k)
\]

would prove `k<=m^(2/3+o(1))` by the same clean-fibre summation.

The squared norm of this key is

\[
 |u(s)+3(I+J)u(t)|^2
 =|u(s)|^2+18|u(t)|^2+2u(s)\cdot3(I+J)u(t).
\]

Thus it combines the live metric scalar charge with the directional term
that was previously tested only in isolation.  Gaussian circle divisor
bounds show that the scalar norm energy lies between the vector energy and
`m^(o(1))` times it, so the two formulations are exponent-equivalent.

The exact normalized vector energies are `1.0148,1.0522,1.0423,1.0507` on
closures `30,40,80,120`, `1.0211` on source 45, `1.03095` on the affine
parabola, and `1.0133,1.0155` on the integer parabolas of sizes 50 and 120.
The charge is exactly injective on both the perpendicular-ruler 40 and
Costas 22 stresses.  Maximum loads never exceed four in the table.

Radial uniqueness alone is decisively insufficient: the canonical set
containing one lattice representative per occupied radius has
`M^(2-o(1))` vectors in an `M`-box, while its Gaussian sumset has only
`O(M^2)` keys, forcing almost cubic vector energy.  Thus any proof of the
new gate must use both complete endpoint realizability and the clean
`H_q` decoration.  Read `GAUSSIAN_EDGE_VECTOR_CHARGE.md` and run its exact
verifier.

One tempting shortcut has also been closed.  The ordinary additive energy
of the full squared-distance label set `D=delta(Sigma)` always satisfies

\[
 E^+(D)\ge {|D|^4\over4m^2+1}.
\]

At the cube-root scale its normalized energy is therefore at least
`m^(2/3-o(1))`.  Consequently one cannot prove the metric scalar gate by
Cauchy followed by separate low-energy estimates for `delta(H_q)` and
`delta(Sigma)`, nor by a black-box bounded-rank distance-matrix energy
theorem.  The dilation-specific correlation must remain coupled.  See
`METRIC_SCALAR_SEPARATE_ENERGY_BARRIER.md`.

The preceding recommendation is now retracted: both vector estimates are
false by the two-arm construction recorded near the top of this handoff.
The best immediate proof target is the coupled metric scalar correlation in
Section 7.  Its collinear branch is proved, its repeated-label collisions are
diagonal-scale, it passes all stored stresses, and the new resonant family
reduces to a positive binary quadratic form rather than defeating it.  The
required four-distinct-distance correlation theorem is still unproved and
is of genuine open-problem strength; #1208 remains open.
