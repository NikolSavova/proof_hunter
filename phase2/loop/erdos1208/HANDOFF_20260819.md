# Erdős #1208 handoff — 2026-08-19

## 1. Bottom line

The problem is not solved.  The published/preprint window is

\[
 n^{1/3}\ll F_2(n)\ll n^{1/2-\varepsilon}.
\]

The project has two distinct achievements:

1. a candidate explicit partial upper improvement
   \(F_2(n)\ll n^{0.494586}\), with a finite certificate and two declared
   external arithmetic inputs still requiring expert audit; and
2. a much more ambitious direct route which would prove the expected
   square-grid order \(F_2(n)=n^{1/3+o(1)}\) if one remaining
   endpoint-overlap theorem can be established.

Do not describe either item as a solution of #1208.  Read
`HANDOFF_20260813.md` for the literature chronology and first prime-power
certificate, then `HANDOFF_20260817.md` for the full upper-construction and
direct-attack history.  This file is the current delta and restart point.

### Latest delta: the adaptive cross-pair `D^2` charge

The six-anchor route below remains valid, but a newer charge is the current
lead.  For a rich fibre `F=(u,s)`, `w=s-u`, define

\[
 X_F(q)=(u+q,w-q,w-(I+J)q)\in D^3.
\]

Every cross pair `(X_F(q)_i,X_F(p)_j)` is a fibre-injective charge into
`D^2`.  The clean fixed route

\[
 \Psi_{02}(F,q,p)=(u+q,w-(I+J)p)
\]

has load `lambda`.  The single estimate

\[
 \boxed{\sum\lambda^2\le K N^{o(1)}\mathcal O_K,
 \qquad K=S/N}                                      \tag{1.1}
\]

implies `O_K<=N^(1+o(1))S` by Cauchy in the `N^2` target.  It handles all
configurations without a common-endpoint split and permits average load
`K`, rather than `N^o(1)`.  The symmetric minimum over all nine cross pairs
is still stronger.

The incidence triple has the exact common factor

\[
 \Phi(X)=(x_0+x_1,x_2+(I+J)x_0)=(s,s+Ju),
\]

which recovers the fibre.  A full nine-projection orthogonal array is
therefore impossible; the missing step is a quantitative sparse
density-increment theorem that uses the canonical endpoints of `D=A-A`.
Abstract radial transversals falsify the same estimate by growing factors.

The exact extended verifier passes through the low-support Costas `p=47`
row: `(N,S,mass,moment,max)=(2071,23427,25194336,361029280,71)`, with
`moment/(K mass)=1.266785` and `moment/S^2=0.657823`.  Thus constant one is
false, but the exponent-level target survives.  Read
`ADAPTIVE_CROSS_PAIR_D2_CHARGE_GATE.md` and run
`verify_adaptive_cross_pair_d2_charge.py --extended`.

### Newer delta: seven-point normal form and transpose graph

The fixed charge cell now has the exact two-variable form

\[
 b,\ b+t,\ b+t+Je,\quad
 \ell,\ \ell+e,\ \ell+e+t,\ \ell+(I+J)t\in D,
\]

with adaptive-popular shifts `p=-Je` and `q=-Je-t`.  Transpose every record
to the simple bipartite edge

\[
 (b,\ell)\in D^2
 \quad\longleftrightarrow\quad
 (s,s-q)\in(D+D)^2.
\]

The endpoints recover `(u,s,q,p)` exactly, and the left-degree second moment
is precisely the missing fixed-route moment.  Every four-cycle forces

\[
 p_{ij}=p_{00}+i\alpha+j\eta,
\]

so the four secondary popular shifts form an additive parallelogram while
all seven endpoint decorations remain present.  This does not yet close the
gate: the projection from cycles to popular-shift parallelograms has maximum
multiplicity `50` on Costas 17 and `1959` on Costas 23.  Thus a popular-set
additive-energy bound alone is too lossy; the next theorem must retain the
canonical `A`-endpoints.  Read
`ADAPTIVE_CROSS_PAIR_TRANSPOSE_GRAPH.md`.  The default and extended runs of
`verify_adaptive_cross_pair_transpose_graph.py` pass, the latter checking
`676822` Costas-23 four-cycles.

### Latest delta: swap-cell component envelope

The `p<->q` involution makes the fixed charge into an undirected
multigraph.  A record and its swap join

\[
 (b,\ell)\quad\hbox{to}\quad(b+t,\ell+(I+J)t),
\]

so every component has the exact invariant

\[
 z=\ell-(I+J)b.
\]

If `h_z` is the number of active vertices in component `z` and `m_e` is a
parallel-edge multiplicity, then the fixed-route moment `M` obeys

\[
 M\le\mathcal B_{\rm sw}
 :=2\sum_z(h_z-1)\sum_{e\in E_z}m_e^2.
\]

This is an unconditional componentwise Cauchy theorem.  Moreover, fixing an
edge from `(b,ell)` to `(b+t,ell+(I+J)t)` leaves exactly the three-copy
parameter

\[
 b+t+Je,\quad\ell+e,\quad\ell+e+t\in D,
\]

with popular shifts `p=-Je`, `q=-Je-t`.  Thus the remaining theorem is the
size-biased product estimate

\[
 \mathcal B_{\rm sw}\le K N^{o(1)}\mathcal O_K,
\]

coupling active affine-component size to repeated three-copy reuse.  The
exact normalized envelope is `0.84085` on Costas 23, versus `3.9202`,
`5.9834`, and `10.9172` on radial sides 4--6.  Read
`SWAP_CELL_COMPONENT_GATE.md` and run
`verify_swap_cell_component_gate.py`.

### Latest correction: use size-biased swap orientation, not maximum degeneracy

Orient the swap multigraph by its weighted degeneracy `d_sw`.  Charge both
ordered records represented by an edge copy to its tail, with one bit to
distinguish their orientations.  This is fibrewise injective, lands in
`2N^2` cells, and has the exact bound

\[
 Q_{\rm sw}=2\sum_vd^+(v)^2
 \le d_{\rm sw}\mathcal O_K.
\]

Therefore the previously highlighted sufficient strengthening was

\[
 \boxed{d_{\rm sw}\le K N^{o(1)}}.
\]

It is no longer the preferred target.  Exact larger stresses give
`d_sw/K=1.996,1.631,2.358,2.695,3.624` on Costas primes
`29,37,41,43,47`; the last uses the low-support determinant-47 transform.
This increasing pointwise ratio is compatible with, but does not support,
an `N^(o(1))` theorem.

The charge argument only needs the strictly weaker size-biased statement

\[
 \boxed{2\sum_v d^+(v)^2\le K N^{o(1)}\mathcal O_K}.
\]

Here the edge copies may be given any orientation; the canonical peeling
orientation already has normalized values `0.500,0.352,0.361,0.440,0.739`
on those five larger Costas rows.  It therefore tolerates small dense cores
that carry negligible total mass.  This averaged orientation-energy bound,
or the fixed cross-pair moment (1.1), is the corrected lead gate.  Read
`SWAP_CELL_DEGENERACY_CHARGE.md` and run
`verify_swap_cell_degeneracy_charge.py --extended`.

The same note now records the symmetric endpoint-core form.  In a component
`z`, an edge copy between vertices `b,b'` is uniquely labelled by `a in D`,
with

\[
 q=b-a,\quad p=b'-a,\quad r=z+J(b+b'-a),
\]

and all seven members are

\[
 a,b,b',b+r,b'+r,z+(I+J)b,z+(I+J)b'\in D.
\]

This is the preferred restart form for proving the size-biased theorem.

A final coarse-relaxation audit shows why all seven memberships must remain
coupled.  If the two translated-pair conditions `b+r,b'+r in D` are dropped,
the resulting popular-correlation majorant exceeds the target normalization
by factors `2710`, `3204`, and `21873` on the exact Costas primes 11, 17,
and 23.  Thus a black-box additive-energy estimate for the adaptive popular
set cannot close the gate.  Section 7.1 of
`SWAP_CELL_DEGENERACY_CHARGE.md` gives the formula and the verifier checks
the three exact values.

### Latest support-gate audit: quadratic fibres require cubic compensation

A second exact closing target is now isolated:

\[
 |2A-(I+J)A|\ge |A|^{3-o(1)}.
\]

Because the left side is `O(m^2)` in an `m` by `m` grid, this would also
give `|A|<=m^(2/3+o(1))`.  Costas, closure, and perpendicular-ruler stresses
all have support of constant order `|A|^3`.

The naive pointwise route to this support estimate is decisively false.
For arbitrarily large integral planar distance-Sidon sets `A`, there is a
nonzero `d in D=A-A` with

\[
 |D\cap(D+(I+J)d)|=\Omega(|A|^2).
\]

The construction is an `s`-mark diagonal Golomb ruler plus one horizontal
edge whose length is a popular difference of `R-R`.  Crucially, the same
diagonal arm injects `s^2(s+1)/2=Theta(|A|^3)` triples into
`2A-(I+J)A`.  Thus heavy dilation fibres must be paid for by transverse
support growth; no subquadratic maximum-overlap theorem can work.  This is
the support analogue of the correction from maximum swap degeneracy to the
size-biased orientation-energy gate.  Read
`DILATED_PAIR_SUM_HEAVY_FIBRE_BARRIER.md` and run
`verify_dilated_pair_sum_heavy_fibre_barrier.py`.

### Cubic support is proved in the high-resonance-index affine branch

The alternate support target is now established for the main affine
low-doubling adversaries.  If `A=T A_0`, where `A_0` is vector-Sidon, put

\[
 \Gamma_T=\{z:T^{-1}JTz\in\mathbb Z^2\},\qquad
 H=|((A_0-A_0)\setminus\{0\})\cap\Gamma_T|.
\]

An exact collision calculation proves

\[
 |2A-(I+J)A|\ge
 {|A|^6\over2|A|^3-|A|^2+H(|A|^2+|A|-1)}.
\]

Thus `H<=|A|^(1+o(1))` gives cubic support.  The resonance-index formula
and a lattice-point count give

\[
 H\ll M^2/I_T+M,
 \qquad I_T=[\mathbb Z^2:\Gamma_T],
\]

for `A_0 subset [0,M]^2`.  In particular every determinant-prime affine
Welch--Costas stress has `H=O(|A|)` and satisfies the uniform explicit bound

\[
 |2A-(I+J)A|\ge |A|^3/7.
\]

This closes the generic/high-index affine branch.  The remaining support
problem is localized to low-index, nearly quarter-turn-stable additive
models.  Read `DILATED_PAIR_SUM_RESONANCE_BRANCH.md` and run
`verify_dilated_pair_sum_resonance_branch.py`.

The same note also closes the line-covered inverse branch.  If `A` has `h`
points on one affine line, those points alone give exactly
`h^2(h+1)/2` distinct values in `2A-(I+J)A`.  Hence any cover of `A` by
`|A|^(o(1))` lines already proves cubic support.  The remaining low-index
model must therefore be genuinely two-dimensional as well as sparse.

### Latest exact decomposition: spread or a Gaussian-lattice hard core

The low-index affine branch now has an exact structural decomposition.
For `A=T A_0`, put `R=T^(-1)JT` and

\[
 \Gamma=\{z\in\mathbb Z^2:Rz\in\mathbb Z^2\}.
\]

Partition `A_0` into `Gamma`-cosets with occupancies `k_C`, and let
`h=max k_C`.  Vector-Sidonicity gives the exact resonance identity

\[
 H=\sum_C k_C(k_C-1),
\]

so the collision theorem immediately yields

\[
 \boxed{|2A-(I+J)A|\ge |A|^3/(2h)}.
\]

Thus subpolynomial occupancy closes the spread branch.  In the
complementary branch, `Lambda=T Gamma` is exactly quarter-turn invariant,
so it is a rotated and scaled Gaussian lattice.  More strongly, if
`A_C=T(A_0 cap C)`, then the internal supports of distinct pieces lie in
distinct `Lambda`-cosets and therefore

\[
 \boxed{|2A-(I+J)A|\ge
        \sum_C|2A_C-(I+J)A_C|}.
\]

There is no overlap loss.  This localizes every remaining affine
counterexample to one or more literal smaller copies of the original
Gaussian-lattice problem.  It does not finish the proof: without an
ambient-sensitive density increment the largest core simply recurses to
the hard case.  Read `RESONANCE_COSET_GAUSSIAN_CORE_DICHOTOMY.md` and run
`verify_resonance_coset_gaussian_core.py`.

### Endpoint switching gives exact self-reproducing neighborhoods

There is now a compact graph form of the density-increment problem.  Put
`U=A+J(A-A)` and, for each `d in D=A-A`, take the block

\[
 B_d=A+Jd\subset U.
\]

The blocks are edge-disjoint copies of `K_k`.  If

\[
 r_x=|\{(a,d):x=a+Jd\}|
\]

and `A_x` is the set of the corresponding first endpoints, then the closed
neighborhood of `x` in the block-shadow graph is exactly

\[
 \boxed{N[x]=x+(A-A_x),\qquad |N[x]|=1+(k-1)r_x.}
\]

Thus a fibre of multiplicity `h` reproduces `h` endpoint stars with exact
local expansion `1+h(k-1)`; the only collisions are at their common centre.
This makes heavy-fibre compensation fully explicit.  The remaining global
theorem is now the assertion that these endpoint-decorated neighborhoods
cannot overlap with projective-plane efficiency.  A general linear block
system can, so the proof must retain the translate, quarter-turn, and
complete-difference decorations.  Read
`ROTATED_TRANSLATE_SELF_REPRODUCTION_GATE.md` and run
`verify_rotated_translate_self_reproduction.py`.

The same note now removes the projective-plane baseline exactly.  For
distinct outputs `x,y`, put `delta=y-x`.  Apart from their unique common
block, every common neighbor is in bijection with a pair
`(p,q) in A_x times A_y` for which

\[
 \delta+p-q=a-b\in(A-A)\setminus\{0\},
\]

with the two noncentral endpoint conditions `a!=p`, `b!=q`.  Consequently

\[
 |N[x]\cap N[y]|=k\,1_{x\sim y}+|\mathcal T_{x,y}|.
\]

The missing density increment is therefore precisely a size-biased
aggregate bound for this shifted six-endpoint count, not a bound for the
unavoidable common `K_k` overlap.

There is an exact warning: the *unweighted* aggregate is self-dual,

\[
 \sum_{\{x,y\}}|\mathcal T_{x,y}|
 =(k-1)^2\sum_z{r_z\choose2}.
\]

Every unordered pair of blocks through `z` permits exactly `(k-1)^2`
noncentral endpoint switches, and these give all transverse wedges.  Thus
ordinary inclusion--exclusion on the expanded neighborhoods simply
reproduces the original collision moment.  The next step must orient the
wedges and control a convex size-biased load, discard a structured part and
charge it to new support, or exploit genuine shrinkage of the Gaussian-core
lattice.  Do not pursue an unweighted neighborhood-overlap sum.

The exact oriented restart is now also written.  Let `H_A` be the graph on
blocks, joining two blocks when they meet, let

\[
 I=|E(H_A)|=\sum_x{r_x\choose2},
\]

and let `S_x` be the pencil of blocks through `x`.  Orient `H_A` and put

\[
 c_x=|\{d\to e:d\in S_x, e\notin S_x\}|.
\]

Propagating one base orientation across its full `(k-1)` by `(k-1)`
endpoint-switch biclique gives the exact load `(k-1)c_x` at `x`.  Therefore

\[
 |A+J(A-A)|\ge{(k-1)^2I^2\over\sum_xc_x^2}.
\]

The full support theorem follows in the collision-dominant branch from the
single pencil-orientation estimate

\[
 \boxed{\sum_xc_x^2\le I^2/k^{1-o(1)}}.
\]

This is the current smallest endpoint-sensitive density-increment gate.  A
random orientation has expected cost `sum_x(b_x^2+b_x)/4`, where `b_x` is
the unoriented pencil boundary, so independent balancing alone is
insufficient; the gain must come from a correlated orientation or a
structural split.

There is now a sharp warning about what that structural split must retain.
The punctured affine-line system in
`AFFINE_DOUBLE_ENDPOINT_ORIENTATION_BARRIER.md` has two orthogonal proper
endpoint colourings, a third proper incidence label, every pencil is a
three-coordinate matching, and every base edge has a unique pencil centre.
After deleting its diagonal cells and adjoining one isolated zero block, it
also has exactly `k(k-1)+1` blocks, every block has size `k`, and every
pencil has size at most `k`, matching the actual incidence parameters.
Nevertheless every
orientation satisfies

\[
 {k\sum_xc_x^2\over I^2}\gg k.
\]

Thus neither endpoint ordering, nor even the abstract three-coordinate
matching design, can prove the gate.  The translate identity `B_d=A+Jd`, radial
uniqueness, and complete-difference realization must remain coupled.

The same barrier note isolates one exact way characteristic zero intervenes.
Every nonidentity zero-intercept affine pencil is full and has incidence
label `p=q`.  A real full fibre of this form would give a permutation
`sigma` of `A` satisfying

\[
 \sigma(r)-x={(I-J)\over2}(r-x),
\]

a strict contraction.  Hence it is impossible unless `A` is a singleton.
This does not handle the six-distinct hard mass, but it gives a concrete
stability direction: an affine-like core must avoid every contracting
pencil or force exponential coordinate height.

The next abstraction is also settled sharply in
`THREE_PROJECTION_FIBRE_BARRIER.md`.  Real nonzero fibres

\[
 \mathcal F_x=\{(p,r,q):x=p+J(r-q),\ r\ne q\}
\]

are three-coordinate matchings, and two distinct fibres meet in at most one
cell in each of the projections `(p,r)`, `(p,q)`, and `(r,q)`.  This is an
exact consequence of oriented-difference uniqueness.  It is still not
enough: an explicit finite-field family has `Theta(k^2)` disjoint fibres,
total mass `Theta(k^3)`, and all three cross-fibre projection intersections
at most one.  Thus projective-plane efficiency survives every matching and
projection-linearity property isolated so far.  The simultaneous
characteristic-zero equation, not just its three projections, is the
minimum remaining input.

`FIBRE_CENTROID_STABILITY.md` now extracts the first quantitative consequence
of that simultaneous equation.  If a fibre has size `h=k-t` and coordinate
complements `P^c,R^c,Q^c`, then exactly

\[
 h(x-\mu)=-\sum_{P^c}(p-\mu)
 +J\left(\sum_{Q^c}(q-\mu)-\sum_{R^c}(r-\mu)\right).
\]

For `A` in a square of side `m`, all such outputs lie within
`3sqrt(2)mt/(k-t)` of the centroid.  Hence there is at most one full fibre,
and the number of fibres of size at least `k-t` is at most
`(6sqrt(2)mt/(k-t)+2)^2`.  This rigorously kills the extreme affine-plane
tail but does not yet reach the medium-rich six-distinct fibres; a
multiscale/density-increment extension is still required.

`FIBRE_PATH_CIRCULATION.md` extends the localization to genuinely medium
fibres whenever their endpoint matching is close to a circulation.  Direct
the edges `q->r` in one fibre and put

\[
 s_x=|Q_x\setminus R_x|=|R_x\setminus Q_x|.
\]

The components are directed paths and cycles, and exactly

\[
 h x=\sum_{p\in P_x}p+
 J\left(\sum_{R_x\setminus Q_x}r-
        \sum_{Q_x\setminus R_x}q\right).
\]

Thus `|x-mu_(P_x)|<=sqrt(2)m s_x/h`; every cycle label set has centroid
exactly `x`, and every path of length `ell` localizes `x` to within
`sqrt(2)m/ell` of its label centroid.  The total internal circulation has
the exact global budget

\[
 \sum_x(h_x-s_x)\le k(k-1)^2.
\]

This is the first medium-fibre consequence of the literal real equation.
It splits the next increment into high-circulation fibres, which have a
cubic global charge, and high-boundary fibres, where many path endpoints
must pay ambient height.  The exact verifier passes all 5,528 fibres of the
20-point closure witness.

`BALANCED_FOURIER_HARMONIC_GATE.md` records a tempting spectral restart that
is now **disproved**.  With `H=|hat(1_A)|^2` and `H_J=H circ J`, define

\[
 \mathfrak B(A)=\int {H^2H_J^2\over H+H_J}.
\]

The origin box proves `B(A)>=k^6/(2048m^2)`, so
`B(A)<=k^(3+o(1))` would finish the cube-root grid theorem.  Exact second
moments control every imbalanced dyadic level, initially leaving only the
balanced joint-large-spectrum power

\[
 |\{H\asymp2^ik,\ H_J\asymp2^jk\}|
 \le k^{o(1)}2^{-i-j-\min(i,j)}.
\]

The exact second moments do prove this when `i>=2j` or `j>=2i`.  Collinear
sets satisfy the valid bound `B(A)<4k^3`, and a cover by `r` parallel lines
gives `B(A)<4r^3k^3`.  But two *perpendicular* ruler arms create balanced
strips of width `s^(-2-o(1))`.  The scalable integral distance-Sidon family
from `PERPENDICULAR_RULER_OBSTRUCTION.md` satisfies

\[
 \mathfrak B(A_s)\ge s^{4-o(1)}=|A_s|^{4-o(1)},
\]

which is exponent-sharp and kills the gate by a full power.  The earlier
finite ratios below `1.10k^3` were pre-asymptotic: the rigorous lower bound
has a small constant and lives on Fourier strips represented by only one or
two rows in those computations.  A spectral successor must distinguish two
biases supported by the same two-dimensional endpoint core from biases
supplied by different perpendicular arms.

### Latest delta: size-biased eight-corner core

`SIZE_BIASED_EIGHT_CORNER_GATE.md` returns to the global transverse relation

\[
 d=f+Je,
 \qquad d,e,f\in A-A,
 \qquad d\cdot e\ne0.
\]

Decorate the three directed edges by their unique endpoint pairs.  Every
relation has eight corner projections selecting one endpoint of each pair.
If `deg_epsilon` is the corresponding projection degree, put

\[
 \delta(\rho)=\min_\epsilon
 \deg_\epsilon(\pi_\epsilon\rho),
 \qquad
 \mathcal C(A)=\sum_\rho\delta(\rho).
\]

The old pointwise claim `max delta=N^(o(1))` is false, but the proof only
needs the size-biased estimate

\[
 \boxed{\mathcal C(A)\le |A|^{3+o(1)}.}
\]

Equivalently, if `R_(>=t)` consists of the relations popular to level `t`
in all eight projections, it suffices to prove

\[
 |R_{\ge t}|\le |A|^{3+o(1)}/t
\]

uniformly.  The layer-cake identity is exact.  On the 30-, 45-, 60-, 90-,
and 120-point closure witnesses, `(max delta,C/k^3)` is respectively

\[
 (5,1.5443),\ (6,2.0990),\ (8,2.2123),\
 (9,2.7686),\ (12,3.5780).
\]

This is compatible with a logarithmic loss while the local maximum grows.
It is a new sufficient gate, not a proof.

The tempting route from `FIBRE_PATH_CIRCULATION.md` to this estimate has
also been audited sharply.  The stored closures put every relation at a
path endpoint in one of four translation-invariant corner fibres.  That is
not a theorem: a new explicit 22-point integer distance-Sidon set contains
one transverse base relation and eight certified completions which make it
internal in all four fibres.  All 231 squared distances are distinct.  Thus
path boundary cannot replace the global rich-core estimate; the aggregate
endpoint cost remains load-bearing.  The new verifier checks both the
22-point barrier and every profile above exactly.

### Newest correction: four fixed-role corners do have stopping sets

The simultaneous peeling phenomenon seen on all stored closures does **not**
survive after reducing from eight corners to the four corners which fix one
endpoint role.  `FOUR_CORNER_STOPPING_SET_BARRIER.md` gives an explicit
31-point integer distance-Sidon set and sixteen transverse relations with a
common `c_0=p` such that every key `(a_i,b_j,p)` occurs exactly twice.

The construction was first found as four edge-disjoint perfect matchings on
sixteen relation records.  Its 31 point labels are Gaussian-linear forms,
and all 465 formal squared-distance polynomials are distinct.  The stored
integral specialization has all positive distances distinct.  On its full
transverse relation set the exact core profile is

\[
 (|R|,|\operatorname{core}_{0,1,2,3}|,
 |\operatorname{core}_{4,5,6,7}|,
 |\operatorname{core}_{0,\ldots,7}|)
 =(584,32,32,0).
\]

Thus the attractive `4k^3` fixed-endpoint peeling proof is false.  The full
eight-corner core remains empty on this new adversary as well as every older
stress, so an eight-sided peeling or size-biased tail theorem remains live.
Do not invoke Latin-bitrade torsion to rule out the four-corner system: the
explicit generic realization is a positive-genus trade counterexample.
Run `verify_four_corner_stopping_set_barrier.py` for the exact certificate.

## 2. What was accomplished in the latest attack

For a distance-Sidon set `A` in an `m` by `m` grid, put

\[
 D=A-A,\qquad N=|D|,\qquad S=|D+D|,
\]

and let `J` be quarter-turn.  The earlier reductions show that the grid
upper bound follows from the adaptive off-diagonal estimate

\[
 \mathcal O_K\le N^{1+o(1)}S.                 \tag{2.1}
\]

The last several hours replaced a loose cross-fibre charge first by an exact
two-anchor graph and then by a swap-coupled six-anchor charge.

For each normal seven-incidence configuration, endpoint realization gives

\[
 \omega=(\epsilon(c),m(u)-m(c)),\qquad
 \ell=w-(I+J)q,\qquad z=w-q.
\]

For fixed `omega`, make a bipartite multigraph `G_omega` with an edge
`(ell,z)` for every configuration.  Orient each edge toward its endpoint of
smaller global degree, with a fixed tie rule.  This charge is injective
inside every rich fibre.  Together with two exact common-endpoint routes it
lands in at most `10NS` labelled keys.

The decisive exact inequality is

\[
 \sum\lambda_N^2\le
 \mathcal B_N:=\sum_\omega\sum_{e=(\ell,z)\in G_\omega}
 \min\{d_L(\ell),d_R(z)\}.                    \tag{2.2}
\]

This is not an estimate: it follows directly from the orientation.  It
removes the one-sided star obstruction which defeated the prior charge.
Any large normal second moment must now contain many edges with both anchor
degrees large in the same endpoint-midpoint graph.

The two fixed-anchor collision systems are also exact.  If two preimages
differ by `(eta,rho,pi)` in their base and two popular shifts, then the
left-anchor displacements are

\[
 \eta,\ \eta+\rho,\ \eta+\pi,\ 0,\ J\rho,
 (I+J)\rho-\pi,\ (I+J)(\rho-\pi),              \tag{2.3}
\]

and the right-anchor displacements are

\[
 \eta,\ \eta+\rho,\ \eta+\pi,\ 0,\ \rho-\pi,
 -J\rho,\ \rho-(I+J)\pi.                      \tag{2.4}
\]

The common endpoint decoration forces `eta`; it is not a free affine
variable.  Both adaptive-popular shifts remain present.

Every ordered configuration `(q,p)` also contains its swapped partner
`(p,q)`.  Retaining both endpoint labels produces four `w`-anchor cells,
and the two moving heads produce two more cells.  Charging to the least
popular of these six candidates gives a new exact envelope

\[
 \sum\lambda_6^2\le\mathcal B_6\le\mathcal B_N.               \tag{2.5}
\]

The complete charge remains fibrewise injective and now lands in at most
`18NS` labelled keys.  The larger constant is irrelevant; the substantial
gain is that a counterexample must be simultaneously dense under the swap
involution, both endpoint labels, four paired anchors, and two heads.

## 3. The older six-anchor theorem still missing

The strongest endpoint-normal target from the preceding route is

\[
 \boxed{\mathcal B_6\le N^{o(1)}|C_N|.}        \tag{3.1}
\]

where `C_N` is the multiset of normal configurations.  The two
common-endpoint charge routes need the analogous size-biased load bound.
Together these imply

\[
 \sum\lambda^2\le N^{o(1)}\sum\lambda,
\]

and Cauchy in an `18NS` target gives (2.1), hence

\[
 |A|\le m^{2/3+o(1)}
\]

and the full expected upper order for #1208.

No proof of (3.1) is known.  It remains a valid fallback, but the cross-pair
gate (1.1) is now preferable because it permits the adaptive factor `K` and
also absorbs the common-endpoint branch.  The older sufficient target for
`B_N` and its arboricity reduction remain valid as well, since `B_6<=B_N`.

For the older target there is a sharper sufficient gate.  Let `a(G_omega)` be
the multigraph arboricity.  Orienting a forest decomposition gives

\[
 \mathcal B_N\le
 2\sum_\omega a(G_\omega)|E(G_\omega)|.         \tag{3.2}
\]

Hence subpolynomial size-biased arboricity still suffices.  Moreover,
if `r_{ell,z}` is the parallel-edge multiplicity, then exactly

\[
 \mathcal B_N=
 \sum_{\omega,\ell,z}r_{\ell,z}^2+\mathcal M_3, \tag{3.3}
\]

where `M_3` is a selectable family of genuine three-edge paths, with one
nonparallel continuation at each endpoint of a central edge.  Read
`BALANCED_ANCHOR_ARBORICITY_GATE.md` for the proof and exact path forms.

## 4. Verification status

Run

```bash
python3 phase2/loop/erdos1208/verify_hybrid_endpoint_opposite_charge.py \
  --extended --literal-max
```

It verifies all charge identities, fibrewise injectivity, the original,
swapped, and fixed-head collision systems, the exact six-anchor degree
envelope (2.5), the common-endpoint resonance identities, and the negative
literal-charge control.

The complete stored suite passes for the swap-coupled charge.  Selected
profiles
`(mass,image,second moment,max load)` are:

| family | profile | size-biased load |
|---|---:|---:|
| closure 40 | `(370516,369663,372254,4)` | `1.00469076` |
| closure 80 | `(357094,356998,357286,2)` | `1.00053767` |
| Costas 23 | `(498674,493947,508164,3)` | `1.01903047` |
| Costas 41 | `(4629690,4525615,4843428,4)` | `1.04616681` |
| Costas 43 | `(8451318,8271632,8815150,4)` | `1.04305033` |

The previous one-anchor Costas-43 profile had load `1.7644` and maximum
`12`; the two-anchor version had `1.2223` and `7`.  The improvement is
evidence that swap coupling removes the observed concentrations, not
evidence that (3.1) holds asymptotically.  Read
`SWAP_COUPLED_SIX_ANCHOR_CHARGE.md` for the new exact reduction.

## 5. Other durable progress and barriers

The direct attack has accumulated substantially more than the final gate:

- Complete rectangular, Gaussian-ideal, and arbitrary oblique lattice
  fibres were classified.  The sharp universal height threshold is
  `M >> r^(3/2)`, exactly the exponent needed at the cube-root scale.
- Sparse oblique shear examples show that even near-full density, three
  affine fibre copies, radial uniqueness, and adaptive bidirectional
  popularity do not recover a polynomial complete patch.  Complete
  difference endpoint realization is indispensable.
- The orthogonal-energy problem was localized to a support-adaptive rich
  tail.  High-support Ruzsa branches, collinear branches, algebraic-curve
  branches, generic affine images, and large resonance-index branches were
  separated or closed.
- Opposite-endpoint charges, exact popular-pair rectangles, an eight-form
  fractional-basis collision majorant, and endpoint-switched two-moment
  charges remain valid fallback interfaces.
- The common-endpoint fallback is not arbitrary: a repeated local midpoint
  class forces three affine copies in `A` and squared-distance ratio
  `1:1/4:5/4`.
- Generic hypergraph codegrees, anti-Ramsey proper coloring, raw distance
  energy, DRC, additive BSG, ordinary third energy, two-sided sumset
  products, scalar Bessel positivity, full directional PSD relaxations,
  and density-to-complete-patch extraction were all shown to bottom out at
  the grid or admit explicit countermodels.  Do not restart these lanes
  without a new endpoint-sensitive ingredient.
- The positive-definite audit is now exponent-sharp as a barrier: exact
  mixed quarter-turn moments, universal radial kernels, support-packing
  duals, and scalar Bessel shell inequalities all admit genuine examples
  or pseudoconfigurations too large for a cube-root proof.  Any successful
  spectral argument must use nonlinear endpoint compatibility, not only
  positivity or the occupied-radius set.
- The prime-power upper framework has a local entropy barrier and will not
  reach exponent `1/3`; its value is the separate explicit partial result
  `0.494586`.

## 6. Best next move

Work on an endpoint-sensitive density increment inside the Gaussian core,
while retaining the `K`-scaled cross-pair estimate (1.1) as the aggregate
closing gate.

1. Use the exact resonance-coset decomposition to prune to a large
   quarter-turn-stable lattice core.  Track its index, scale, and ambient
   endpoint contribution; a recursion on core size alone is too weak.
2. On the surviving core, use the exact neighborhoods
   `N[x]=x+(A-A_x)`.  Prove a size-biased overlap bound after removing the
   unique common-block contribution.  The unweighted overlap total is
   exactly self-dual and gives no gain, so an orientation or convex pruning
   is mandatory; this is the graph version of the endpoint density
   increment.
3. Prefer the pencil-cut formulation: orient the block-intersection graph
   so that `sum_x c_x^2<=I^2/k^(1-o(1))`, but retain the full translate and
   complete-difference geometry.  The affine endpoint barrier proves that
   even two endpoint colourings plus a third proper incidence label miss by
   a full factor `k`; a random orientation likewise reaches only the
   fractional boundary cost.
4. Write every record as two incidence triples `X,Y in D^3` with
   `Phi(X)=Phi(Y)`.  Dyadically prune on the nine cross-coordinate
   codegrees at the natural threshold `tK`.
5. Use the covariance identity for the two components of `Phi` to force a
   nonquasirandom cross projection in every nonconstant-fibre core.
6. Turn that correlation into a density increment while retaining all nine
   cross projections.  Dense regularity is insufficient; the loss must be
   polynomially controlled at the sparse `K` scale.
7. The radial transversals show that the affine/common-factor system alone
   is false.  The increment must use the endpoint identity
   `widehat(1_D)=|widehat(1_A)|^2-(|A|-1)` or an equivalent combinatorial
   endpoint-switch statement.
8. The fixed-route collision displacements are
   `delta,0,delta+pi,delta+(I+J)pi,Jpi,(I+J)(delta+pi),0`.
   Any inverse theorem must retain all five nonzero translated copies.
9. If the fixed route develops a genuine counterexample, use the symmetric
   minimum over all nine `D^2` charges, then combine it with the six
   endpoint-anchor candidates by normalized minimum degree.
10. Do **not** attack the scalar balanced Fourier tail: perpendicular rulers
    disprove it by a full power.  Any spectral successor must decompose the
    contributing endpoint pieces before balancing the two orthogonal
    biases.  The physical-space `FIBRE_PATH_CIRCULATION.md` split remains
    valid: a medium fibre either has chargeable circulation or exposes many
    path endpoints whose lattice height must be tracked.
11. In the transverse formulation, prefer the size-biased eight-corner tail
    `|R_(>=t)|<=k^(3+o(1))/t` over the false pointwise corner bound.  Do not
    assume that one of the four same-sign corner fibres exposes the relation
    at a path endpoint: the 22-point internal-fibre certificate disproves
    that shortcut.  A proof must charge a *population* of simultaneously
    rich relations, not one locally affine relation.

The practical progress estimate is: the construction/verification
architecture is mature, and the full conjecture is reduced to one crisp
new combinatorial inequality, but that inequality is of genuinely
open-problem strength.  We are meaningfully closer to a proof route, not
close enough to predict completion.
